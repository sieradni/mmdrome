# mmdrome — Developer Knowledge Base

## 0. Purpose & Usage

This file is the definitive reference for developers and AI agents working on **mmdrome**. Its purpose: a fresh session — human or agent — can work correctly **without re-deriving the codebase**, at the lowest context cost, and **without the doc lying as the code evolves**.

- **Injection**: the whole file is auto-injected at session start via `opencode.json` (`"instructions": ["AGENTS.md"]`). The injected copy is a **session-start snapshot** — if you suspect drift (file changed mid-session), re-read the file on disk.
- **Usage**: grep for symbols/anchors; do NOT re-read the file wholesale — the injected copy already contains it. Deep historical detail (the *why* behind every rule) lives in `docs/DEVLOG.md`, which is NOT injected — read it on demand.
- **Anti-rot contract** (how the doc stays correct):
  1. Every §4 rule carries anchors (file, function, or test). When you touch referenced code, verify the anchor and fix it if drifted — a doc with dead anchors is worse than no doc.
  2. When a test pins a rule's invariant, tag it `[test-enforced: …]` — the test is the real documentation; the entry stays as awareness.
  3. Untested invariants are flagged `[not test-pinned]` (gap audit 2026-08-13). Invariants that *can* be tested should eventually get tests (design rule F2).
  4. Every "do NOT X" rule exists because someone broke X — preserve the why-clause. New learnings: dated detail → `docs/DEVLOG.md`; promote to §4 only if a future session could break it and no test pins it (add the date + why).
  5. Parity claims must be RE-VERIFIED against the source before pinning — never asserted from memory (2026-08-13 lesson: a misremembered `_enterBgSeq` guard was enshrined in a test before the re-review caught it).

---

## 1. Tech Stack & Framework Conventions

- **Framework**: Svelte 5 (runes: `$state`, `$derived`, `$effect`) + TypeScript + Tailwind CSS v4.
- **Build**: Vite, client-side SPA. **Deploy**: manual `npm run deploy` (gh-pages) — NOT CI-triggered.
- **Design philosophy**: minimalist; no heavy UI/component libraries; native Svelte reactivity + Tailwind + simple CSS.
- **Persistence**: Dexie (IndexedDB) for settings, EQ presets, queue, and the track-metadata cache (`src/lib/db.ts`).
- **Design rule**: logic lives in pure, injectable modules (no DOM, no Dexie, fake clocks) so it can be unit-tested; thin adapters bind DOM/Swift. This is the operational form of "clean models for intended behavior, not straightforward patches."

---

## 2. Architecture Map

### 2.1 Web audio engine (`src/lib/audioManager.ts`)
- Dual `HTMLAudioElement`s (`a`/`b`) for gapless playback, preloading, and crossfading (≤15 s, exponential ramps via dual gain nodes); crossfade EXECUTION is gated by the pure `src/lib/playbackCore/crossfadePolicy.ts` (A12).
- Routing chain: `element → MediaElementAudioSourceNode → GainNode (Track RG) → SoundTouchNode (pitch/tempo worklet) → EQ biquad chain → preamp GainNode → destination`.
- ReplayGain: track/album modes via dedicated gain nodes (`_rgGainA/B`).
- iOS background element `_bgEl` + handoff machinery — see §3.1.

### 2.2 Native iOS engine (`native/BackgroundAudio` Capacitor plugin + `native/BackgroundAudioCore` SPM core)
- JS client: `src/lib/nativePlugin.ts` (`nativeEngine`); facade: `src/lib/engineFacade.ts` (`engine` singleton — views talk to this on BOTH platforms; web delegates to `audioManager`).
- **Bridge protocol**: JS sends combined-queue snapshots (`setQueue` then `playTrackAt(index, autoPlay)`); native owns the clock; events: `trackChanged {trackId}` (**NOT index** — queue reindexing makes index mapping drift), `playbackStateChanged`, `ended`, `error`, `sleepTimerFired`, `networkStateChanged {isExpensive, isConstrained}` (low data mode; `NetworkMonitor.swift` NWPathMonitor, started unconditionally in plugin `load()`, initial path update emits the boot snapshot). `getNetworkState` answers the same snapshot on demand; `refreshQueue({tracks, activeIndex})` re-syncs the native tail without disturbing the active track; `setLoopMode` for mid-play changes.
- Swift files: `AudioEngine.swift` (graph, segment scheduling, crossfades — the heart), `NowPlayingController.swift` (lock screen), `SessionController.swift` (interruptions), `BackgroundAudioPlugin.swift` (bridge).
- `BackgroundAudioCore` is a dependency-free SPM package holding the pure cores (LoaderState, Crossfade, PitchSnap, QueueDivergence, ArtworkRequestGuard, StableID) so `swift test` can build them on macOS (see E5).

### 2.3 State, queue & playback
- `src/stores/appState.ts` — global stores: `currentTrack`, `queue` (`userQueue`/`autoQueue`/`recentTrackIds`), `playbackState`, `settings`, `library`, `loopMode`, `shuffleEnabled`, `effectiveDuration` (appState.ts:98), `sleepTimer`.
- `src/lib/queueManager.ts` — ALL queue logic: advancement, auto-queue replenish/filtering, promotion, `_mutateQueue` re-anchor choke point. Does NOT delegate back to playbackManager.
- `src/lib/playbackManager.ts` — track transitions, play/pause/next/prev/seek, crossfade handling, bg handoff, sleep parks, native engagement. Does NOT own queue logic.
- Pure cores: `src/lib/queueMutation.ts` (mutation reducers), `src/lib/recentWindow.ts` (LRU window), `src/lib/autoQueuePlan.ts` (fill planner — predicate, tiers, rank, rotation), `src/lib/metadataCore.ts` (WebDAV matching/scoring — evidence gate, ambiguity, fingerprint, mtime diff), `src/lib/playbackCore/` — policy (`advanceDecider.ts`, `retryPolicy.ts`, `bgStateMachine.ts`) + transport layer (`types.ts` `PlaybackTransport` contract, `webTransport.ts` fg adapter over audioManager, `webBgTransport.ts` iOS bg adapter, `nativeTransport.ts` iOS native adapter, `crossfadeReconcile.ts`, `replayGain.ts` — Steps 1–4 of TODO 1.0 landed). The `PlaybackManager` constructor takes injectable deps (defaults = module singletons; `AudioManager`'s a/b elements are lazy so the ctor is Node-safe) — the manager glue is pinned by `tests/playbackManagerBg.test.ts`.

### 2.4 Sync, metadata & WebDAV
- `src/lib/navidromeApi.ts` — stateless Subsonic HTTP layer + config cache (layer rule D7).
- `src/lib/syncEngine.ts` — owns ALL policy: credential resolution, cache/fallback, orchestration (`connectNavidrome`, `loadLibraryFromNavidrome` — shared pipeline: connect → setLibrary → seed → auto-scan). The load-pipeline decisions live in the pure `src/lib/navidromeLoadPlan.ts` (`planNavidromeLoad`) so the glue stays a thin interpreter.
- `src/lib/metadataScanner.ts` — `scanAll('modified'|'force')`, tag probe, match/bind/reverify/unbind services, File Matching, the force-scan heal pass (D16), and the wipe-then-relink recovery (`wipeMetadataForRelink`/`resetMetadataAndRelink`, D15). File Matching's `searchWebdavFiles` routes through the shared `searchCore` (keyword tokens over [path, filename] — a token may hit a DIFFERENT field, deliberately widened from the old whole-query substring; NO relevance weighting, the picker stays path-sorted) `[test-enforced: tests/scannerLifecycle.test.ts]`.
- `src/lib/metadataCore.ts` — pure matching/scoring core (TODO 3.6t + Phase 6): filename/tag scoring, year/duration corroboration, the auto-bind evidence gate, ambiguity ties, reverse matching, cache/sweep planning, candidate classification, `verifyEntryAgainstTrack`, the heal decision rule (`selectHealEvictions`, D16), the FNV fingerprints, and the mtime diff (`mtimeChanged`/`findChangedTracks`). DOM/Dexie-free — pinned by `tests/metadataCore.test.ts`.
- `src/lib/metadataReader.ts` — tag extraction (rating quantization), WebDAV index build (Depth fallback + crawl), chunked tag reads; a thin adapter over `metadataCore` for the pure parts (network/taglib only).
- `src/lib/tagWriter.ts` + `taglibSingleton.ts` — tag writes (POPM layout D1).
- `src/lib/webdavUtils.ts` — `webdavFetch` choke point (D10).
- `src/lib/feedbackService.ts` — `commitFeedback` single commit path (D6); `src/lib/scrobbleManager.ts` — scrobbling (A9, A11); `src/lib/sleepTimer.ts` + `sleepTimerMirror.ts` — sleep timer (E8).

### 2.4b Direct scrobbling (Last.fm + ListenBrainz)
- Layer stack (mirrors D7/D10): pure protocol core `src/lib/lastfmCore.ts` (LEXICOGRAPHIC `apiSig`, `[i]` param flattening, `planFlush`, `nextAuthStep` poll machine) → stateless method layers `lastfmApi.ts` / `listenbrainzApi.ts` (explicit creds/session args) → ONE transport choke point `lastfmTransport.ts` (**POST form-urlencoded on BOTH platforms** — field finding 2026-08-25: Last.fm rejects GET writes with error 4 "You must use POST", and ws.audioscrobbler.com sends `Access-Control-Allow-Origin: *`, so web = plain fetch (simple request, no preflight), native = CapacitorHttp; body-first error parsing keeps typed `{error:n}` codes that ride non-2xx statuses) → orchestration `lastfmAuth.ts` (desktop auth flow, session in userSettings key `lastfmSession`) + `scrobbleFlushEngine` (`scrobbleFlush.ts`, durable queue over Dexie v6 table `pendingScrobbles`). Shared MD5: `src/lib/md5.ts` (also used by navidromeApi Subsonic tokens).
- Destination fan-out lives in `scrobbleManager` (A11); outward-only heart mirror hooks `commitFeedback` (8.9).

### 2.5 UI support modules
- `src/lib/libraryFilters.ts` (shared persisted filter/sort), `viewState.ts` (view-state persistence), `thumbLoader.ts` (distance-prioritized thumbnail queue), `coverArtCache.ts` (cover-URL cache — see C5), `matchNormalize.ts` (CJK-safe normalization + search folding `normalizeForSearch`), `searchCore.ts` (keyword-token search core — token-AND/OR-fields matcher with quoted phrases, shared by the Songs/Albums/Artists views and the auto-queue filter predicate; WeakMap-memoized per-track field folding (`foldTrackFields`/`trackMatchesQueryFast`) for the per-keystroke hot loop; `rankTrackMatch` relevance ranking — field weight title>artist>album>composer/albumArtist × quality exact>word-start>substring, sum over tokens, 0 = no match — applied ONLY in SongsView when a query is active and no shared sort is set (the sort block overrides it); the App.svelte header input is 120 ms-debounced before reaching the views; `highlightSegments` + `matchNormalize.foldMapForSearch` (folded→raw index alignment for match highlighting — the map is VERIFIED against `normalizeForSearch` at every call and degrades to plain text on any mismatch, so highlights can never be wrong; used by the File Matching picker to bold matched tokens in result paths AND by TrackRow's `highlightTokens` prop (title/artist/album) + the Albums/Artists group cards — all three views pass `searchTokens` and render plain text when it is empty, so the no-search path is unchanged; the picker is SEARCH-AS-YOU-TYPE — 120 ms-debounced in-memory filter (Enter flushes immediately, no Search button), `searchedQuery` snapshots the query whose results are on screen so the no-match copy can never contradict them); `[test-enforced: tests/searchCore.test.ts, tests/matchNormalize.test.ts]`), `version.ts` (build-time version injection).
- Debug HUD (`src/components/DebugHud.svelte`, gated by `?debug` / `localStorage mmdrome:debugHud` / Settings → About / `window.__toggleDebugHud()`) — live overlay polling `BackgroundAudio.getState()` + `getDebugState()` (engine running, live schedule, gains, generations, `hasLocalURL`) plus JS queue/position; captures `error`/`trackChanged`/`ended` and `console.error` for copy-paste. Instrument for the 2026-08-22 native stall (the HUD surfaced the `no such file` temp-file race).

---

## 3. Platform Quirks

### 3.1 iOS background audio mode
iOS aggressively suspends `AudioContext` and throttles background timers/events. The PWA uses a specialized handoff mechanism owned by `WebBgTransport` (`playbackCore/webBgTransport.ts`, Step 3 of TODO 1.0):
- **Background element**: a bare `HTMLAudioElement` kept warm; playback transfers on `visibilitychange` (handler wired ONLY when `engine.isIOS` — the transport holds its own machine state, see A6).
- **Volume**: iOS ignores `HTMLAudioElement.volume` — the bg element's `volume` stays `0` forever (setting it is a no-op). Volume = master preamp while foregrounded, iOS hardware rocker while backgrounded.
- **Bypassed in bg**: SoundTouch worklet + EQ biquad chain (suspended AudioContext). Only the bg element's `playbackRate = speed` applies.
- **Media session routing**: ALL lock-screen actions (`play`, `pause`, `seekto`, `nexttrack`, `previoustrack`) call the manager, which dispatches to the transport when engaged (`sessionElement` targets the bg element while engaged) — mediaSession.ts has NO bg branches and NO audioManager import.
- **Position polling**: `timeupdate` stalls on iOS bg elements → the transport's **250 ms** tick polls position and fires `onTick` (manager refreshes `currentTime` + lock-screen position state).
- **Race protection**: the old `_handlingEnd` + sleep-park + `_enterBgSeq` trio is replaced by the machine + the transport's **settle token** (stale `play()` settles are dropped) + `onParked` reporting (the sleep park lands via `onParked(trackId)` → `sleepTimerManager.parkAtEnd` + paused state; the exit-bg park gate reports too). Queue advancement happens exactly once: the machine's trackEnded/exit-ended events are the only advance triggers, resolved manager-side (`_handleBgLoad`).
- Native iOS has its own bg path (plugin engine owns the clock; media-session commands route through the plugin) — see §2.2, E7/E8.

### 3.2 iOS viewport, safe areas & zoom
- `viewport-fit=cover` in index.html is REQUIRED or `env(safe-area-inset-*)` is always 0; `.safe-area-*` helpers in app.css (`safe-area-full`, `safe-area-x` — the latter includes left/right insets for the landscape notch); `SystemBars.setStyle` for the status bar (native only).
- **Zoom lock (3 layers)**: viewport `maximum-scale=1.0, user-scalable=no` + `ios.zoomEnabled: false` (capacitor.config.ts) + 16 px font floor on text inputs (Safari ignores `user-scalable=no`).

### 3.3 WebDAV from native
Plain `fetch` from the webview dies at CORS (the origin whitelist on the user's Cloudflare Worker hardcodes the web origin). `webdavFetch` (`webdavUtils.ts`) is the single choke point: on native it routes through `CapacitorHttp.request()` (URLSession — no CORS), translates `RequestInit`→`HttpOptions` (ArrayBuffer body→base64, binary response→chunked `atob`), and returns a minimal `WebdavResponse` shim. `NSLocalNetworkUsageDescription` in Info.plist (LAN servers). Navidrome API calls stay on plain fetch (server is CORS-enabled).

### 3.4 Swift & AVFoundation
- `AVAudioUnitGain` does NOT exist (phantom class). Gain = `AVAudioMixerNode.outputVolume`. Graph: `playerA/B → gainA/B (mixers) → mixer → timePitch → varispeed → eq → preamp (mixer) → mainMixerNode`.
- **Native speed/pitch/tape params are written ONLY inside `scheduleCurrentTrack`** (while nodes are stopped, `refreshPlaybackParams`). Live writes on a running engine wedge the time-pitch kernel → frozen/wrong-pitch audio → `play()` SIGABRTs in `AVAudioPlayerNodeImpl::StartImpl`. While playing, a param change sets `paramsDirty` → 60 ms-debounced `restartForParams()` (re-schedule from current position, cancels armed crossfade).
- Capacitor `CAPPluginCall` accessors REQUIRE the default-value argument (`call.getInt("x", 0)`; `call.getArray("tracks", [])` + `object as? JSObject` — the `JSObject.self` overload does not compile).
- MPRemoteCommand: `.enabled` is `.isEnabled`; now-playing time/queue keys are `MPNowPlayingInfoPropertyElapsedPlaybackTime` / `PlaybackQueueIndex` / `PlaybackQueueCount` (the `MPMediaItemProperty…` variants for those three do not exist).
- Swift 6: capture a local (`let scheduledIndex = activeIndex`) before using `self` in `scheduleSegment` completions.
- Lock-screen sync: `play`/`pause`/`toggle` plugin handlers call `refreshNowPlaying()` UNCONDITIONALLY after the engine command (no-op/edge commands otherwise leave the lock screen showing the stale button); `refreshNowPlaying` threads the EFFECTIVE duration (`state.duration`), not raw `track.duration` (can be 0).
- Interruptions (`SessionController`): `.began` snapshots `wasPlayingBeforeInterruption`; `.ended` calls `onResume()` → `engine.play()` only when the note carries `shouldResume` AND playback was active.
- Native EQ mapping: `NativeFilterSnapshot` type strings map to `AVAudioUnitEQFilterType` in `AudioEngine.swift::mapFilterType` (`bandpass→.bandPass`, `notch→.bandStop`, default `.parametric`); Q→bandwidth via `1/max(q, 0.05)`.
- **Standby crossfade generation**: `AVAudioPlayerNode.stop()` fires the pending completion handler, so `refreshQueue`/`setLoopMode` (which tear down armed crossfades) must not let the standby's stopped segment fake a natural track advance — guarded by `standbyScheduleGeneration`, bumped ONLY when a crossfade is armed/in-flight (`hadCrossfade` captured before the flags clear); after `finalizeCrossfadeSwitch` the former-standby node's completion IS the natural-end trigger and must stay valid or the queue stalls.
- Native sleep timer runs on the MainLoop (survives backgrounding), cancelled in `stopPlayback()`, tears down armed crossfades at end-of-track, pauses at the natural end, emits `sleepTimerFired`.
- PROPFIND/WebDAV from Swift side: N/A — WebDAV always goes through JS `webdavFetch` (§3.3).
- **Artwork fetch is race-guarded (TODO 4.4)**: `NowPlayingController` records the latest artwork request via the pure `ArtworkRequestGuard` (latest-requested + current-track), and a completion applies only when it answers that request AND the track is still current — an out-of-order completion (older fetch landing last, or a track change to one without a cover, which fires no superseding request) must never stamp stale art over the lock screen. Guard lives in `BackgroundAudioCore` so the ordering matrix is unit-tested. Swift imports are PER-FILE: any new reference to a core symbol requires `import BackgroundAudioCore` in that file — the 4.4 commit shipped without it in `NowPlayingController` and CI (run 72, `Build unsigned app`) caught it.
- **Cache filenames are STABLE across launches (TODO 4.5a)**: `destinationURL` derives from `StableID.fnv1a64(trackId)` — deterministic FNV-1a over UTF-8 bytes. NEVER revert to `String.hashValue`: it's SipHash-seeded per process, so it re-downloaded the whole queue every launch and orphaned the previous launch's files; it could also trap on `abs(Int.min)`. `effectiveDuration` (TODO 4.5b) memoizes positive computed durations per trackId — `state()` (250 ms poll + `refreshNowPlaying`) must not re-open `AVAudioFile` for zero-duration tracks while stopped; 0 is never cached (a not-yet-downloaded file re-probes once the file lands). The memo hit ALSO requires the file to still be in the loader cache (`loader.localURL(for:)` — a pure cache lookup) so an evict + re-download of a different-length file re-probes instead of serving a stale duration forever — do NOT drop that guard to save the lookup. SessionController retains its block-observer tokens and removes them in `deinit` — never discard them (registrations become unremovable, TODO 4.5c).
- **Download temp file must move synchronously before the main hop (2026-08-22)**: `URLSession.downloadTask`'s temp file is valid only until its completion handler returns. Moving it AFTER an async hop to `DispatchQueue.main` lets iOS delete the file first (`no such file` in the HUD). The loader moves synchronously on the delegate queue to `Caches/mmdrome-tracks/<FNV>` BEFORE hopping; state bookkeeping (`isCurrent`/`complete`/`store`) and stale-request cleanup still happen on main. A stale completion deletes its synchronously-moved file so it never leaks. Do NOT move the hop before the move.
- **Track file cache is robust to system purge + volume mismatch (2026-08-22)**: `Caches/mmdrome-tracks` can be purged between tracks; the parent directory is re-created immediately before every move (not only at `destinationURL` creation). `moveItem` failure (`NSCocoaErrorDomain 516` — temp and caches on different volumes) falls back to `copyItem`+`remove`. Navidrome `/stream.view?…` URLs have `pathExtension == "view"` — stripped to no extension so `AVAudioFile` probes the header; real WebDAV file URLs keep their suffix.
- **Manual Next lock-screen sync requires atomic handoff (2026-08-22)**: split `setQueue` then `playTrackAt` left `changed == false` (JS already moved `activeIndex` on `setQueue`) so `trackChanged` was suppressed and the lock screen stayed on the previous track. The bridge now exposes `setQueueAndPlay` (atomic queue+track update + immediate `refreshNowPlaying`) and `playTrack` compares by `trackId` not index; divergence/stale-generation drops are retry-visible; `AVAudioSession` is re-activated before `engine.start()` after a `stopPlayback` pause.
- **Audio-sharing mode is single-owned by `SessionController` (2026-09-09)**: the Settings → Playback → Audio Mixing toggle (`iosAudioMixing: 'exclusive' | 'mix'`, default `'exclusive'` = historical take-over) maps to category options ONLY in `SessionController.categoryOptions(for:)` (`'mix' → [.mixWithOthers]`, everything else → `[]`). `AudioEngine.ensureEngineRunning` and `SessionController.configure/setMixingMode` ALL resolve through it — never hardcode `options: []` at a `setCategory` site again, or engine restarts silently clobber Mix back to exclusive. The mode applies live (re-set category + re-activate, no restart); interruptions (calls/Siri) and route changes fire in every mode, so that machinery is untouched. JS side: `settings` object-store key (C6 config scalar) → facade `setAudioMixing` (native-only no-op on web — the PWA has no session API) → `setAudioMixing` bridge with `'exclusive'` fallback; manager pushes at `_initNative` boot and on every settings change. `[test-enforced: tests/audioMixing.test.ts — default + live push]` `[not test-pinned: Swift mapping — ios.yml build only, E5]`

### 3.5 iOS build & deploy
- **No local Swift toolchain on Windows** — native compilation is verified ONLY in CI (`.github/workflows/ios.yml`: `xcodebuild` + `swift test`; trigger: push to `main` or manual). Sideload result: unsigned IPA artifact → resign with Sideloadly (7-day expiry).
- **Checking the CI result from the dev box (no `gh` CLI needed, public repo)**: query the GitHub REST API via `read_url` — `https://api.github.com/repos/sieradni/mmdrome/actions/workflows/ios.yml/runs?head_sha=<FULL 40-char sha>` — **the `head_sha` filter silently returns 0 runs for a short SHA** (use the full SHA from `git rev-parse HEAD`). A fast run is NOT suspicious (npm cache + small project ≈ 70 s), so do not trust the wall-clock: fetch `/actions/runs/<id>/jobs` and verify the two gating steps actually ran green — `Swift package tests (pure core, macOS host)` (runs the Core XCTests) and `Build unsigned app` (compiles `AudioEngine.swift`). The same push also fires `test.yml` (JS suite) — check that run for the same SHA too. Recipe proven 2026-08-14 on run 51.
- `VITE_BASE=./` is REQUIRED for the native build (`scripts/build-ios.mjs`) — the gh-pages base `/mmdrome/` 404s under `capacitor://localhost/`. `sync:ios` = `npm run build:ios && cap sync ios && node scripts/fix-spm-path.mjs` (SPM chokes on Windows backslashes in plugin paths).
- No `.xcworkspace` in Capacitor 8 SPM mode — build the `.xcodeproj` directly.
- `BackgroundAudioCore` MUST stay its own SPM package: `swift test` builds every package target, and the plugin target fails on macOS (`import Capacitor` — XCFrameworks have no macOS slices).

---

## 4. Design Rules & Invariants

### A. Audio & playback
- **A1 — Duration truth**: `track.duration` (metadata) is the sole source of duration everywhere. `HTMLAudioElement.duration` lies (NaN/truncated/short) → early track end, broken seek clamps, wrong slider max. `effectiveDuration` (appState.ts:98) is a plain `track.duration` proxy — keep exporting it (consumed by App.svelte and QueueView).
- **A2 — iOS bg invariants**: bg element `volume` stays 0; effects bypassed in bg; media-session actions route via the manager's engaged check; 250 ms tick in bg. All bg mechanics live in `WebBgTransport` (machine + settle token + watchdog + tick) — `audioManager` has NO bg code left and NO `_enterBgSeq` exists (§3.1).
- **A3 — Native params only at schedule time**: speed/pitch/tape writes happen ONLY inside `scheduleCurrentTrack`; while playing, a change goes through `paramsDirty` + `restartForParams()`. Do NOT move param writes outside the schedule-time path (§3.4). Related UI rule: `PitchSpeedView.resetAll()` resets speed + pitch only, NOT tape mode.
- **A4 — Advance chain**: park → loop-one → advance → loop-all → stop, from the ONE `decideAdvance` (`playbackCore/advanceDecider.ts`); `fromError` skips the park (a dead stream can't play out its end). STOP is a uniform decision — the adapter maps it. `_onTrackEnded` now routes through it (the hand-rolled chain is gone, 2026-08-13); the only remaining copy site is `_handleCrossfadeEnd` (reconcile-driven, kept for the crossfade-rescue bookkeeping). `[test-enforced: tests/advanceDecider.test.ts]`
- **A5 — Retry**: one `RetryPolicy` (`playbackCore/retryPolicy.ts`): web 3× exp backoff (1 s/2 s/4 s), native 2×, bg 0 (gives up on first error). Timers + the track-keyed validity check + the give-up action live in the ADAPTER (`webTransport.ts` — give-up fires `onTrackEnded({kind:'natural', fromError:true})` so the manager's A4 chain skips the park); the manager's `onRetry` guard is `get(currentTrack)?.trackId !== trackId`. The play()-rejection loop in `webTransport.playLoaded` is DELIBERATELY outside (autoplay-policy problem, not backoff) and returns `{started, errorName}` (the last rejection's name, never invented). The manager routes on it: `AbortError` (a newer load superseded this one — rapid skip) stays SILENT so it can't clobber the new track's state; `NotSupportedError` (bytes that can never decode — including transient decoder failures under load, which surface identically per the e2e probe) advances ONCE through the fromError A4 chain instead of stranding the queue forever, bounded by `_advancingPastUndecodable` + the in-chain guard (a nested second failure stops; loop-one needs no flag case — its restart rewinds in place and never re-enters); anything else (notably `NotAllowedError` autoplay blocks) keeps the legacy stop and must NEVER skip the user's track. `[test-enforced: tests/retryPolicy.test.ts, tests/webTransport.test.ts, tests/playbackManagerBg.test.ts]`
- **A6 — Bg interlock is one reducer**: `transitionBg` (`playbackCore/bgStateMachine.ts`); states: `foreground · handoff{enter|load} · bg-playing · bg-paused · park-pending · resuming`. Pinned behaviors (corrections 1–7): exit park gated on `parkArmed && (ended || atEnd)`; exiting paused states carries the bg position; exit-ended routes through `decideAdvance` (loop-one RESTARTS); `resuming + enterBg → handoff{enter}` (re-hide re-engages); pause during a handoff cancels the swap (audible element pauses); a superseded loadRequest re-routes the exit to the fg load path. The ADAPTER (`webBgTransport.ts`) is a device driver: it owns the bg element, visibilitychange, settle token, watchdog, tick, swap, park nudge — policy exits `onLoad`/`onStop`/`onParked`/`onTick`; it NEVER touches queue/stores/sleepTimer. The exit-bg park gate reports `onParked` (manager records `parkAtEnd` + paused); the engaged-pause filter (manager) keeps a swap-settle fg pause from writing 'paused' while bg audio plays. `startBgLoad` MIRRORS the src onto the fg element — an exit-resume/carry (`resumeFg`/`carryPaused`) would otherwise play the PRE-SWAP track's audio under the current track's UI/position (2026-08-13 review finding 1, pre-existing, fixed). A GENUINE bg load failure (token-valid play() rejection) RESTORES the pre-mirror fg src — the dead URL must not outlive the failed load; a pause-dropped settle (token bumped) keeps the mirror (the fg element holds the current track's src by design). `[test-enforced: tests/bgStateMachine.test.ts, tests/webBgTransport.test.ts]`
- **A7 — Crossfade RG leak**: the standby element's replay-gain is ALWAYS reset (`standbyRgGain.gain.value = _nextTrackReplayGainLinear ?? 1`) — a no-RG track must not inherit the old track's gain. On the switch, `webTransport` re-applies the ARMED `ReplayGainFields` to the engine BEFORE the ended event (1.10-3) — idempotent vs this rule in every mode. The RG **mode** itself is pushed by the manager at every `_loadAndPlay` (`setReplayGainMode(get(settings).replayGainMode ?? 'off')`) — the settings subscription's `_initialized` guard skips its startup fire, so without the push the engine keeps the 'off' default (silent no-RG until a mode toggle). The transport only carries gains, never the mode. `[test-enforced: tests/webTransport.test.ts]`
- **A8 — prev()/volume parity**: `prev()`: `currentTime > 3` → restart at 0; else back one; else (loop-all) last track; else restart. Master volume clamp is 0–2 (200 %) on native — the preamp is a plain GainNode on web but a mixer whose `outputVolume > 1` is valid.
- **A9 — Scrobble accrual**: forward seeks ≥ `MAX_SEEK_DELTA` (5 s) are never credited (playhead re-anchors); backward jumps ≥ 5 s reset `played` to 0; accrual clamped to track duration; only `navidrome-`-prefixed tracks scrobble. There is NO `nowPlaying` Subsonic endpoint — "now playing" is `scrobble` with `submission=false`, and a completed listen is `scrobble` with `submission=true` + `time` in MILLISECONDS (Navidrome parses `time` with `time.UnixMilli`; the pre-2026-08-14 code called a fabricated `nowPlaying.view` and sent seconds). The API-shape half (endpoint, params, ms time) is pinned by `tests/navidromeApi.test.ts`; the ACCRUAL half (seek deltas, clamp, `canScrobble`) is pinned by `tests/scrobbleAccrual.test.ts` (pure `advancePlayhead` + `canScrobble`, TODO 3.10).
- **A11 — Direct-scrobble destination fan-out (2026-08-25)**: ONE accrued listen event (`ListenEvent`, built in `scrobbleManager.evaluate`) dispatches to ALL enabled destinations — Navidrome (unchanged drop-on-fail policy; its own `settings.scrobbling` + prefix + config gates) AND Last.fm/ListenBrainz legs, which are DURABLE: they enqueue into `scrobbleFlushEngine` (Dexie v6 `pendingScrobbles`, unique compound index `[kind+artist+track+timestamp]`) and survive offline/restarts. Do NOT make the direct legs fire-and-forget like Navidrome's — Last.fm is the canonical listening history; the asymmetry is deliberate. The manager itself stays gate-free beyond the shared accrual threshold — do NOT reintroduce a master `scrobbling` gate there (the 2026-08-25 review regression: it starved direct-only users); each DEFAULT leg re-checks its OWN toggle/session/token at fire time via the pure `should*Scrobble` helpers (`lastfmScrobbling`/`listenbrainzScrobbling` govern plays AND hearts for that service). Scrobble timestamp = playhead `startedAt` (unix sec). Signing: `format`/`callback` are transport-only — never signed; sort keys LEXICOGRAPHICALLY (`artist[10]` < `artist[2]`). Batches are ≤50 on BOTH platforms (POST bodies have no URL limits — the old web-JSONP 20-cap died with JSONP itself). Hearts mirror OUTWARD only via `commitFeedback`'s loved-flag diff IN BOTH rating-source branches (the navidrome branch returns early — a tail-placed fan-out silently never runs for navidrome users); rating edits with unchanged hearts must not re-deliver; there is NO reverse import (Last.fm exposes no love timestamps → no merge basis). Time-sensitive kinds (`lfm-scrobble`, `lb-listen`) expire after 13 days (Last.fm rejects older listens); poison rows drop at 8 attempts; rate limits back off 15 min. `[test-enforced: tests/scrobbleManagerDestinations.test.ts, tests/scrobbleFlush.test.ts, tests/lastfmCore.test.ts, tests/lastfmApi.test.ts, tests/listenbrainzApi.test.ts, tests/lastfmAuthFlow.test.ts, tests/md5.test.ts]`
- **A10 — Pitch snap + tape persistence (persisted stores, engine-applied in order)**: `tapeMode`, `snapTolerance`, `playbackSpeed`, `pitchOctaves`, `masterGain`, `shuffleEnabled`, and `loopMode` are `persisted(key, initial)` stores (`appState.ts` → `persistedStore.ts`): the store is the single source of truth, Dexie persistence lives in the STORE layer (the factory's subscription persists on change only AFTER `restore()` resolves, so the initial/module-load fire can never clobber a saved value), and `initStores` restores them once. They are NOT in `SettingsMap` and NOT `updateSetting`-driven (the dead `tapeMode`/`snapTolerance`/`playbackSpeed`/`pitchOctaves`/`masterGain` keys were removed from `loadSettings`). The ENGINE push lives in the manager: `_applyPlaybackParams()` (called from BOTH `_initWeb` and `_initNative`, routing through the facade so web/audioManager and native/plugin share one path) applies snap-tolerance BEFORE pitch — `setPitchOctaves` quantizes against the current tolerance, so pitch-first would re-snap a saved off-grid pitch against the 0.15 default. `_subscribeShared` is now REACTIONS-only (settings crossfade/replay-gain, speed→bgTransport, and the queue/library/scan/shuffle/sort syncs) — it returns unsubscribe handles collected into `_unsubscribers` and released by `destroy()`; it no longer persists (`setSetting`) or echoes values back to the engine (the facade/audioManager callbacks already write the store and push the engine, so the echo was redundant). The web audioManager callbacks (`onSpeedChange`/`onPitchChange`/`onTapeModeChange`/`onSnapToleranceChange`) and the facade's native branches still feed the stores; the facade's `setMasterVolume` also writes `masterGain` (the views call only `engine.setMasterVolume`). **Snap tolerance unit is SEMITONES (0–0.5, default 0.15)** — not octaves; the old Settings slider was octave-scaled and its octave branch was mathematically dead (octaves ⊂ semitones, so `semitoneDist <= octDist` always). Snap = pure `snapPitchToSemitone` (`playbackCore/pitchSnap.ts`, mirrored in `BackgroundAudioCore/PitchSnap.swift`), applied at the engine boundary (audioManager `_snapPitch` web / facade native / Swift) so `engine.pitchOctaves` returns the snapped value and the view's thumb sticks to the grid. The rounding tie breaks HALF AWAY FROM ZERO on BOTH sides (TS re-implements Swift's `.rounded()` default — plain `Math.round` would round negative halves toward +∞ and diverge); the native facade also clamps pitch to [-2, 2] before snapping (web/Swift parity). `setSnapTolerance` re-snaps the current pitch on all three boundaries (widening the tolerance can pull an off-grid pitch onto the grid; a snapped value is already on-grid so the common case is a no-op). UI lives ONLY in `PitchSpeedView` (Settings no longer has Tape Mode / Snap Tolerance). `[test-enforced: tests/pitchSnap.test.ts, tests/playbackManagerRestore.test.ts, tests/persistedStore.test.ts]`
- **A12 — Seek owns the transition state (crossfade × scrubbing × short tracks, 2026-08-25)**: a user seek NEVER executes a fade. Seeking INTO the current track's window LATCHES suppression for that track instance (`audioManager.markUserSeeked` → `_seekSuppressed`; cleared when `setNextTrack` arms a fresh target = next load, and by `_cleanupWebAudio`) — held-drag scrubbing must not chain one fade per `oninput` event (each switch flips the active element, `sliderMax` follows the NEXT track, and the held thumb maps into the new element's own window: the cascade fed itself). Any IN-FLIGHT fade COLLAPSES on seek (`_collapseInFlightFade`: freeze both gains, active → 1, retire old now) — the switch is already committed queue-wise at execute time. The monitor + bg-exit resume execute ONLY through the pure gate `evaluateCrossfadeGate` (`playbackCore/crossfadePolicy.ts`): durations gate BOTH sides — an unknown or short TARGET (`nextDuration < fadeDuration`; null = unknown = too short) natural-ends instead of fading in blind (native `crossfadeReadiness` parity; web previously gated only the current side). The fade's delayed retire is a TRACKED timer (`_retireTimer`) validity-checked by `shouldPauseOldElement(oldEnded, oldIsActive)` — with alternating elements a stale fire could pause an element that became ACTIVE again (the "next song pauses afterwards" bug) — and only ONE fade may be in flight (`_fadeInFlight`). The manager routes fg seeks through `markUserSeeked(clamped)`; bg-engaged and native seeks skip it (no fg machinery applies). Native mirror: pure `isSeekInCrossfadeWindow` (`BackgroundAudioCore/Crossfade.swift`) latches `seekSuppressedTrackId` in `AudioEngine.seek` (cleared per track instance in `playTrack`; guards in monitor setup AND tick); mid-fade seeks already collapse natively via `cancelScheduled` + `scheduleCurrentTrack`'s gain reset. iOS-bg note: the enter-bg swap tears down the MONITOR but deliberately leaves `_fadeInFlight`/`_retireTimer` alone — a fade interrupted by backgrounding has its retire timer suspended, so `resumeCrossfadeAfterBgExit`'s immediate execute may be blocked until the overdue timer fires on resume (self-heals ≤1 tick). Do NOT clear `_fadeInFlight` in `reviveContext` to "unstick" this — that resurrects overlapping fades, which the one-in-flight rule exists to prevent. **Queue mutations re-arm the target (2026-08-29)**: ANY queue change (remove/reorder/clear/fill re-rank) re-arms the armed next track from the LIVE queue via `_rearmCrossfadeTarget()` on the manager's queue subscription (web only — native re-arms via the snapshot sync; microtask-coalesced so a same-task write burst collapses into ONE re-arm reading the final snapshot — scheduleSync convention, do NOT re-arm synchronously per write; runs even while bg-engaged — a hidden scan/fill re-rank must not leave the fg target stale for the plain-resume exit, and the iOS-hidden guard keeps the fg monitor from fading while hidden; skipped under loop-one, where the arm is always cancelled — the loopMode subscription keeps that true mid-track: flipping TO 'one' cancels an armed fade, flipping AWAY re-arms); `_setupNextTrack` computes the target through `advanceTargetIndex` (playing-track-aware), NEVER `activeIndex + 1` — after an active-row removal the row AT `activeIndex` IS the next row, and `+1` would arm one past the highlighted successor. `_setupNextTrack` also RE-VALIDATES its target ID against the live queue after the URL resolves and drops a stale arm — compare IDs, NEVER indices (a removal can slide a different row into the same index). The preloader (`preloadNext`) uses the same `advanceTargetIndex` start so its cache warm never disagrees with the arm. Do NOT "simplify" the target back to `activeIndex + 1` or drop the re-arm: a stale arm crossfades into the REMOVED row at the transition point, and the crossfade-end reconcile then RESCUES it back into the user queue at the top — the removed-song autoplay + scrambled-order bug (queue plays the removed track, then a just-played track, before recovering). `[test-enforced: tests/crossfadePolicy.test.ts, tests/playbackManagerSeek.test.ts, tests/webTransport.test.ts, tests/playbackManagerAdvance.test.ts]`

- **A13 — Low data mode & server transcoding (2026-09-06)**: `effectiveLowData` (`networkMode.ts`) is THE gate — NEVER read `settings.lowDataMode` raw. Composition: manual toggle OR (`lowDataOnCellular` AND cellular known) OR the OS Low Data Mode flag (`isConstrained`, native). Cellular truth: native = plugin `getNetworkState`/`networkStateChanged` (NWPathMonitor, exact); web = Network Information API hint ONLY (`effectiveType` 2g/3g — '4g' is ambiguous, so Safari NEVER engages; never present the web hint as exact). `initNetworkMode()` is awaited in App.svelte onMount BEFORE the Navidrome pipeline (everything earlier is local-only) and must never block boot on a plugin failure (`known:false` → falls through ungated). What LDM gates (all read the derived store): the boot auto-scan (`planNavidromeLoad` `lowData` → `scanWebdav:false`, suppressed like offline — the library load itself NEVER), the boot tag probe, the scrobble flush auto-drain (`setAutoFlushEnabled` — `kick`/tick no-op, explicit `runNow` unaffected, durable queue untouched), and the DIRECT Navidrome scrobble legs only (`lowDataSuppressesNavidromeScrobble` — Last.fm/LB stay durable per A11). **Auto-preload does NOT gate (2026-09-07 correction — the plan's own Principle wins):** the plan's §2 Principle says auto-preload "stays ON in LDM (it's bounded to the next few tracks and is what makes LDM streaming viable on a marginal connection)" — preload IS the counter to intermittent connections, the exact condition LDM describes; the 2026-09-06 commit gated the web poll and the native preload count to 0 against that Principle (the §4 table rows 3/4 contradict it), and both gates are REMOVED. Do NOT re-introduce an LDM gate on either preloader — the serialized one-fetch-per-tick cadence (A14) is the bandwidth-friendliness mechanism, not a mode gate. Engage edge cancels an ACTIVE scan via `cancelScan()` (D4-resumable honest landing; idle cancelScan is a harmless gen bump — `cancelled` resets at each `runScan`) AND aborts an active standalone tag probe via `cancelTagProbeIfActive()` (`metadataScanner.ts` — unconditional `tagProbeGen` bump + `tagProbeState` reset; the post-scan tail and the boot/restore probe both run while `metadataScanState` is terminal, so the scan-status guard alone misses them) and suspends the flush gate (`setAutoFlushEnabled(false)` — the App.svelte boot read covers only the pre-manager state; the TRANSITION lives in the manager edge). Lift edge re-enables the flush gate THEN kicks one drain (order matters — the kick no-ops while gated) and there is deliberately NO make-up scan or probe (no suppressed-op backlog exists — same reasoning as C7; the next natural trigger covers it). Transcode: ONE pure decision `transcodeParams` (`transcodePolicy.ts`) consumed by the three URL sites (`_resolveUrl` web fg+bg, `_buildSnapshot` native — every row shares one decision per snapshot); default format **opus** (= Navidrome's own `DefaultDownsamplingFormat`), `transcodeFormat` is a free string (built-ins opus/mp3/aac/flac + custom server formats), mp3 fallback ONLY on a persisted probe verdict `'unsupported'` (`formatProbe.ensureFormatProbe` fire-and-forget at connect; `'unknown'` never persists — no probe pass, no fallback, ever; failed probes retry next boot). `buildStreamUrl(config, id, transcode?)` is byte-identical without the third arg and appends `format`/`maxBitRate`/`estimateContentLength=true` with it. A transcode-affecting settings change (mode/format/bitrate/probe) drops the armed web crossfade target (cancelNext + re-arm → fresh params), re-syncs the native snapshot, and sweeps the web preload cache of format-bearing entries (`sweepStaleTranscodeEntries` — old-param keys can never match again and would waste slots against the 50-entry FIFO) — the CURRENT track is never re-fetched mid-play. Thumbnails: LazyThumb's restored `size` prop (128 rows / 256 grids / 512 now-playing; TrackRow rows default) steps DOWN one canonical level under LDM via `effectiveThumbSize` — canonical sizes only (96/128/256/512) so the server's resize cache is reused. `[test-enforced: tests/transcodePolicy.test.ts, tests/buildStreamUrlTranscode.test.ts, tests/lowDataMode.test.ts — the composition matrix, the flush autoFlush gate (kick gated / runNow alive), the engage edges (ACTIVE-scan-only cancel via cellular AND manual toggle, idle no-op, probe abort + flush suspend on engage), the lift kick-once + gate flip (held rows deliver — pinned on a fresh engine over the memory store), the probe-abort edge (`tagProbeState` active→idle observable-effect pin) and the no-make-up lift, the planner's LDM scan suppression (`tests/navidromeLoadPlan.test.ts` — `lowData:true` → `scanWebdav:false` with the load itself ungated), the transcode-change invalidation edges (native re-sync / web re-arm by delta, init-fire suppression), and the A11 asymmetry under LDM (direct Navidrome legs fire pre-LDM then go silent, LFM/LB legs keep enqueueing durably — pinned through the real defaultDestinations wiring, non-vacuously with a live config)]`
- **A13b — LDM-transition transcode invalidation + variant-aware native cache (2026-09-10)**: the `transcodeKey` edge (mode|format|bitrate|probe) ignores LDM state, so the `effectiveLowData` edge in `_subscribeShared` ALSO invalidates transcoded URLs when `transcodeMode === 'lowData'` — native `_scheduleNativeQueueSync` (engine-side auto-advance + lock-screen skips play the tail verbatim), web `cancelNext` + `_rearmCrossfadeTarget`; the current track is never re-fetched. Scoped to `'lowData'` by construction ('off' has no params, 'always' URLs are invariant — re-syncing there is pointless bridge churn). Web lift sweeps format-bearing preload entries; engage never sweeps (raw entries are the offline buffer). `[test-enforced: tests/lowDataTranscode.test.ts B1–B5]` The native loader cache is VARIANT-aware: one trackId can hold raw OR transcoded bytes, keyed by `transcodeCacheKey(trackId, TrackVariant(url))` (pure `BackgroundAudioCore/TrackVariant.swift` — variant parse, `key`, quality `rank`, `shouldServeCached`), filenames hash the composite key (4.5a determinism preserved; pre-variant bare-FNV files orphan harmlessly). Serve policy is preserve-unless-upgrade (product decision): a cached variant serves any equal-or-lower request (never re-download just to downgrade), a higher request evicts + re-downloads; raw outranks all transcodes, transcodes rank by maxBitRate, same-rate cross-format ties serve cached. In-flight claims are per-variant (parallel variants can't chain onto wrong bytes); corrupt-evict is variant-scoped, queue-cleanup evicts all variants. `[test-enforced: TrackVariantTests.swift — pure Core, ios.yml-gated; the TrackFileLoader adapter itself is plugin-target (imports Capacitor) so macOS `swift test` cannot cover it — E5]`. User-visible: Settings → Data & Network shows the live LDM state + reason (`low-data-status`), Streaming Quality shows the effective variant (`transcode-effective`, probe fallback resolved through the same `resolveTranscodeFormat` — never a copy), and the Debug HUD shows `ldm:` + `stream:` plus both in its Copy payload (built via the same `transcodeParams` the URL layer uses).
- **A14 — Preload offline-buffer contract (2026-09-06)**: the web preloader is a SERIALIZED priority fill — ONE fetch per poll tick (1 s cadence), the window computed via `advanceTargetIndex` (the SAME playing-track-aware function as the crossfade arm, A12/B2 — the cache warm can never disagree with the arm), so queue order IS the download order and the next track never waits behind a later one. Offline resilience is asymmetric by kind: a network EXCEPTION (fetch throw/abort/15 s timeout) never counts — the head keeps its place and the next tick retries, so captive portals and dead-cellular blips self-heal with no event wiring — while a SERVER non-ok counts per-URL and 2 strikes mark the URL dead for the session (`deadUrls`, `NON_OK_DEAD_THRESHOLD`) so one vanished file cannot head-of-line block the whole window. ALL THREE web load paths route through `resolveSrc(url)` (`_loadAndPlay` fg, `_setupNextTrack` crossfade arm, `_bgLoad` bg advance): a cache HIT returns a blob URL (the track plays fully offline), a MISS returns the raw URL (the element still streams — a miss must never hard-fail a load). The `_bgLoad` routing was the fix for the "preloaded song doesn't play when I lose connection" report: the bg element bypasses the HTTP cache's ability to hide a dead connection (a stalled range request fires NO 'error'), so a raw-URL load over a dropped connection hung in `waiting` forever even with the track sitting in the preload cache. Cache keys are the EXACT resolved URLs (transcode params included) — a param drift must miss, never serve stale audio (that's also why `sweepStaleTranscodeEntries` exists, A13). The poll bails on a paused element, on missing metadata duration (A1), and while `remaining > 30 s` UNLESS `bufferCoversEnd` — the playhead's buffered range already extends to (metadata) duration − 0.75 s, meaning the current track needs no more bandwidth and window preloading is free (the user's "current track fully downloaded first" mental model; browsers that cap their buffer below the file length never satisfy it and fall back to the time rule; coverage is measured from the PLAYHEAD's range, not the furthest buffered end — a buffered tail behind a gap doesn't count). Native parity needs none of this: the Swift loader's disk cache + `loader.localURL(for:)` already plays offline (evict-on-corrupt; offline advance errors loudly and the A5 retry chain re-engages). Session-lifetime failure books (`nonOkFailures`/`deadUrls`) are cleared only by `__resetForTests` — do NOT add a production reset (a dead URL is dead for the session by design). `[test-enforced: tests/preloader.test.ts — serialized order, cached-skip, exception-retry, dead-row, resolveSrc hit/miss/drift, LDM stays-ON (fill continues, cadence unchanged), pause gate, remaining gate + buffer-coverage early start (playhead-range coverage, partial coverage defers); tests/playbackManagerBg.test.ts — the two PRELOADED offline-advance pins (bg `startBgLoad:blob:` and fg element-src blob); tests/e2e/offline-advance.spec.ts — the BROWSER-level pin, 5 scenarios: the original mid-track-drop advance (blob src + playhead + dead raw URL never assigned), the FULL window fill in queue order surviving two offline advances, the bg handoff mid-track with a dead connection (visibilitychange swap → cached bg advance → fg resume), the A13 transcode sweep (format-bearing entries die, raw entries + the offline buffer survive, the playing track never gains a format param), and dead-URL resilience (a vanished 404 file goes dead after two strikes while the window behind it keeps filling)]`

### B. Queue
- **B1 — Mutations re-anchor by id**: every length-changing mutation routes through `queueManager._mutateQueue` → pure `applyQueueMutation` (`queueMutation.ts`), which re-anchors `activeIndex` by the pre-mutation active id. **Invariant: `combined[activeIndex]` points at the playing track whenever it remains queued.** Transitions that change the active id (advanceTo, native re-adopt, the crossfade **rescue** — a mid-fade target removed from the queue is re-inserted at the top of the user queue via a direct `queue.update` + `saveQueue`, deliberately NOT routed through `_mutateQueue`) must NOT route through it. `null` = explicit no-op, no store write. `[test-enforced: tests/queueMutation.test.ts — 10k×6-seed fuzz, tests/crossfadeReconcile.test.ts]`
- **B2 — `removeFromUserQueue` = DELIBERATE deviation**: active-row removal anchors to POSITION, not the id — the removed id may be the playing track itself, so `indexOf` fails by construction. Semantics (2.4, decided 2026-08-14 — option b) live in the pure `queueMutation.removeFromUserQueue`: removing a preceding row decrements the anchor; removing the ACTIVE row KEEPS the index so the highlight slides to the next playable row (never the already-played predecessor); removing a following row is a no-op anchor. Removing the active LAST row lands the anchor past the new end (out of range) — every consumer bounds-checks, so it degrades like `-1` (no highlight; end-of-track advance stops since nothing follows). **Advance-chain corollary**: after an active-row removal the playing track is NOT in the queue, so `combined[activeIndex]` is the next row — the advance paths (`_hasNextQueued`, `advanceQueue`, `next`) target `activeIndex` itself (pure `queueMutation.advanceTargetIndex(q, combined, playingId)`); the plain `activeIndex + 1` would read "queue ended" and stop/wrap, or skip the highlighted row. `playingId === undefined` keeps the plain `+1` (end-of-queue refill path). Do NOT convert to the id re-anchor; do NOT "simplify" the advance paths back to `activeIndex + 1`. **Removal skips NOW, on both platforms (2.7, decided 2026-08-14)**: removing the PLAYING row advances immediately — not at the track's natural end — **when a successor exists**. `removeFromUserQueue` returns the removed id; `QueueView` reports it to `playbackManager.handleQueueRowRemoved`, which runs the same A4 chain with the park skipped (the skip supersedes the end-of-track sleep park, matching the engine's divergence stop cancelling its timer) and loop-one disabled (a removed track is unloopable — it must never restart). Native needs no JS: the 1.4 divergence `ended` drives the same chain engine-side (even under loop-one, the 'restart' engage targets `activeIndex`, which post-removal IS the next row). **No successor (the removed row was the active LAST one, or the queue emptied) → play the track out on BOTH platforms** — `handleQueueRowRemoved` early-returns when `_hasNextQueued()` is false, because the native sync guard bails on the out-of-range index (and the Swift empty-snapshot guard no-ops) so the engine finishes the song, then the natural-end chain stops/wraps; web must not cut the audio. Belt-and-suspenders for the same invariant: both loop-one RESTART cases (`_onTrackEnded` web, `_onNativeTrackEnded`) stop instead of restarting when the current track has left the combined queue — a played-out removed row can't re-engage (its index is out of range). `[test-enforced: tests/queueMutation.test.ts, tests/playbackManagerAdvance.test.ts, tests/playbackManagerNative.test.ts]`
- **B3 — Recency window**: `recentTrackIds` (`RECENT_LIMIT = 100`) is the anti-repeat LRU: Clear Queue KEEPS it, Play All RESETS it; inscribe on leaving the active slot; loop-one restarts and same-index resumes do NOT. Survives queue trims — the Clear-Queue replay case is why it exists. `[test-enforced: tests/recentWindow.test.ts]`
- **B4 — Tier-3 recycling admits user-queued tracks — intentional** (starvation tradeoff 2026-08-10; excluding `inUser` can stop playback at queue end). Do NOT "fix" without revisiting the tradeoff. Promotion COLLAPSES cross-section duplicates: `promoteActiveTrack` dedupe-appends a user slot only when none exists and slices the auto queue at the copy. `[test-enforced: queueMutation collapse scenarios]`
- **B5 — Auto-queue scope**: `albumScope`/`artistScope` set ONLY when playing from album/artist detail views (or Play All), NOT on view entry/exit; cleared on plain TrackRow play (no `onplay` prop) and on shuffle toggle. Scopes live in the dedicated session-only `autoQueueScope` writable, split from the persisted filter fields (`autoQueueFilterFields`) and recombined by the derived `autoQueueFilters` — NEVER persist the scopes (they're absent from the persisted type by construction). The fuzzy `searchQuery` IS a persisted filter (product decision 2026-08-14 — it's how the user keeps a whole artist flowing without exact-scope semantics); do NOT move it to the session side. `[test-enforced: tests/autoQueueFiltersPersistence.test.ts]`
- **B6 — Rotation anchor**: non-shuffle rotation anchors to the LAST user-queue entry (not the active track — the user may insert songs past it; the visible queue must stay stable except appended auto tracks). `queueWrapNotice` fires only when nothing ranks after the anchor (`splitAt < 0`).
- **B7 — Auto-queue follows the shared sort**: non-shuffle fill ranks the whole library with the shared `libraryFilters` sort (`buildOrderRank`, ties by library index); a sort change rebuilds the auto queue. Album/artist-scoped pools follow the global sort too. The fill decision lives in the pure `planAutoQueueFill` (`autoQueuePlan.ts` — predicate, tier-1/2/3 admission, rank, anchor rotation; `queueManager` is a thin interpreter that applies the plan and permutes the FULL pool before slicing in shuffle mode). Ranks are SORTED POSITIONS — the old code ranked by pre-sort library index, which silently discarded the sort (2026-08-14 fix). `autoQueueEmptyNotice` fires when a fill adds nothing (inverted ranges, exhausted pool) and clears on the next successful fill; the Queue panel also validates inverted ranges via the pure `filterRangesValid`. `[test-enforced: tests/autoQueuePlan.test.ts, tests/queueManagerFill.test.ts]`
- **B8 — Promotion kept**: played tracks stay listed in the user queue — intended behavior (product decision 2026-08-11). `queueSize` counts `userQueue.length` only.

### C. Settings & UI state
- **C1 — Stores are the single source of truth for settings inputs**: every input calls `updateSetting(field, value)` directly on `input`. NEVER reintroduce local mirrors + debounce + sync `$effect` (the 2026-08-11 fix; the old debounce caused stale-value action clicks). `commitCredentials()` (SettingsView) trims URL/user fields and persists BEFORE any network call — every action handler awaits it.
- **C2 — Session-only state**: Settings tab/scroll persistence uses `sessionStorage` — NEVER localStorage (no credential mirroring). Restore synchronously at component init (not onMount) or the save effect clobbers it. The SAVE side must gate on a `restored` flag flipped after the onMount restore (SettingsView does): the first save-effect run must never write `scrollTop 0` over the restored value — ordering-defense (the race doesn't reproduce on Svelte 5.56.7, but the scheduler has shifted across 5.x releases), pinned end-to-end by `tests/e2e/viewstate.spec.ts`.
- **C3 — Shared filter/sort**: `libraryFilters` store is the single filter/sort source for Songs/Albums/Artists (`applyFilterSort` with any-track album/artist rating semantics via `ratingOf`; `avgRating` averages only rated tracks). Svelte `class:name` directives can't contain `/` (e.g. `class:bg-white/15` — parse error): use a computed class string.
- **C4 — Rating-slider vs scan race**: sliders commit on the FIRST `input` tick (row flips `pending_sync` instantly → scanner skips it), `dragging` guard in the re-sync effect, final commit on `change`/`blur` (modal-close mid-drag keeps the edit). All three views (DetailView/TrackOptionsView/TrackDetailsModal).
- **C5 — Cover URL cache**: `getCoverUrl` (`coverArtCache.ts`) keys by `baseUrl|username|password|trackId-size` — auth token/salt and baseUrl are baked into the URL, so switching servers or credentials must never serve stale cached URLs.
- **C6 — Persistence boundary: `persisted` stores vs the `settings` object store**: engine-bound playback params and scalar engine state — `playbackSpeed`, `pitchOctaves`, `tapeMode`, `snapTolerance`, `masterGain`, `shuffleEnabled`, `loopMode`, `eqBypassed`, and the queue filter fields (`autoQueueFilterFields`) — live in `persisted(key, initial)` stores (`persistedStore.ts`), NOT the `settings` object store + `updateSetting`, and NOT a hand-rolled `getSetting`/`setSetting`/subscription dance. `persisted` keeps persistence in the STORE layer: its subscription persists on change only after `restore()` resolves (so the module-load/initial fire can never clobber a saved value), `restore()` is awaited once by `initStores` (or the owning module's init, e.g. `initEqStore`), and a rejecting write logs a warning instead of becoming an unhandled rejection. Engine side-effects stay OUT of the store layer — `playbackManager._applyPlaybackParams` reads `get(store)` and pushes in the required order. Reactive CONFIG scalars (`preloadTracks`, `crossfadeDuration`, `replayGainMode`), policy flags, and credentials stay in the `settings` object store (single source, one `settings.subscribe` reaction in `_subscribeShared`). The `persisted` factory takes an optional `decode` hook for value-format migration (the `autoQueueFilters` row was a JSON string before 2026-08-15). Session-only state NEVER goes through `persisted` — the queue scopes are their own `autoQueueScope` writable by construction. `[test-enforced: tests/persistedStore.test.ts, tests/autoQueueFiltersPersistence.test.ts]`
- **C7 — Continuation affordances for cancellable long ops (2026-09-06)**: a cancelled operation lands with HONEST copy (what happened, what survived) and gets a dedicated continuation button ONLY when the natural next click is absent, ambiguous, or mislabeled. Applied: Push has NO resume button (the pending_sync table IS the resume state — pressing Push Changes again re-derives exactly the un-pushed rows; a second button would be a duplicate label whose count would compete with the dialog's live derivation — the D5 two-sources problem); Navidrome load has NO resume button ("Connect & Load Songs" already calls the same handler, nothing partial survives a load cancel — never-apply-partial — and "Resume" would falsely imply progress exists to continue; the cancelled landing is honest yellow copy only); the scan DOES get "Resume scan" (the shape fork is real — a cancelled force scan must resume as force, its heal pass only runs there — and partial work persists; `progress.cancelledShape` carries it, cleared by the next scan-state write). Do NOT "complete the set" by adding buttons where this rule fails all three tests.


### D. Metadata / WebDAV / Navidrome
- **D1 — POPM layout is FORK-specific — do NOT "spec-fix" it** (`tagWriter.ts`): the taglib-wasm fork's model is `[owner \0][rating byte][counter(4)]` (rating immediately after the null terminator). Writing the ID3v2.3 §4.15 order (`[owner \0][counter][rating]`) reads back as **0**. The WRITE map (`ratingToMp3Popm`) and READ map (`popmToLocalRating`) are **asymmetric by design** (MusicBee-calibrated write; write 90 → POPM 204 → read 80). Do NOT align them — it breaks MusicBee interop; the read map wins on rescan. `[test-enforced: tests/tagWriter.test.ts — round-trip + asymmetry drift]`
- **D2 — Rating read scaling**: `getRating()` is normalized 0..1 for EVERY format (ID3v2 POPM/255, Vorbis RATING/100, MP4 freeform atom/100). Only `fileType === 'mp3'` uses the POPM grid (`popmToLocalRating(round(r*255))`); other formats map `round(r*100)`.
- **D3 — M4A loved flag**: MusicBee writes it as freeform atom key `LOVERATING` (no space); ID3/TXXX uses `LOVE RATING`. Find by normalized key (`k.replace(/\s+/g,'').toUpperCase() === 'LOVERATING'`).
- **D4 — Scans must skip `pending_sync` rows** (and scans are user-cancellable: `cancelScan()` bumps `scanGen`/`tagProbeGen` — the credential-swap mechanism — and lands the state on an HONEST complete with a `Cancelled — N of M processed` annotation; processed rows keep their persisted results and the next ordinary scan resumes from the freshness fingerprints, so cancel is RESUMABLE, never a rollback): `metadataScanner.processItem` skips rows whose existing `syncStatus === 'pending_sync'` (counted scanned, not failed) — a force rescan before Push would wipe local edits. **Do NOT regress the skip.** `[test-enforced: tests/scannerLifecycle.test.ts]`
- **D5 — Push provenance**: no `webdavPath` → skipped; no `webdavBase` → skipped (legacy/unverified); `webdavBase !== current baseKey` → `wrongServer` (distinct count); `ignored` → skipped even with a path. After each successful PUT, re-check the LIVE row via the pure `shouldKeepPushPending` (`pushReconcile.ts`): newer `rating`/`loved`/`webdavPath`/`webdavBase`/`comments`/`matchSource`/`ignored` edits are re-upserted as `pending_sync` (never flattened to `synced` from the stale snapshot — flattening writes the whole snapshot back and would lose a mid-push re-bind's NEW path/base, a comment, a manual-bind marker, or a dismissal). The `current baseKey` and the scan's `webdavBase` stamp both derive from the ONE `webdavBaseKey(url, user)` (`webdavUtils.ts` — trim url + trim user; trailing slash AND case preserved deliberately, so the key format matches how existing rows were already stamped), so stray whitespace can never flag the whole library `wrongServer` (TODO 3.5). The push CONFIRMATION count (`pushChanges`) uses the SAME derivation and excludes `ignored` rows (Push skips them — counting them overstated the dialog; TODO 3.8c) — enforced by CONSTRUCTION now: both the loop and the dialog classify rows through the ONE pure `classifyRowForPush` (`pushReconcile.ts`; buckets `no-path`/`ignored`/`no-base`/`wrong-server`/`pushable`, first-match precedence mirrors the loop's original early-continue chain — no-path before ignored before base), so the dialog's `pushable` count can never drift from what the run actually does. A server that sends no ETag makes the MOVE a blind overwrite (no `If-Match`) — such rows are counted in the push result (`blindOverwrite`) so the loss of concurrency protection is surfaced in the result line (ETags are only known at push time, so the surface is post-push, not the confirmation dialog; TODO 3.8b). `[test-enforced: tests/pushReconcile.test.ts (the dialog's full picture is pure `buildPushBreakdown` — bucket counts + the pushable track list through the ONE classifier; a breakdown that disagreed with the run would be the 3.8c drift bug rendered prettier), tests/navidromeApi.test.ts (paginateSearch3 `isCancelled` hook: stops before the next page, returns the partial set, checked before page 0), tests/webdavAtomicWrite.test.ts (the atomic write is a pure injectable-fetch core, `webdavAtomicWrite.ts` — exactly-once temp cleanup on EVERY failure path: 412/other-status/network, no DELETE on success or PUT failure, If-Match forwarded, 412 → `ConflictError`; syncEngine's `webdavPutAtomicViaAppFetch` is a thin adapter binding `webdavFetch`, and the module MIRRORS webdavUtils' `webdavTempPath`/`authHeaders`/`buildWebdavUrl` to stay Capacitor-free — changing those utils requires updating the mirror, pinned by the mirror-check test); real-bundle e2e: tests/e2e/pushchanges.spec.ts (4 specs: happy path, 412 retry, bucket-breakdown dialog, and mid-run cancel — `runManualWebDAVSync` takes `onProgress(done,total,title)` + `isCancelled`, cancels BETWEEN rows only (an in-flight row always completes atomically; pushed rows stay pushed, the rest stay pending), the `cancelled` flag rides the result; the live progress bar + Cancel button live in the Push SECTION, not the modal — the modal closes when the run starts; `pushState.update` MUST spread the prior state or the next onProgress wipes `cancelRequested` and the run never stops; the mock's `setGetDelay(ms)` keeps a row in flight so the spec can cancel mid-run) — a UI rating edit pushes through GET(etag) → PUT .mmdrome-tmp → MOVE If-Match exactly once, no temp orphan, POPM frame lands, and a second spec arms the mock's conflict simulation (concurrent-writer bytes+etag mutate before the first MOVE) to prove the single 412 retry in the browser: re-GET → re-modify → re-PUT → MOVE with the FRESH etag, exactly ONE temp DELETE — on the route-mocked write-path WebDAV (tests/e2e/webdavMock.ts; its CORS exposes ETag, which is NOT safelisted — without Access-Control-Expose-Headers the browser hides it and Push silently degrades to a blind overwrite, making every If-Match assertion vacuous)]`
- **D6 — Single commit path**: `commitFeedback` (feedbackService) is the ONLY UI commit path for rating/loved. It (and `seedNavidromeFeedback`) rebuilds the FULL `LocalMetadataStore` and `put`s it (replace, not merge) — both MUST preserve `matchSource`/`ignored`, or a rating edit / server seed on a dismissed or manually-bound row silently drops the marker and the next scan re-matches/re-pushes it (D8). `[test-enforced: tests/metadataWriters.test.ts]` `ratingSource: 'webdav'` → local cache `pending_sync` (Push writes tags); `'navidrome'` → direct server writes (`setRating` 1–5, star/unstar), never `pending_sync` — UNLESS `writeTagsInNavidromeMode` (default off) is ON, which marks the row `pending_sync` so Push also writes the tag to the WebDAV file (MusicBee sees phone edits). Do NOT confuse `writeTagsInNavidromeMode` with `syncToNavidrome` — the opposite-direction mirror (webdav → server), forced ON when flipping to navidrome. `seedNavidromeFeedback`: in webdav mode, server values only seed when there's no existing entry or the row is unbound/zero — coarse server stars must NEVER clobber fine-grained tag ratings; navidrome mode: server always wins. Push of a cleared rating: push `setRating(songId, 0)` only when `rating == 0 && prevRating > 0` (an actual clear, not a heart-only).
- **D7 — Layer rule**: `navidromeApi.ts` is a stateless Subsonic HTTP layer (+ config cache `setCachedConfig`/`getCachedConfig`); `syncEngine.ts` owns ALL policy (credential resolution, baseKey-scoped cache, cache fallback, orchestration). API functions always take an explicit `NavidromeConfig`; ONLY orchestration functions pull config. Do not resurrect wrapper fossils (`connectNavidrome`, `getNavidromeSongStreamUrl` in the api layer). Song pagination is the pure `paginateSearch3(fetchPage, { pageSize, maxPages })` driver (cap + short-tail + repeated-first-id dedupe — an offset-ignoring server can't hang the loop); `loadNavidromeSongs` only supplies the `callSubsonic` closure. The in-memory config cache is keyed by server identity (trimmed baseUrl + username): `commitCredentials` and `connectNavidrome` clear it via the pure `cachedConfigMatches` whenever the committed url/user no longer match — a disconnect or server swap must not keep serving stale stream/cover URLs until restart (password changes alone don't invalidate). `[test-enforced: tests/navidromeApi.test.ts]`
- **D8 — Auto-bind requires evidence**: gate `score >= 40 && (nameScore > 40 || tagScore > 0)` — size-only (40) never auto-binds. A unique exact-title tag match may auto-bind without artist certainty; shared exact-title candidates require artist disambiguation, and a duration conflict beyond ±2 seconds demotes certainty. Probed tags that contradict the track suppress filename and size evidence too; no identity tags are not a contradiction. Manual bindings (`matchSource: 'manual'`) are never re-matched by scans (issue-1 guard; the bound path is re-READ on scans so tag edits propagate). The claim guard (`claimedInScan`) must claim synchronously (check+add with no await between — it's the atomicity across concurrent `processItem`s). Re-verify flows: 'conflict' REFUSES the re-stamp (would canonize a wrong binding); 'unknown' allows it (the user's click IS the verdict). Rule of thumb: automatic paths never write unverified bindings; user-initiated paths may. The ONE automatic exception is the force-scan heal release (D16) — an AUTO binding whose bound file's tags PROVE it wrong (the file is another track, or its title is unrelated) is unlinked so the same scan can re-link it; manual/ignored/pending rows are never auto-cleared anywhere. All bound paths are excluded from candidate suggestions. The manual-bind re-READ (the `processItem` manual path) re-checks `pending_sync` AND `ignored` after its fetch and carries `ignored` through the `updateMetadata` full-row replace — a dismissal made mid-fetch must never be dropped (TODO 3.6b; same guard the auto-match branch uses). `[test-enforced: tests/scannerLifecycle.test.ts]` The evidence-gate half (size-only-never-binds, `certain` verdicts, ambiguity ties, tag-led-uncertain, `verifyEntryAgainstTrack`) IS pinned — `[test-enforced: tests/metadataCore.test.ts]`
- **D9 — Scan freshness**: `scanAll('modified'|'force')` is the only scan entry. ALWAYS a fresh PROPFIND probe FIRST (`refreshIndex`) — never scan a stale snapshot (a stale index can't detect remote tag edits; error + abort on probe failure). Exactly ONE PROPFIND per connect. Unmatched-retry gated by EITHER `computeIndexFingerprint` (FNV-1a over sorted `path|size` pairs) OR `computeTagCacheFingerprint` (FNV-1a over sorted `path|size|mtime|status|probedAt` pairs; new probe evidence) OR unprobed audio files remaining (`hasUnprobedAudioFiles`, same freshness predicate as the probe pool — covers the probe-failed/offline case where both fingerprints stay stable; 2026-08-21). The hint-gated probe ROTATES: hinted files are uncapped, then a bounded unhinted window (`max(unclaimedTrackCount*2, 100)`) is appended — probed files leave the pool next run, so repeated scans make monotonic progress toward full tag coverage instead of permanently abandoning unhinted files (the old zero-hints-only fallback stranded them). Index entries carry an in-memory `probeStatus` annotation (stamped wherever fresh cache entries are applied; dropped by `slimIndexForPersistence`) so `deriveNoMatchReason` distinguishes `not-probed` / `read-failed` / `no-identity-tags` honestly — a probed-but-empty candidate must NEVER surface as "run Scan to read it" (2026-08-21). `force` (`Rescan All`) mirrors `modified` on PROPFIND failure: on `!rebuilt` it sets `metadataScanState.error = INDEX_REFRESH_FAILED` before returning, so the UI never sticks at `Scanning 0/0` with disabled buttons (2026-08-22). **Credential swap MUST bump `scanGen` AND set `cancelled = true`** (0.4) — otherwise in-flight items match against the cleared empty index and the vanished-clear branch mass-wipes every binding. Index key captured BEFORE the probe await. In `ratingSource === 'navidrome'` mode, `processItem` preserves the row's rating/loved/comments (Navidrome is authoritative; only `fileType`/`webdavPath`/`webdavLastModified`/`webdavBase` refresh). The persisted index snapshot is slimmed via `slimIndexForPersistence` (content-probe tags dropped — they live in the `webdavFileTags` cache; `fingerprint` hashes `path|size` and `tagFingerprint` hashes probe-result identity/mtime/status/time), and a malformed PROPFIND href falls back to the raw value instead of aborting the whole build (`stripBasePath`). The debug track view matches against the LIVE in-memory index (`getCurrentIndex()`), never the slimmed snapshot — tag-only matches would otherwise lose their evidence. The inline scan probe runs before the drain, applies reverse binds before queueing, and consumes its full-metadata cache entry (including fresh failure entries for filename matches, so no immediate duplicate GET); cache freshness follows size + WebDAV mtime, complete fresh indexes prune vanished cache paths while partial crawls retain them; partial indexes allow auto-binds (driven by probe tag verification) but block vanished-path clearing (a bound path in an unreadable directory must not be deleted); an empty complete index still drains existing bindings so deletions are reported/cleared, probe failures use separate network/parse TTLs, the online-gated restore trigger waits behind scheduled scans/tails, all callers share one session-aware probe promise, fresh index requests share one PROPFIND promise, post-probe tag fingerprints are persisted, and credential swaps remove old-base cache rows. The mtime diff (`mtimeChanged`) compares EPOCHS when both sides parse — RFC1123 vs ISO for the same instant must NOT mass-flag unchanged files after a server switch (TODO 3.7b); absent/unparseable sides fall back to the raw-string diff, so a server omitting `getlastmodified` still re-reads (the safe direction). **Stamp-fallback tradeoff (review 2026-08-15)**: the scan's manual-bind re-read and the re-verify flows stamp `entry.lastModified ?? existing.webdavLastModified` — on a `getlastmodified`-less server the `??` preserves the old stamp, so those rows keep diffing as changed and re-read EVERY scan (fresh at a per-row GET cost), while auto-bound rows stamp `undefined` and stabilize (cheap, but remote tag edits stop propagating). The `??` is load-bearing for manual-row freshness — do NOT simplify it away without revisiting the tradeoff. Webdav-mode comment imports preserve a cached comment when the file LACKS the tag (`mergeFileComments` — the file is authoritative otherwise; TODO 3.7a; all three import sites: the manual-bind re-read, the auto-match path, and `writeReverified`). `[test-enforced: tests/indexHygiene.test.ts, tests/metadataCore.test.ts]`
- **D10 — Choke points**: the tag probe is also a single read/cache boundary: a fresh entry carries complete `FileMetadata` (identity + rating/loved/comments/year/duration), successful/empty entries are fresh while size + WebDAV mtime remain unchanged, network failures retry quickly, and parse failures retry on a longer TTL. `buildWebdavFileIndexDetailed` reports partial crawl results; cache pruning is allowed only for complete indexes, and the scanner refuses vanished-path clearing on a partial index (auto-binding proceeds with tag verification). The shared probe/index promises and monotonic completion revision keep readers and File Matching synchronized. `webdavFetch` (`webdavUtils.ts`) is the single WebDAV fetch path (native → CapacitorHttp, §3.3). Star/unstar batching ≤ 30 ids per request (GET URL length, HTTP 414). PROPFIND `Depth: infinity` falls back to `crawlIndex()` (BFS `Depth: 1`, concurrency 6). `deriveFileType` keeps the actual suffix as-is (`.wav` must stay `wav`); only missing suffixes parse the content-type through `CT_SUBTYPE_ALIASES`; unknown subtypes fall back to `mp3`. The atomic-write temp naming is single-sourced here too — `MMDROME_TMP_SUFFIX`/`webdavTempPath`/`isTempFile` (TODO 3.8a): the writer (`webdavPutAtomic`) and the orphan cleaner (`metadataScanner.cleanupOrphanTempFiles`, deletes temp-named entries after every fresh probe) share it by construction, so a suffix change can never strand old orphans. Tag reads: chunked `Range` fetches (opus/ogg/m4a/wav 512 k initial, mp3/flac 262 k — byte-optimal vs 1 M; tail-retry head+tail 640 k for WAV INFO; full-file for opus/ogg ≤ 8 MiB); truncated durations (`!gotFullFile`) are dropped to `undefined` (no-signal) for every format that can lie on a partial buffer (opus/ogg/mp3/m4a/aac/wma) so scoring never conflicts on a 10 s vs 403 s estimate; empty files can be force-re-probed via `ensureTagProbe(forcePaths)` (Refresh collects up to 20 visible empties, per-row Retry one) bypassing the fresh-cache TTL. `[test-enforced: tests/webdavTempFile.test.ts]`
- **D11 — taglib-wasm disposal**: `try { reads } finally { dispose }` in BOTH `extractMetadataFromBuffer` and `extractRawTagProperties` (dispose in its own try/catch); post-open errors RETHROW — never return `rating: 0` silently (it clobbers real ratings and defeats the chunk-doubling retry). `modifyMetadataBuffer` is already safe (taglib's own finally).
- **D12 — fileType must not outlive the Track**: a cached row's `fileType` never survives the library Track — always write `track.fileType` (library-stamped); Push resolves the write-branch type from the library (`fileTypeOf`), never the stale row.
- **D13 — Normalization is CJK-safe**: `normalizeForMatch` = `[^\p{L}\p{N}\s]/gu` (letters/numbers of any script survive — `\w` strips Japanese and made every CJK track a `.includes("")` near-match). `normalizeForHint` is an ALIAS — the two must not drift. Punctuation-only titles (`/////////////////////////`, `━━━━` etc) normalize to `""`; `effectiveTitle(s)` keeps raw punctuation (lowercased, whitespace-collapsed) when the fold is empty so `///` retains identity, and every `includes`/equality guard requires non-empty on both sides — otherwise `cleanedFilename.includes("")` is vacuously true for every file (score 74, false bind). Title-scoring changes only affect FUTURE scoring: healing unmatched rows from a scoring-formula change still needs a force rescan or the File Matching picker (tag-evidence gaps, by contrast, now heal on ordinary scans via the D9 rotation). `[test-enforced: tests/matchNormalize.test.ts, tests/metadataCore.test.ts]`
- **D14 — API normalization**: Subsonic `userRating` is 1–5 → local ×20; `starred` arrives as ISO timestamp (or boolean on some servers) — always use `isStarred()`. Navidrome search results have no rating field (no cross-source guard needed). `connectNavidrome`'s `if (!forceRefresh && lastScan)` cache guard is INTENTIONAL correctness-first — do NOT "fix" it to cache when `getScanStatus` fails. Cache is validated on `baseKey` alone (offline-start fallback; stale-cache risk accepted — "Connect & Load" forces refresh). The `loadLibraryFromNavidrome` orchestration — bail rule (apply iff songs present OR connected-and-clean), cached-connect seed skip, and the `navigator.onLine`-gated auto-scan — is the pure `planNavidromeLoad` (`navidromeLoadPlan.ts`); the async glue is a thin interpreter of its plan. **Cancellation (2026-09-06)**: `loadLibraryFromNavidrome` registers an independent cancel token (`activeCancel` in syncEngine; `cancelLongOperation()` targets the newest) and threads `isCancelled` into `paginateSearch3`'s hook (same name, same polarity — the hook is checked BEFORE each page and a partial page-set is returned). The cancel return is checked FIRST in the pipeline, BEFORE the plan's bail rule: a partial page-set can have `songs.length > 0` and would pass the bail rule — a cancelled load must apply NOTHING (no `setLibrary`, no seed, no cache write) and report `loadResult.error = 'Load cancelled'`. Tokens are per-run, NOT generation-stale-checked: a background online-restore load legitimately races the user's Connect & Load click, and invalidating older tokens on new runs poisoned the user's run with a silent empty library. The cache-vs-refresh decisions above (baseKey validation, non-empty, fresh-path forceRefresh/scan-freshness, offline fallback NOT forceRefresh-gated) are pinned by `tests/syncCachePolicy.test.ts` (pure `cachedLibraryUsable`, TODO 3.11); the bail/seed/scan matrix is pinned by `tests/navidromeLoadPlan.test.ts` (TODO 3.13), and the cached-reconnect wiring is exercised end-to-end by `tests/e2e/sync.spec.ts` (route-mocked Subsonic). A **cached connect never re-seeds Navidrome feedback** (its server values are stale; in `ratingSource:'navidrome'` mode the server always wins over local `synced` edits that commit straight to the server and are therefore unprotected by the `pending_sync` skip) — pure `shouldSeedFeedback` gates `seedNavidromeFeedback` in `loadLibraryFromNavidrome`, pinned by `tests/syncCachePolicy.test.ts` (TODO 3.2).
- **D15 — Metadata recovery is a dedicated wipe-then-rescan, NOT `'force'`**: the "Reset metadata & re-link all files" action (`wipeMetadataForRelink` + `resetMetadataAndRelink`; canonical single home = the danger-zone section at the BOTTOM of Settings → Library — escalation Scan → File Matching (audit) → Reset LAST; the Sources tab keeps only a muted pointer) is the ONLY destructive mass-clear and the fallback for the mass-wrong binds the heal pass (D16) cannot prove: a file bound to one track is excluded from every other track's candidate set, and when the file's tags are missing, identical, or otherwise give no testimony, two swapped-but-existing bindings are stable under ANY re-scoring (only clearing every binding at once makes each file claimable). Wipe scope = `localMetadata` (bindings + cached rating/loved/comments + ignored/manual markers + pending edits) + `webdavFileIndex` + the WHOLE `webdavFileTags` table (all bases) + the in-memory mirrors; PRESERVED = credentials/settings (`userSettings` + persisted stores), `songLibraryCache` (the catalog must stay so the re-link scan has a library), play queue, scrobble history. It cancels in-flight scan/probe (gen bump) and awaits the index/tag persistence queues BEFORE clearing so a stale queued write cannot resurrect rows, then runs `scanAll('force')` and re-seeds Navidrome feedback (server-authoritative rows must not end up holding file-tag ratings). Do NOT fold this into `scanAll('force')` (routine, D4-protected, reflex-clicked from two UI spots) and do NOT extend it to the catalog/queue (separate Connect & Load / Clear Queue concerns). UI gates it behind a confirmation dialog that counts the pending edits it would destroy. Between-steps cancellation (2026-09-06, same contract as Push/load): the reset takes an {isCancelled} closure checked pre-wipe/post-wipe/post-scan, the view's Cancel ALSO calls cancelScan() so the re-link scan stops via the generation bump, and scanAll now resolves {cancelled} so a scan stopped by ANY cancel source (reset Cancel, the scan-stop buttons, a credential swap) can never surface as a successful reset — the cancelled reset still runs the navidrome seed (initMetadataForTracks zeroed the rows; an unseeded row's 0s would be preserved forever by the next scan's server-authoritative branch) and lands resumably ("run Rescan All Metadata to finish"). The ERROR check precedes the CANCELLED check (a failed scan also resolves cancelled:true). The reset's re-link work happens in the scan's INLINE PROBE phase (its progress rides tagProbeState), so at cancel time the drain total may honestly be 0 while tagProbeState.done carries the real work count. The cancelled landing also stamps `progress.cancelledShape` ('modified'|'force') — the scan result blocks surface a one-click **Resume scan** that re-runs the SAME shape (a force scan must resume as force; its heal pass only runs there); any next scan-state write clears the marker, and the button carries NO remaining-count (the probe-phase totals are honestly 0, a count would mislead). Empty-library refusal happens BEFORE the wipe (nothing lost); a re-link scan failure after the wipe throws an honest "cleared, re-link failed" error instead of a fake success; the navidrome-mode re-seed keeps server ratings authoritative over file tags (pin: file 60/90 never clobbers server 100/40). `[test-enforced: tests/scannerLifecycle.test.ts — wipe clears bindings/index/tag cache at the DB layer, preserves queue/catalog/settings; reset heals swapped auto bindings; empty-library refusal; post-wipe scan-failure throw; navidrome-mode seed; pre-wipe cancel is non-destructive; mid-scan cancel keeps processed links + follow-up scan finishes; cancelledShape stamped on cancel landings and cleared by the next scan]`
- **D16 — Force scans heal provably-wrong AUTO links (2026-09-05)**: the "Rescan All Metadata" heal pass (`computeHealReadPaths`/`releaseHealEvictions` in the scanner over the pure `selectHealEvictions` in `metadataCore.ts`) makes a plain force rescan the FIRST-line recovery for bad links — no wipe needed when the bound file can testify. Before the drain it force-reads every AUTO-bound file lacking fresh tag evidence (one-time probe cost; afterwards the cache is fresh) and RELEASES (unlinks only — path/base/matchSource cleared, rating/loved/comments and `synced` preserved) any binding whose file's own identity tags prove it wrong: the file's exact title belongs to a DIFFERENT library track (swap containment — the file is that other song, uniqueness NOT required), OR the titles differ and are not a same-release family (neither starts with the other at a word boundary: "Song (Live)" under "Song" is a family and stays when no track "Song (Live)" exists; "Rainbow" under "Rain" is not). Exact normalized titles, tag-less/empty reads, and manual/ignored/pending rows never release — a "(Live)"/feat/alternate-title variant that merely differs is kept (D8's hint-not-proof trap), and user-audited rows are sacred. The freed file becomes claimable, so the same scan's drain re-links everyone the evidence supports; rows nothing re-matches surface in File Matching with their values intact. The tags-are-the-catalog premise (file tags and Navidrome parse the same file) also raised the auto-bind floor: an exact-title tag match with no duration conflict scores 111, above the strongest filename evidence (exact name + equal size = 110), so re-links pick a correctly-tagged file over an untagged same-name same-size twin. Heal is FORCE-ONLY — `'modified'` scans never release (they only re-queue changed/unmatched rows anyway). A heal that fires is USER-VISIBLE: `progress.released` (scan-complete line: "N wrong link(s) healed") plus the running annotation "Released N wrong auto links — re-matching…" while the drain re-links — never a silent console-only pass. `[test-enforced: tests/metadataCore.test.ts — selectHealEvictions matrix + floor, tests/scannerLifecycle.test.ts — one-scan swap heal (cold cache), second-scan stability, manual/pending protection, modified no-op; swap-heal scan reports released 2, a healthy rescan reports none]`
- **D17 — File Matching is also a link auditor (2026-09-05)**: every MATCHED row in the list carries a verdict from the bound file's OWN fresh cached tags (`auditBoundFile` in `metadataCore.ts` over `verifyEntryAgainstTrack` — the single comparison source) — verified / tags-conflict (the file title is surfaced) / not-verified with `readState` saying WHY (never-probed, empty, unreadable, network-error — the same honesty as the D9 no-match reasons; never "run Scan" when the read itself failed). The list makes ZERO network reads (verdicts are cache-derived); the UI's "Re-read file tags" action forces a probe of exactly that path via the single read boundary. The view never AUTO-clears on conflict (that is the force-scan heal's job for AUTO rows, D16; manual rows are the user's verdict) — it sorts audit-worthy matched rows (conflict/unknown) BEFORE the verified bulk so the display cap shows the wrong-link candidates, and shows manual picks as such. The UI's rescan-promise is HONEST by construction: a matched AUTO row whose conflict the heal would provably release is stamped `fixable` via the SAME pure predicate as the scan (`bindingReleaseable` + shared `buildEffectiveTitleCounts` in `metadataCore.ts` — selectHealEvictions refactored onto it; never export a drifting copy of the family/ownedElsewhere logic). Copy and buttons may promise auto-fix ONLY on `fixable` rows; family-titled conflicts ("Song (Live)" under "Song") and ALL manual rows never get the promise. The matched-links summary's "Fix N wrong links automatically" button is wired to the SAME force-scan handler as Rescan All Metadata (the heal releases exactly the `fixable` rows and re-matches in that drain) — do NOT build a bespoke per-row relinker outside a scan (single-row release leaves swapped pairs half-resolved and re-implements drain claims). Library-tab escalation order is Scan → File Matching (audit) → Push → Reset LAST — the destructive reset section is the bottom section; never re-insert it between the everyday scan and the auditor. Do NOT reintroduce a per-row fetch at list time or auto-clear from this view. Browser-level: the auditor + recovery loop also run against the REAL bundle with real taglib-wasm parsing hand-built MP3 fixtures served by a route-mocked WebDAV server (`tests/e2e/filematching.spec.ts` + `tests/e2e/fixture.ts` + `tests/e2e/webdavMock.ts`, parse-guarded in Node by `tests/fixtureParse.test.ts`) — but a user-made mis-link is ALWAYS manual (`bindTrackToFile` stamps `matchSource: manual`), so the D16 auto-RELEASE path is unreachable through the UI by design and stays Node-pinned; the e2e proves verified audit → manual-conflict protection → rescan recovery, and that a recovery rescan adds ZERO file GETs (fresh probe cache, D10). `[test-enforced: tests/metadataCore.test.ts — auditBoundFile + bindingReleaseable matrices, tests/scannerLifecycle.test.ts — matched rows classified from the tag cache with zero reads, conflict ranks before verified, fixable flagged exactly (swap pair both sides / family never / manual never); tests/e2e/filematching.spec.ts — real-bundle journey]`
  - **D17b — Buckets, Confirm, usable list (2026-09-09)**: rows carry a pure attention bucket + severity (`classifyFmRow`/`fmSeverity` in `metadataCore.ts`, stamped by `listUnresolvedMatches` with exact `bucketCounts`): `action` (dead links vanished/stale-base → errors conflict/read-failed → missing ambiguous/no-match → auto-unknown confirmations) → `confirmed` (ANY manual link) → `verified` (auto + tag-verified) → `ignored`; `pendingPush` boosts within equal severity. A manual pick IS the user's verdict and resolves into `confirmed` — the SAME-path Confirm writes a manual binding with the audit evidence untouched — but confirmation NEVER fabricates a tag `verified` (the row keeps its honest unknown/conflict line; D17 honesty). Do NOT re-sort the view by kind or re-derive buckets view-side (the stamp is the policy). The view is multi-select bucket chips (live counts, default action-only) + debounced token search over pre-folded track+path strings (searchCore tokens + matchNormalize fold, folded once per refresh) + a contained scroll list growing by 120-row windows (the `matchCap` number input is gone — never reintroduce a cap control); Clear returns a row to Needs action with suggestions (the undo path — no history stack by product decision). `[test-enforced: tests/metadataCore.test.ts — bucket/severity matrix, tests/scannerLifecycle.test.ts — action→confirm→confirmed→clear→action, tests/e2e/filematching.spec.ts — filter journey]`

### E. Native engine & bridge
- **E1 — No `AVAudioUnitGain`**: gain = `AVAudioMixerNode.outputVolume`; graph order §3.4.
- **E2 — `CAPPluginCall` accessors** require default args; `getArray("tracks", [])` + `as? JSObject` (§3.4).
- **E3 — MPRemoteCommand**: `.isEnabled`; the three `MPNowPlayingInfoProperty…` keys (§3.4).
- **E4 — Swift 6**: capture locals before closures (§3.4).
- **E5 — iOS build invariants**: `VITE_BASE=./`; `sync:ios` chain; `BackgroundAudioCore` = own SPM package; native verification = `ios.yml` only (§3.5).
- **E11 — iOS releases via SideStore (2026-09-09)**: the SideStore source (`sidestore/apps.json`, AltStore format, served via jsdelivr `@main`) points at the per-tag GitHub Release asset `mmdrome.ipa` (unsigned — SideStore signs with the user's Apple ID on install and auto-refreshes; never commit an IPA to the repo). Cutting a release is `npm run release:ios <x.y.z> [notes]` — the script is the ONLY writer of the four version sites (`package.json` → `__APP_VERSION__`, pbxproj `MARKETING_VERSION` every config + `CURRENT_PROJECT_VERSION` max+1, `apps.json` flat fields + `versions` history + `news` capped at 5); never bump one by hand without the others or SideStore update detection (source version vs installed `CFBundleShortVersionString`) breaks. `versions[]` entries MUST use the NEW key names (`version`, `buildVersion`, `date`, `localizedDescription`, `downloadURL`, `size`, `minOSVersion` optional) — NOT the legacy flat-app keys (`versionDate`/`versionDescription`); SideStore's decoder requires `date` and rejects the source otherwise (2026-09-09: the exact error names the missing path, e.g. `apps > 0 > versions > 0 > date), and likewise hard-fails on a missing `size` (2026-09-10: `apps > 0 > versions > 0 > size` — a sizeless 1.1.1 entry failed source refresh). `size` is unknowable pre-build, so releases are two-phase: cut without it, then after the IPA publishes re-run the script for the SAME version with `--size <asset bytes>` (idempotent — build/date preserved, only the size key is added; pass flags after `--`, bare `--size` is swallowed by npm and pollutes the notes) → commit + push + purge the CDN. The flat legacy keys stay on the app object for old clients. Then commit + `git tag ios-v<x.y.z>` + push: `ios.yml` (branch + tag trigger) builds and its `release-ios` job (`contents: write`, `gh release create`) publishes the IPA. The release job MUST check out first — `gh release create` shells out to git and dies instantly (`not a git repository`) without it (2026-09-09 post-mortem; token perms were never the cause). Retry path: fix, push, delete + re-push the tag (main pushes never publish). Push the tag in its OWN push — never `git push origin main ios-vX` together: both refs fire `ios.yml` into the single `ios-build` concurrency group and `cancel-in-progress` kills the tag run before `release-ios` runs (2026-09-10: ios-v1.1.1 died at 2 s, no release published; delete + solo re-push fixed it). After the release publishes, purge the jsDelivr CDN cache for the source file (`https://purge.jsdelivr.net/gh/sieradni/mmdrome@main/sidestore/apps.json`) — branch responses are edge-cached and SideStore keeps seeing the previous version until purged (2026-09-10: source stuck on 1.1.0 post-publish; purge fixed it instantly). Web `npm run deploy` stays manual and independent — the source URL tracks git, not gh-pages. `[test-enforced: none — pipeline only, E5]`
- **E6 — Webview constraints**: WebDAV via CapacitorHttp (§3.3); `viewport-fit=cover` + safe-area helpers; 3-layer zoom lock (§3.2).
- **E7 — Bridge reconciliation**: reconcile by `trackId` (never index); `refreshQueue` re-arms the native tail without disturbing the active track; native speed/pitch ECHO into the stores (`engineFacade.ts:51/61`) — `_initNative` MUST set `playbackSpeed`/`pitchOctaves` stores BEFORE subscribing (defaults clobber restored values). Native-tail resyncs route through `NativeTransport.scheduleSync` (microtask-coalesced, lazy snapshot at fire time, serialized against engage), driven by the `queue` store subscription — do NOT add explicit `refreshQueue` calls back to the manager.
- **E8 — Sleep timer**: JS owns the intent, native the timer (MainLoop). `_nativeLoadPlay` calls `transport.engage()` (the ONLY setQueue+playTrackAt path) and `sleepTimerManager.rearmAfterSnapshot()` after a successful engage — native re-arms from JS state; `refreshQueue` needs no re-arm. `[rearmDecision test-enforced: tests/sleepTimerMirror.test.ts]`
- **E9 — NativeTransport owns the native machine**: retry (timers, track-keyed validity, give-up → `onTrackEnded({kind:'natural', fromError:true})`), seek-retry memory (1.7), the 250 ms position poll (enabled ONLY while engaged), engage serialization (pending-latest), the tail-sync serialization (a scheduled `refreshQueue` AWAITS any in-flight engage so it can never interleave `setQueue`/`playTrackAt`) and the fail-fast empty-url guard all live in `NativeTransport` (A6 boundary — it never touches queue/stores/sleepTimer). The manager wires five policy callbacks in `_initNative` (`onTrackChanged`/`onTrackEnded`/`onRetry`/`onPlaybackState`/`onTick`), routes native `ended` through `_onNativeTrackEnded` → `decideAdvance` (A4; `parkArmed:false` — the engine parks at end-of-track, E8), and treats `engage() === false` as stopped (parity with web `playLoaded` not-started). `_onNativeRetry` re-loads only when `currentTrack` still matches (A5); `_nativeLoadPlay` reflects an engage's outcome ONLY when `currentTrack` still matches its track (a superseded engage never promotes/rearms/stops). `[test-enforced: tests/nativeTransport.test.ts, tests/playbackManagerNative.test.ts]`
- **E10 — Webview-reload reconcile (1.5)**: `_reconcileNativeReload` (called from `_initNative` after `transport.init()`) maps `NativeTransport.getState()` through the pure `reconcileReload` (trackId + combined `indexOf` + library `findTrack`, NEVER `state.index` — E7): `resync` → `transport.adopt(trackId)` (marks engaged + `_lastTrackId` + starts the 250 ms poll WITHOUT `setQueue`/`playTrackAt` — re-sending the snapshot would kill the engine's live playback) then re-anchors via `_onNativeTrackChanged` + `currentTime`/`playbackState`; `stop` (unknown trackId) → warn + `pause()` (the engine is audibly playing and the bridge has no `stop`; `_stopPlayback` only disengages JS) + `_stopPlayback()`; `idle` → no-op; a `getState` rejection skips gracefully. `[test-enforced: tests/playbackManagerNative.test.ts, tests/nativeReconcile.test.ts]`

### F. Process
- **F1 — Verification gates**: `npm run check` AND `npm test` (add `npm run build` after any src change). Swift items verify via `ios.yml` only (no local toolchain on Windows). The production-bundle smoke test (`npm run test:e2e`, Playwright) builds `dist` + serves it via `vite preview` at `/mmdrome/` and asserts the app boots with no console errors — it runs as the `smoke` job in `test.yml` (only gate that exercises the bundled output; F2b).
- **F2 — Design rule**: pure, injectable modules + thin adapters (§1). Invariants get TESTS, not prose (test-enforced tags in this file track that).
- **F2b — No module-eval cycles through eager singleton reads**: a module-level `const x = new X()` (or any constructor that runs at eval time) must NOT read a binding from a module that imports it back. Native ESM throws a TDZ `ReferenceError` on that read; the production bundle hoists the cyclic `const` to `undefined`, so the field silently becomes `undefined` at runtime. The concrete case (fixed 2026-08-14): `playbackManager`'s singleton constructor eagerly captured `sleepTimerManager` into `this._stm` (introduced by the `d6fae25` DI seam) while `sleepTimer.ts` imported `playbackManager` back for `clearLocal`'s pause; `App.svelte` imports `sleepTimerManager` BEFORE `playbackManager`, so `_stm` resolved `undefined` and `init()`/`play()` threw. Fix = invert the dependency (`sleepTimer` exposes `setExpireHandler(() => pause())`, registered in `playbackManager.init()`), AND make the manager's engine deps lazy getters (`_am`/`_qm`/`_stm` read their module singletons on first use, not in the constructor) — construction now reads no module binding at all. Break cycles with callbacks/registration + lazy reads, not eager ones. `[test-enforced: tests/moduleEvalOrder.test.ts — imports sleepTimer FIRST, asserts playbackManager._stm === sleepTimerManager]` Build-enforced too: `vite.config.ts` enables Rolldown's `circularDependency` check (OFF by default — the cycle shipped silently) and escalates any cycle touching `src/` to a hard `npm run build` failure (node_modules-only cycles, e.g. Svelte's own, are allowed).
- **F3 — Test harness**: `npm test` = `node --test --import ./scripts/test-loader.mjs` — default discovery only; passing a directory FAILS (`ERR_UNSUPPORTED_DIR_IMPORT`). The loader self-registers via `module.register` and maps the `$lib` alias to `src/lib/`. Tests that import the playback graph (playbackManager/audioManager → @soundtouchjs) must `import './stub-audio-worklet-node'` FIRST (`SoundTouchNode extends AudioWorkletNode` at class-definition time; the stub defines the global before the graph loads). Node has no IndexedDB: paths that persist through Dexie (e.g. `setActiveQueueIndex` → `saveQueue`) must stub the table write — patch the real db's table directly: `Object.getPrototypeOf(db.playQueue).put = ...`. **ALL tables on a Dexie instance share ONE prototype**, so `Table.prototype.get`/`put` are the same function across tables: patching `db.playQueue.get` CLOBBERS the `db.userSettings.get` patch (2026-08-15 lesson). Patch each method once and dispatch on the table (`this.name`) when different tables need different behavior. The `PlaybackManager` constructor takes injectable deps (defaults = module singletons) so manager glue tests never construct a DOM (`tests/playbackManagerBg.test.ts`). Test-only TS constraints (tsconfig.test.json): `erasableSyntaxOnly` (no constructor parameter properties), `allowImportingTsExtensions`, `paths` without `baseUrl`; discriminated-union array literals need explicit `as` annotations. `tests/e2e/**/*.spec.ts` are Playwright specs (run via `npm run test:e2e`, NOT `node --test`); they're typechecked by tsconfig.test.json but named `*.spec.ts` so Node's test discovery (which matches `*.test.ts`, never `*.spec.*`) skips them.
- **F4 — Commit style**: small, logical, verified steps (check + test before committing); verify each feature/bugfix fully functional before the next task.

---

## 5. Development Workflow

- **Commit granularity**: small, logical steps; verify each feature/bugfix fully functional before moving on (F4).
- **Commit hygiene**: commit messages MUST NOT contain a Codebuff footer (`Co-Authored-By: Codebuff <...>` / `🤖 Generated with Codebuff`). A `commit-msg` hook (`.githooks/commit-msg`, enabled via `git config core.hooksPath .githooks`) strips any Codebuff line as a backstop; on a fresh clone run `git config core.hooksPath .githooks` to enable it (2026-08-14 user request).
- **Shell text-file writes (encoding)**: never use PS 5.1 `Add-Content`/`Set-Content`/`Out-File`/`Get-Content` without `-Encoding UTF8` — their default is ANSI/Windows-1252 and mangles UTF-8 (2026-08-13: a DEVLOG append silently corrupted `—`/`→`/`×` into the replacement char U+FFFD/`?`). Prefer the Edit/Write tools (native UTF-8); for appends/transforms use `[System.IO.File]::AppendAllText($p, $t, [System.Text.UTF8Encoding]::new($false))` (`-Encoding UTF8` writes a BOM on create in PS 5.1). Canary: `git diff` + scan for U+FFFD after any shell-written text. **Whole-file diffs (every line changed) = line-ending conversion** (LF→CRLF): restore the LF blob with `git checkout -- <file>` and re-apply the append via raw byte concat (HEAD blob + UTF-8-no-BOM bytes → `WriteAllBytes`); verify CR count is 0 and the diff is back to insert-only.
- **Verification**: `npm run check` (svelte-check + tsc, incl. tsconfig.test.json) AND `npm test` (node --test, zero-dep suites in `tests/`). `npm run build` after src changes. Native: `.github/workflows/ios.yml` (xcodebuild + swift test — the ONLY native compile gate); JS CI: `.github/workflows/test.yml`. Check CI results from the dev box with the §3.5 API recipe.
- **Technical debt plan**: `TODO.md` at repo root is the phase-ordered refactor plan (Phase 0 foundations CLOSED 2026-08-12; 0.5 test foundation; Phase 1 centerpiece **1.0 PlaybackCore** — policy Step 1 + WebTransport Step 2 LANDED 2026-08-13, bg transport Step 3 LANDED 2026-08-13 (webBgTransport + rewired manager, 1.11 closed), native transport pending; Phase 2 queue — closed (2.4 option b: active-row removal keeps the highlight on the next playable row; 2.5: user queue left unbounded); Phases 3–5). Item gates: `npm run check` AND `npm test`; items flagged `[DECISION]` need a product decision before implementation.
- **Knowledge base maintenance**:
  - New learnings: append a dated entry to `docs/DEVLOG.md`.
  - Promote to §4 immediately when it's a "do not regress"-class invariant; tag `[test-enforced: …]` when a test pins it; `[not test-pinned]` otherwise (see §0 contract).
  - README.md is the user-facing doc — do not duplicate dev knowledge there; this file is the dev reference.

---

## 6. Historical Log

The full chronological development log (2026-07-25 → present) — technical discoveries, platform workarounds, bug post-mortems, and the *why* behind every §4 rule — lives in **`docs/DEVLOG.md`** (NOT auto-injected; read on demand).
