# Project Context, Architecture & Developer Knowledge Base

This file serves as the definitive reference guide for developers and AI agents working on **mmdrome**. It provides core architectural context, implementation patterns, known platform quirks, and a persistent log of learned information to minimize analysis overhead.

---

## 1. Tech Stack & Framework Conventions
- **Framework**: Svelte 5 (utilizing runes: `$state`, `$derived`, `$effect`) + TypeScript + Tailwind CSS v4.
- **Build System**: Vite (Client-side SPA).
- **Design Philosophy**: Minimalist. Avoid heavy third-party UI/component libraries. Use native Svelte reactivity, Tailwind utilities, and simple CSS.
- **Local Storage / Database**: Dexie (IndexedDB wrapper) for user settings, custom EQ profiles, and track metadata cache (`src/lib/db.ts`).

---

## 2. Audio Engine Architecture (`src/lib/audioManager.ts`)
mmdrome features an advanced custom audio pipeline built on top of the Web Audio API and HTML5 Audio:
- **Dual-Element Architecture**: Uses two `HTMLAudioElement` instances (`a` and `b`) to enable gapless playback, preloading, and crossfading.
- **Audio Routing Chain**:
  `HTMLAudioElement (a or b)` â `MediaElementAudioSourceNode` â `GainNode (Track RG)` â `SoundTouchNode` (AudioWorklet for pitch/tempo shifting) â Parametric/Graphic EQ Biquad Filter Chain â Preamp `GainNode` â `AudioContext.destination`.
- **SoundTouch Integration**: `@soundtouchjs/audio-worklet` provides independent pitch and speed (tempo) control.
- **Crossfading**: Managed via dual gain nodes (`_gainA`, `_gainB`) with exponential volume ramping (`exponentialRampToValueAtTime`) over a configurable duration (up to 15s).
- **ReplayGain**: Supports track and album replay gain modes, dynamically adjusting gain nodes (`_rgGainA`, `_rgGainB`).

---

## 3. iOS Background Audio Mode (Critical Platform Quirks)
iOS aggressively suspends `AudioContext` and throttles background timers/events. mmdrome implements a specialized background handoff mechanism:
- **Background Element (`_bgEl`)**: A bare `HTMLAudioElement` kept warm specifically for background playback when the page hides (`visibilitychange` event).
- **Volume Handling**: iOS does not respect `HTMLAudioElement.volume` â the hardware volume rocker is the only volume control. `_bgEl.volume` is set to `0` (and never raised) because setting it is a no-op on iOS. Volume is controlled entirely by the master preamp (`_preamp` GainNode) while in foreground, and by the iOS system volume while in background.
- **Limitations & Workarounds in Background Mode**:
  - **Bypassed Effects**: SoundTouch and Web Audio Biquad filters (EQ) are bypassed in background mode because iOS suspends AudioContext and worklets. `_bgEl.playbackRate = speed` is applied directly to the HTML audio element.
  - **Media Session Routing**: Browser/lockscreen media session action handlers (`play`, `pause`, `seekto`, `nexttrack`, `previoustrack`) explicitly check `audioManager.isInBgMode` and route controls directly to `_bgEl`.
  - **Position Polling**: Because `timeupdate` events can stall on iOS background elements, position state and store updates (`currentTime`) are polled at a **250ms interval** while `isInBgMode` is true.
  - **Race Condition Protection**: `_exitBackground` and `_onBgTrackEnd` coordinate via a `_bgTrackEndHandled` flag to prevent double queue advancement or conflicting `src`/`currentTime` writes on the active element when a track ends in background concurrent with the user returning to foreground.

---

## 4. State Management & Core Services
- **Global App State (`src/stores/appState.ts`)**: Svelte stores managing current track, playback state, queue (`userQueue`, `autoQueue`), shuffle mode, settings, and library tracks.
- **Queue Manager (`src/lib/queueManager.ts`)**: Encapsulates all queue logic â advancement, auto-queue replenishment/filtering, auto-to-user promotion, history management. Separated from `playbackManager` to keep concern boundaries clean.
- **Playback Manager (`src/lib/playbackManager.ts`)**: Controls track transitions, playback (play/pause/next/prev/seek), crossfade handling, and syncs with `audioManager`. Does NOT own queue logic â delegates to `queueManager`.
- **Sync & Metadata Engines (`src/lib/syncEngine.ts`, `metadataScanner.ts`, `navidromeApi.ts`)**: Integrates with Navidrome (Subsonic API) for streaming/syncing library data and WebDAV for local/remote indexing.
- **Tagging (`src/lib/taglibSingleton.ts`, `tagWriter.ts`)**: Uses `taglib-wasm` for robust audio file metadata reading and writing.

---

## 5. Git Hygiene & Development Workflow
- **Commit Granularity**: Commit changes in small, logical steps. Verify each feature/bugfix is fully functional before moving to the next task.
- **Type Checking**: Run `npm run check` (`svelte-check` + `tsc`) to verify type safety across Svelte and TypeScript files before finalizing changes.
- **Technical Debt Plan**: `TODO.md` at the repo root is the phase-ordered refactor plan (produced by the 2026-08-10 audit). Phase 0-1 fix foundations/playback, Phase 2 consolidates ALL queue mutations into `queueManager` (item 2.2 CLOSED 2026-08-11 — `addToUserQueue`/`playNext`/`promoteToUser`/`promoteToUserNext`/`moveToNext`/`moveToEnd`/`removeFromAutoQueue`/`promoteActiveTrack`/`clearQueue` removed from `appState.ts`, the `QueueView.svelte` inline duplicates deleted, ALL routed through the `_mutateQueue` re-anchor helper into the pure `src/lib/queueMutation.ts` core — fuzz-verified duplicate-free builders (see the 2026-08-11 log entry below; `removeFromUserQueue` remains the deliberate exception, previously moved in the 2026-08-10 recency-window refactor, which also replaced `historyQueue` with the `recentTrackIds` window, item 2.3/2.6 closed), Phase 3 hardens sync/metadata, Phase 4-5 cover UI + docs. Item 2.4 (`removeFromUserQueue` active-row semantic decision) and 2.5 (userQueue memory policy) remain open. Items flagged `[DECISION]` need a product decision before implementation.

---

## 6. Learned Information & Operational Log
*(Add new technical discoveries, platform workarounds, or architectural decisions here as you encounter them during development).*
- *[2026-07-25]* Established comprehensive context guide to replace minimal AGENTS.md, eliminating redundant codebase analysis loops.
- *[2026-07-26]* Extracted queue management from `playbackManager.ts` into dedicated `src/lib/queueManager.ts`. Cleared dead `src/stores/queueManager.ts` and empty `src/lib/{api,audio,database,sync}/` directories. Fixed `DEFAULT_BAND_Q` inconsistency (both `audioManager.ts` and `builtInPresets.ts` now use `Math.SQRT1_2`). Moved `promoteActiveTrack()` to after successful `el.play()` to prevent polluting user queue on playback failure.
- *[2026-07-27]* Added build-time version injection via Vite `define` in `vite.config.ts`. `src/lib/version.ts` exports `appVersion`, `commitHash`, and `buildTime` from injected globals (`__APP_VERSION__`, `__COMMIT_HASH__`, `__BUILD_TIME__`). Displayed at the bottom of SettingsView. The commit hash comes from `git rev-parse --short HEAD`; falls back to `'unknown'` if git is unavailable.
- *[2026-07-27]* Eliminated all reliance on `HTMLAudioElement.duration` for track duration. It can report incorrect/wrong values (NaN, truncated, or shorter than actual), causing early track ending, broken seek clamping, and wrong slider max values. `track.duration` (metadata) is now the sole source of truth for duration everywhere. The `elementDuration` store and `effectiveDuration` fallback to it were removed. `onEnded` no longer has an early-detection workaround that compared `elemDur` to `metaDur` (which caused stuck queues when `elemDur` was wrong).
- *[2026-07-27]* Fixed scroll position reset when switching between song/album/artist tabs. Created `src/lib/viewState.ts` with `saveViewState`/`restoreViewState` functions. Each view component (`SongsView`, `AlbumsView`, `ArtistsView`) saves ALL local UI state (scroll position, filterOpen, sortOpen, min/max rating/year/length, sortBy, sortAsc, infinite-scroll limit, selected album/artist) via `onDestroy` and restores it via `onMount`. The `{#if}` rendering approach is retained (no CSS visibility toggling), avoiding scrolling regressions that occurred when wrapper `<div>` elements broke the `h-full` height inheritance chain. The `offsetHeight > 0` guard on SongsView's IntersectionObserver prevents phantom triggering when the container has zero dimensions.
- *[2026-07-28]* Added **auto-queue scoping** via `albumScope`/`artistScope` fields on `AutoQueueFilters`. Scope is set only when the user plays a track (or hits Play All) from an album/artist detail view via `handlePlayFromAlbum`/`handlePlayFromArtist` handlers, NOT on view entry/exit â scope persists across tab switches. Scope is cleared when toggling shuffle. Implemented in `_matchesAutoQueueFilters()` in `queueManager.ts`. Added **Play All** button to album/artist detail headers that clears the user queue and queues all tracks of the opened album/artist while setting the scope. Added **Loop-One** mode (`loopMode` store with values `'none' | 'one' | 'all'`). `_onTrackEnded`, `_onBgTrackEnd`, and `_handleCrossfadeEnd` in `playbackManager.ts` check `loopMode === 'one'` and restart the current track. Loop button added to now-playing overlay. Fixed `QueueView.svelte` â `autoQueueFilters.set()` changed to `.update()` to preserve scope. Added `onplay` callback prop to `TrackRow`. Removed `addToUserQueue` from `TrackRow.handlePlay` to prevent duplicate queue entries. Added `replenishAutoQueue()` in `_loadAndPlay` for immediate queue population.
- *[2026-07-28]* **Scope clearing on default TrackRow path**: When `TrackRow.handlePlay` is called without an `onplay` prop (e.g., from SongsView, search results, QueueView), auto-queue scope (`albumScope`/`artistScope`) is cleared before playing. This prevents stale scope from filtering the auto-queue when the user plays a track outside the scoped album/artist context.
- *[2026-08-04]* **Native iOS engine â JS bridge (Phase 3)**: Architected the JSâSwift bridge. `Capacitor.isNativePlatform()` gates every path â the PWA keeps the Web Audio engine untouched; iOS routes through the `BackgroundAudio` plugin client (`src/lib/nativePlugin.ts`, `nativeEngine` singleton; plugin `jsName: "BackgroundAudio"` registered via `registerPlugin<BackgroundAudioPlugin>`).
  - **Bridge protocol**: JS sends a full combined-queue snapshot (`setQueue`, then `playTrackAt(index, autoPlay)`), native owns the clock and emits `trackChanged` (payload `{ trackId }`, NOT index â queueManager's `promoteActiveTrack()` reindexes the combined queue whenever an auto track starts, so index mapping drifts; JS reconciles by **trackId**), `playbackStateChanged`, `ended`, `error`. `refreshQueue({tracks, activeIndex})` re-sends the full current combined queue without disturbing the actively playing track (native verifies `tracks[activeIndex].trackId == currentTrackId`, tears down any armed crossfade on the old tail, monitor re-arms). `setLoopMode` for mid-play loop changes. JS resends `refreshQueue` after every `trackChanged` reconcile so the native tail stays in lockstep with JS promotions/replenishment; native list only exhausts â `ended` â JS wraps (loop all) or continues via `combined[activeIndex+1]`, else stops.
  - **File facade (`src/lib/engineFacade.ts`, `engine` singleton)**: All views (`EQView`, `PitchSpeedView`, `VolumeView`) now talk to the facade, which mirrors speed/pitch/tape/volume/preamp/filters locally on native and pushes to the plugin, or delegates straight to `audioManager` on web (PWA behavior unchanged â `npm run check` clean after conversion). `EqPoint[][]`/curves ignored on native. Crossfade curve hard-coded to sigmoid k=8 (web only exposes duration).
  - **playbackManager refactor**: `init()` splits into `_initWeb()` (verbatim old body) / `_initNative()` (nativeEngine.init, settings restore, `pushNativeEqFromStore()`, loopMode/settings subscriptions, 250ms position polling â `currentTime`). Shared subscriptions (`playbackSpeed`/`pitchOctaves` â `engine.set*`, replenish/library/shuffle/filters) move to `_subscribeShared()`. **Gotcha**: on native `engine.setSpeed(savedSpeed)` does NOT echo back into the `playbackSpeed` store (no `onSpeedChange` hook), and `_subscribeShared` fires immediately â so `_initNative` MUST `playbackSpeed.set(savedSpeed ?? 1)`/`pitchOctaves.set(savedPitch ?? 0)` before subscribing or the defaults clobber restored values. `mediaSession.ts`, `setupPreloader`, `_attachPlaybackListeners` are web-only; App.svelte's `timeupdate` `$effect` early-returns on native. `_nativeLoadPlay` = setQueue+playTrackAt+promote+replenish; `_onNativeTrackChanged` re-adopts a playing track if it fell out of combined (re-insert into userQueue), pushes history for the previous track, promotes/replenishes, refreshes queue; `_onNativeError` mirrors web's exponential-retry then ends.
  - **Native EQ mapping**: `NativeFilterSnapshot` type strings map to `AVAudioUnitEQFilterType` in `AudioEngine.swift::mapFilterType` (`bandpassâ.bandPass`, `notchâ.bandStop`, default `.parametric`); Qâbandwidth via `1/max(q,0.05)` approximation.
  - **SPM path fix (`npm run sync:ios`)**: `cap sync ios` regenerates `ios/App/CapApp-SPM/Package.swift` and computes local plugin paths via Node `path.relative()`, which emits backslashes on Windows (`..\..\..\native\BackgroundAudio`) â SPM can't resolve those. `scripts/fix-spm-path.mjs` normalizes backslashes to forward slashes and is chained after `cap sync ios` in the `sync:ios` npm script (no-op on macOS CI, which already produces POSIX paths).
  - **Standby crossfade generation**: `AVAudioPlayerNode.stop()` fires the pending completion handler. `refreshQueue`/`setLoopMode` tear down armed crossfades, so the standby's stopped segment must not fake a natural track advance â guarded by a dedicated `standbyScheduleGeneration` (`AudioEngine.swift`). It is only bumped when a crossfade is armed/in-flight (`hadCrossfade` captured before `stopCrossfadeMonitor()` clears the flags); after `finalizeCrossfadeSwitch` the former-standby node's completion IS the active track's natural-end trigger and must stay valid, or the queue stalls.
  - **CI build (`Phase 4`, `.github/workflows/ios.yml`)**: triggered on push to `main` (+ `workflow_dispatch`), `macos-14` runner. Steps: `npm ci` â `npm run build` (dist) â `npm run sync:ios` (copies web assets into `ios/App/App/public`, regenerates SPM package â POSIX on macOS so `fix-spm-path` is a no-op) â `xcodebuild -project ios/App/App.xcodeproj -scheme App -configuration Release -sdk iphoneos CODE_SIGNING_ALLOWED=NO` (this is where the Swift engine finally gets compiled/verified â impossible on Windows) â packages `Payload/App.app` into `mmdrome-unsigned.ipa` â uploads artifact `mmdrome-ios` (14-day retention). Download on Windows â resign with Sideloadly (free Apple ID, 7-day expiry). No `.xcworkspace` in Cap 8 SPM mode â build directly against the `.xcodeproj`. `deploy` (gh-pages) is a manual `npm run deploy`, NOT triggered by CI (no deploy workflow exists).
- *[2026-08-05]* **Phase 5 â first CI compile â green build**. All 34 errors from the first `xcodebuild` run fixed and verified via run #2 (`npm run check` clean, workflow success, 1 MB unsigned IPA artifact). Key learnings:
  - **`AVAudioUnitGain` does not exist in AVFoundation** (phantom class from Phase 2, written from memory). Gain nodes are now `AVAudioMixerNode` with `outputVolume` (Float, ramps via the 40-step `Timer` in `rampVolume`). The graph: `playerA/B â gainA/B (mixers) â mixer â timePitch â varispeed â eq â preamp (mixer) â mainMixerNode`. `EQFilterParameters.gain` and `NativeFilterConfig.gain` (EQ band gains) stay `.gain`.
  - **Capacitor 8.5 `CAPPluginCall` accessors**: `getInt/getDouble/getString/getBool(_ key:, _ defaultValue:) -> T` REQUIRE the default-value argument (`call.getInt("activeIndex", 0)` â there is no `?? fallback` form); `getArray(_ key:, _ defaultValue: JSArray) -> JSArray` / `getArray<T>(_ key:, _ ofType: T.Type) -> [T]?` â the `JSObject.self` form does NOT compile, use `call.getArray("tracks", [])` and `object as? JSObject`. Verified against `node_modules/@capacitor/ios/Capacitor/Capacitor/JSTypes.swift`. (`call.reject` exists on `CAPPluginCall` but the `guard let getArray` + `reject` pattern was removed anyway since the default-value overloads make it unnecessary.)
  - **MPRemoteCommand**: `.enabled` renamed to `.isEnabled` (skipForward/skipBackward/seekForward/seekBackward disabled at NowPlayingController.swift:53-56). Lock-screen time/queue metadata keys: use `MPNowPlayingInfoPropertyElapsedPlaybackTime`/`MPNowPlayingInfoPropertyPlaybackQueueIndex`/`MPNowPlayingInfoPropertyPlaybackQueueCount` â the `MPMediaItemProperty...` variants for those three do not exist.
  - **Swift 6 closure capture**: `self?.handleSegmentCompletion(index: activeIndex, ...)` inside a `scheduleSegment` completion fails with "requires explicit use of 'self'" â capture a local (`let scheduledIndex = activeIndex`) before the closure.
  - **Native white screen** (first sideloaded IPA rendered blank): the built `index.html` used the gh-pages base `/mmdrome/` (vite default), so `/mmdrome/assets/...` 404'd under `capacitor://localhost/`. Fix: `scripts/build-ios.mjs` runs `vite build` with `VITE_BASE=./`; `sync:ios` = `npm run build:ios && cap sync ios && node scripts/fix-spm-path.mjs`; `.github/workflows/ios.yml` uses `npm run sync:ios` (no separate `npm run build`). Web deploy unaffected (`predeploy` still uses default base). SW registration already guarded by `!Capacitor.isNativePlatform()` (src/main.ts:13).
- *[2026-08-05]* **Native WebDAV + safe area fixes**:
  - **WebDAV "load failed" on iOS**: plain `fetch` from the `capacitor://localhost` webview dies at CORS for servers that whitelist web origins (the user's Cloudflare Worker hardcodes `Access-Control-Allow-Origin: https://sieradni.github.io`), failing for ANY url â even invalid ones (blocked at preflight, before DNS). Fix: `src/lib/webdavUtils.ts::webdavFetch` is now the single choke point with a native branch â on `Capacitor.isNativePlatform()` it routes through **`CapacitorHttp.request()`** (built into `@capacitor/core` v8, zero new deps; URLSession has no CORS). Translates `RequestInit`â`HttpOptions`: body `ArrayBuffer`âbase64 + `dataType:'file'` (decoded in `CapacitorUrlRequest.swift::getRequestData`), binary responseâbase64 (`HttpRequestHandler.swift:165`)âchunked `atob`â`Uint8Array`, timeoutâ`connectTimeout`/`readTimeout`, returns a minimal `WebdavResponse` shim (`ok/status/statusText/headers.get/arrayBuffer/text` â callers use only these; web path returns the real `Response`, which satisfies the interface structurally). `navidromeApi.ts::testWebdavConnection` switched from `fetchWithTimeout` to `webdavFetch` so the Settings test uses the same path as real sync. Added `NSLocalNetworkUsageDescription` to Info.plist (LAN servers). Navidrome API calls stay on plain fetch (their server is CORS-enabled).
  - **Dynamic island overlap**: viewport meta lacked `viewport-fit=cover`, so `env(safe-area-inset-*)` was 0 and the existing `.safe-area-*` helpers (`app.css`) did nothing â the `h-dvh` grid laid out under the island (PWA standalone affected too). Fix: `viewport-fit=cover` in index.html; `.safe-area-full` now includes left/right insets, new `.safe-area-x` class applied to the root grid (landscape notch). Status bar: `SystemBars.setStyle({ style: SystemBarsStyle.Dark, bar: StatusBar })` in App.svelte onMount (native only) â `SystemBars`/`SystemBarsStyle`/`SystemBarType` also from `@capacitor/core` v8.
- *[2026-08-05]* **Zoom lock + UI overhaul**:
  - **iOS auto-zoom lock (3 layers)**: viewport `maximum-scale=1.0, user-scalable=no` (index.html) + `ios.zoomEnabled: false` (capacitor.config.ts) + global CSS `font-size: 16px` floor on `input:not([type=range/checkbox/radio]), textarea, select` (app.css). Layered because plain Safari ignores `user-scalable=no` â the 16px floor covers that case and also applies to the web app so both platforms match.
  - **Sliders**: global `input[type="range"] { border: 1px solid rgb(255 255 255 / 0.15); border-radius: 9999px; }` â dark border on all sliders everywhere.
  - **App shell (App.svelte)**: root switched from `grid-rows-[auto_1fr_auto]` to flex column (`main` = `flex-1 min-h-0`) so the sticky header can be conditionally removed on the Settings tab without breaking row placement. Header: `bg-surface/95 backdrop-blur` â solid `bg-background` (seamless black), input `bg-surface-hover` â `bg-white/5`. Mini player scaled up (thumb `h-12`, `text-base`/`text-sm`, transport `p-2`/`h-5`).
  - **SettingsView**: rewritten with 4 tabs (Sources / Playback / Library / About) â Sources = WebDAV + Navidrome, Playback = preload/crossfade/pitch-speed/replay gain, Library = metadata scan + index, About = version. Per-tab scroll persistence: session-only view state (`saveViewStateSession`/`restoreViewStateSession`, backed by sessionStorage â never localStorage) stores `{ tab, scrollTops: Record<tab, number> }`; the save `$effect` writes `scrollTops[tab]` only when it differs (prevents self-trigger loop). Typography scaled up to match 16px inputs (h3 `text-base`, descriptions `text-sm`, action buttons `text-base py-3`).
  - **SongsView**: filter/sort row under the search bar removed entirely; two floating pill buttons (`absolute bottom-5 left-4 z-20`, `bg-surface ring-1 ring-white/10 shadow-lg px-5 py-2.5`) sit bottom-left above the mini player. Sort state shown in the button label (`Sort: Rating â`) â the old "Sorted by" indicator is gone. `filterOpen`/`sortOpen` panels unchanged and still persisted via viewState.
  - **TrackRow + queue rows**: row `gap-3âgap-2`, `px-2âpl-1.5 pr-1`, stars `-ml-1`, `+` button `px-2.5âpx-0.5`, 3-dots `px-2âpx-0.5` (more title/artist room). Queue rows additionally lost the index numbers AND the yellow play arrow entirely (current track is shown by bg highlight + pinned card), drag handle `p-1âpy-1 pl-1 pr-0.5`, action buttons `gap-1âgap-0.5`, current-track `bg-white/5âbg-white/10`.
  - **Albums/Artists detail headers**: Play All is now dark (`bg-surface-hover text-primary`, `px-4 py-2 text-sm`) matching the enlarged back button (`p-2 h-6`) and title (`text-lg`).
  - **Expanded now-playing**: header back/queue buttons `p-2.5 h-7`; new right-aligned utility row between album art and title with the **loop** button (moved out of the transport row) and a new **volume** button opening a floating vertical-slider popover (same logic as VolumeView: `engine.setMasterVolume` + `updateSetting('masterGain')`, 0â200%; rotated -90Â° range input in a `h-32` container; closes on outside click via `[data-volume-popover]` closest-check + `stopPropagation` on the toggle). Title `text-2xl`, transport row scaled up (play `p-3.5 h-9`, prev/next `p-2.5 h-8`).
  - **QueueView**: header â `grid grid-cols-3` (title truly centered, `text-base`), home/back `p-2.5 h-7`, Clear â real button (`bg-surface-hover px-3 py-1.5 text-sm`). The now-playing card is **pinned outside the scroll container** (fixed block between header and list â always visible/controllable while the queue scrolls). Boundary row: dividers `bg-white/20âbg-white/30`, "Auto Queue" label removed, Filter â light button (`bg-white/15 hover:bg-white/25 px-3 py-1.5`).
  - **Drag proxy snap bug**: the ghost was positioned `translate(-50%,-50%)` (centered on pointer) causing a half-width snap left at drag start. Fix: `startPointerDrag` captures the grab offset (`dragOffsetX/Y = e.clientX/Y - rowRect.left/top`); ghost positioned at `pointer - offset` with `transform-origin: top left; scale(1.02)`.
- *[2026-08-05]* **Shared Filter/Sort + non-shuffle auto-queue rotation**:
  - **Shared library filter/sort (`src/lib/libraryFilters.ts`)**: single persisted `libraryFilters` store (`filterOpen, sortOpen, minRating, maxRating, lovedOnly, fromYear, toYear, minLength, maxLength, sortBy, sortAsc`; localStorage key `mmdrome/libraryFilters`) shared by all three views; persisted so it survives tab switches and app restarts. `makeGroupAggregates(tracks, ratingOf, lovedOf)` computes album/artist-level values (avgRating, lovedCount, earliest year, total length) and `applyFilterSort(groups, f)` filters/sorts any `TrackGroupAggregates`-bearing group array â Songs/Albums/Artists all feed the same module. Session-only view state (scroll tops, selected album/artist, Songs' infinite-scroll limit) still goes through `viewState.ts`; the old `[2026-07-27]` note that per-view filter/sort state lives in `viewState` no longer applies (filter/sort now global).
  - **Shared UI (`src/components/FilterSortBar.svelte`)**: Floating Filter/Sort pills + both panels extracted from SongsView into one component mounted in Songs/Albums/Artists. Pills use `bg-white/15` (open `bg-white/25`) for contrast vs the surface background; panel labels bumped `text-xs â text-sm`. Songs passes `onopen={() => limit = CHUNK}` so opening a panel resets the infinite-scroll limit, exactly like the old local `toggleFilter`/`toggleSort`. **Gotcha**: Svelte `class:name={...}` directives can't contain `/` (e.g. `class:bg-white/15`) â parse error; use a computed single `class` string. Album grid count header shows `visibleGroups.length` (post-filter), not the full group count; album/artist detail track lists intentionally use the UNfiltered group so detail views always show all album/artist tracks.
  - **Auto-queue rotation (`queueManager.ts`)**: replaced the `_autoQueueCursor` instance field (started at 0 â never actually rotated) with anchor-based rotation. `_rotateAfterAnchor(pool, libPos, anchorId)` sorts the eligible pool by library position then rotates so the first track positioned *after* the anchor leads, earlier tracks wrapping to the tail; anchor = **last user-queue entry** (`q.userQueue[q.userQueue.length - 1]`), NOT the active track â the user may insert songs past the currently playing one, and the visible queue must stay stable except for appended auto tracks. If the anchor is missing from the library, the pool is left unchanged (starts at the beginning, matching old behavior). Applied in both `replenishAutoQueue` (keptAuto head preserved) and `rebuildAutoQueue`.
- *[2026-08-05]* **Thumbnail load queue + opaque pills + jump-to-current**:
  - **`src/lib/thumbLoader.ts`**: shared distance-prioritized thumbnail loading queue. `requestThumb(el, load)` dedupes by element and starts a single `requestAnimationFrame` loop; each tick recomputes distance-to-viewport-center via `getBoundingClientRect()`, sorts nearest-first, and loads a batch of `MAX_PER_TICK = 3` per frame; entries farther than 3Ã viewport are dropped (they re-request when the still-active IntersectionObserver fires again on re-entry). `cancelThumb(el)` removes on unmount. `LazyThumb` no longer sets `visible` directly or disconnects its observer after first fire â it calls `requestThumb(container, () => visible = true)` and adds `decoding="async"`. This fixes fast-scroll decode backlog: thumbnails at the current scroll position always load first at bounded throughput (native `loading="lazy"` alone doesn't order or throttle decodes).
  - **Opaque pills**: new theme token `--color-surface-raised: #2a2a2a` (app.css) â pills are now fully opaque dark (`bg-surface-hover` closed / `bg-surface-raised` open, `text-primary`, `ring-white/10|20`) instead of translucent `bg-white/15` which read as "transparent" and blended into content scrolling underneath. Same style applied to QueueView's boundary Filter button.
  - **Jump-to-current (`src/components/JumpToCurrentButton.svelte`)**: pill-styled (`absolute bottom-5 right-4 z-20`, single `â` glyph) mirroring the filter/sort pills, mounted in Songs/Albums/Artists (list AND album/artist detail branches) and QueueView. Visibility is derived from the post-filter list (`currentIndex >= 0` in Songs; `visibleGroups` membership for album/artist grids; `selectedTracks` membership in detail views; always in QueueView). Scroll target: `data-track-id` on every `TrackRow` root (prop `dataTrackId`, defaults to `track.trackId`) + queue rows, `data-album`/`data-artist` on grid buttons â looked up with `CSS.escape()` in the view's own scroll container, then `scrollIntoView({ behavior: 'smooth', block: 'center' })` after `tick()` + rAF. SongsView bumps `limit = currentIndex + CHUNK` first when the row isn't loaded yet (infinite scroll).
  - **Detailâback scroll restore**: `scrollRestorePending` in Albums/Artists views is a one-shot flag (armed on mount). After entering and leaving a detail view the list branch remounts at the top and the flag is already consumed, so nothing restores. Fix: a transition `$effect` (`wasInDetail`) re-arms `scrollRestorePending = true` whenever `selectedAlbum`/`selectedArtist` goes non-null â null (the existing restore effect reads the flag as a dependency, so it re-runs once the remounted `scrollContainer` binds).
- *[2026-08-05]* **Metadata rating/love reads + album/artist filter semantics**:
  - **Rating quantization on read** (`metadataReader.ts::extractMetadataFromBuffer`): raw `file.getRating()` returns a normalized 0..1 = **POPM byte / 255** (not a percentage). `Math.round(r * 100)` turned 0.949â¦ (POPM 242, a 4.5â) into 95/100 â 5 full stars. Fix: `rating = popmToLocalRating(Math.round(r * 255))` â 242 â 90 (4.5â), 255 â 100, and other MusicBee POPM bytes land on the 10-step grid. IMPORTANT gotcha: the WRITE map (`ratingToMp3Popm`) and READ map (`popmToLocalRating`) in `tagWriter.ts` are **asymmetric** (write is MusicBee-calibrated and user-verified: writes 204 for 90% but read maps 204 â 80). Do NOT "align" the write map to the read map â it would break MusicBee interop. Round-trip drift exists by design; the read map wins on rescan (re-sync will normalize values to the read map's grid).
  - **M4A loved flag (`LOVERATING`)**: MusicBee writes the loved flag into M4A/iTunes-style freeform atoms as key `LOVERATING` (no space), but ID3/TXXX uses `LOVE RATING`. `extractMetadataFromBuffer` now finds the loved key by `Object.keys(props).find(k => k.replace(/\s+/g,'').toUpperCase() === 'LOVERATING')` â covers both variants. The old hard-coded `props['LOVE RATING']` lookup silently missed MusicBee-tagged M4A files (reported "loved: No").
  - **Filter semantics (`libraryFilters.ts`)**: album/artist rating filtering is now **any-track** â a group passes when at least one of its tracks' ratings is in `[minRating, maxRating]` (implemented in `applyFilterSort` via a new `ratingOf` param + group `tracks`), instead of the old avg-based check that unrated (0) tracks dragged down. `makeGroupAggregates.avgRating` now averages **only rated tracks** (rating > 0) so sorting by rating isn't skewed by unrated songs. Callers pass `ratingOf` (AlbumsView/ArtistsView line: `applyFilterSort(albumGroups, $libraryFilters, getRating)`). Navidrome search `NavidromeSong` has no rating field, so no cross-source guard needed. SongsView was already per-track (filtered directly), unaffected.
- *[2026-08-05]* **Lock-screen / MPRemoteCommand sync fixes (Swift + facade)**:
  - **play/pause commands**: `MPRemoteCommandCenter`'s `playCommand` AND `pauseCommand` both routed to `togglePlayPause()` â if iOS's displayed lock-screen state disagreed with native `isPlaying` (e.g. after interruption/stop), the button did the *opposite* ("press pause â starts playback"). Now explicit `onPlay`/`onPause`/`onToggle`; `playCommand â engine.play()`, `pauseCommand â engine.pause()`, `togglePlayPauseCommand â togglePlayPause`.
  - **Duration sync**: lock screen used raw `NativeTrack.duration` (can be 0 for tracks with missing metadata) while the engine schedules/seeks with `effectiveDuration()`. `refreshNowPlaying` now threads `state.duration` into `NowPlayingController.update(... duration:)` (effective duration), preferred over `track.duration`.
  - **Interruption resume** (`SessionController.swift`): `.ended` previously did nothing (comment claimed "JS layer re-issues play()" â no such JS hook exists). Now `.began` snapshots `wasPlayingBeforeInterruption = isPlaying()`, and `.ended` calls `onResume()` â `engine.play()` (resumes the paused node at `cachedPosition`; `pause()` only pauses nodes, never clears `hasLiveSchedule`) only when the interruption note carries `shouldResume` AND playback was active. `configure(onPause:onResume:isPlaying:)`.
  - **Native speed/pitch persistence** (`engineFacade.ts`): native branches of `setSpeed`/`setPitchOctaves` now also write the `playbackSpeed`/`pitchOctaves` stores (on web those store writes flowed via `onSpeedChange`/`onPitchChange`). `_subscribeShared` persists them. Safe: Svelte `.set()` with an identical value is a no-op (which is also what breaks the web echo loop). Before this, native speed/pitch changes were never saved and restart restored the stale value.
  - **`setSpeed` minimum clamp aligned**: native lowers to `0.2` (was `0.25`), matching web `audioManager` (0.2â4) and the slider range â otherwise 0.2x on iOS actually rendered 0.25x.
- *[2026-08-05]* **Native speed/pitch/tape â params applied only at schedule time (crash fix)**:
  - **Crash signature**: `SIGABRT` on the Capacitor `bridge` queue via `+[NSException raise:format:]` â `AVAudioPlayerNodeImpl::StartImpl` â `-[AVAudioPlayerNode play]` â `NativeAudioEngine.play()` (iPhone 17, iOS 26.5.2). Repro: change speed â audio glitches/freezes at a wrong pitch ("stuck time-stretch kernel") â play/pause â `play()` on the wedged node throws.
  - **Mechanism**: live-writes to `AVAudioUnitTimePitch.rate/.pitch` (or `AVAudioUnitVarispeed.rate` in tape mode) on a running engine can land the unit in a corrupted internal state. Dragging is self-healing (each subsequent tick re-inits the kernel), but **reset** (and tape toggles, which force one unit to `1.0`) make a single final write with no follow-up â kernel stays wedged â frozen/wrong-pitch output â subsequent `player.play()` aborts in `StartImpl`.
  - **Fix**: `speed`/`pitchOctaves`/`tapeMode` are now plain fields (guarded no-op setters); `refreshPlaybackParams()` writes the units ONLY inside `scheduleCurrentTrack`, after `player.stop()` on both nodes (`AudioEngine.swift`, "units only ever touched while nothing is rendering"). While playing, a param change sets `paramsDirty` + debounces (60ms Timer) a `restartForParams()` = `cancelScheduled()` + `scheduleCurrentTrack(from: currentPosition, autoPlay: true)` â replays from the same spot with a fresh render plan (cancels any in-flight crossfade). `play()` resumes via `restartForParams()` when `paramsDirty` (paused param changes must take effect). `stopPlayback()` invalidates the restart timer. No `bypass` flags needed since the units never transition mid-render.
  - **UI**: `PitchSpeedView.resetAll()` no longer resets Tape Mode (removed `tapeMode=false` + `engine.setTapeMode(false)`); only speed slider + pitch reset. This also removes a forced tape-toggle from the crash scenario.
- *[2026-08-05]* **Blank thumbnails on first-ever Navidrome connect (root cause + 3 fixes)**:
  - **Root cause**: `LazyThumb.svelte` gates the `<img>` on `{#if visible && $coverConfig}`, and the ONLY writers of the `coverConfig` store were App.svelte startup (`setCachedConfig` from persisted settings) and the *cached* library path in `syncEngine.connectNavidrome`. A first-ever connect (empty library â fresh-load path; SettingsView's `connectNavidrome(true)` always forces fresh) never set it â `$coverConfig` stayed `null` â every thumbnail blank until a full reload re-ran startup from now-persisted settings. (Confusingly, the now-playing overlay still worked because `playbackManager` reads the module-private `_cachedConfig`, which `loadNavidromeSongs` did set.)
  - **Fix**: `navidromeApi.loadNavidromeSongs` now calls `setCachedConfig(config)` (single choke point â sets both `_cachedConfig` and the `coverConfig` store). Covers first connect, reconnects, and all future callers. Removed the dead `connectNavidrome as navidromeConnect` import in `syncEngine.ts`.
  - **Stale URL cache on server switch**: `coverArtCache.getCoverUrl` cached by `trackId-size` only, but baseUrl/username/password (+ auth token/salt) are baked into the URL â switching Navidrome servers (or creds) served URLs pointing at the OLD server until refresh. Cache key now prefixes `baseUrl|username|password`.
  - **thumbLoader stranding**: `tick()` silently DROPPED entries whose rect was 0Ã0 â but `LazyThumb`'s IntersectionObserver only re-fires on intersection *changes*, so a dropped entry never re-requested â blank until remount/refresh. Fix: zero-size entries now stay pending behind a retry counter (`MAX_ZERO_SIZE_RETRIES = 10` frames, sorted last via `MAX_SAFE_INTEGER`) and only disappear if still unsized after ~10 frames.
  - **Gotcha**: keep `coverConfig` populated whenever a Navidrome config is used to fetch songs, or thumbnails silently stay blank with no error anywhere.
- *[2026-08-06]* **Session-only settings view state, Push Changes, user-queue counter, Songs load-more fix**:
  - **Settings tab persistence is session-only**: `src/lib/viewState.ts` gained `saveViewStateSession`/`restoreViewStateSession` (same merge semantics as the localStorage-backed pair, persisted to `sessionStorage` â never localStorage). SettingsView restores `tab`/`scrollTops` **synchronously at component init** (not in `onMount`) so the save `$effect` can't clobber the saved tab with the `'sources'` default before it's read. Result: the tab survives tab switches, view switches, and reloads within the session, but a fresh session opens on Sources. Other views' localStorage viewState is untouched.
  - **Sync Now â Push Changes**: the WebDAV "Sync Now" button moved to the Library tab as its own **Push Changes** section (same `runManualWebDAVSync()`; `syncNow()` renamed `pushChanges()`; result reads "Pushed X track(s), Y failed"). In its place, the Sources/WebDAV section now has a second **Check Modified Ratings** button wired to the same `startMetadataScan()` + shared `metadataScanState` store â both tabs show live "Scanning X/Y..." progress and the complete/scanning status lines.
  - **Queue counter counts only the user queue**: App.svelte's `queueSize` is now `$queue.userQueue.length` (was combined). `queuePosition` stays `activeIndex + 1` â valid because the user queue is always a prefix of the combined queue and the active track is promoted into it, so the 1-based combined index equals the track's position in the user queue in steady state (x can briefly exceed y during auto-track transitions; "Queue is empty" now means the user queue is empty).
  - **SongsView infinite-scroll stuck fix**: the sentinel IntersectionObserver only fires on intersection *changes* â broadening the filter while the sentinel was already intersecting (user at the bottom) flipped `hasMore` to true ("Loading more...") but never triggered a load until a scroll up/down. Added an `$effect` that, while `hasMore`, compares the sentinel's `getBoundingClientRect()` against the container bottom + 200px (matching the observer's `rootMargin`) and bumps `limit += CHUNK`; it self-terminates because appended rows push the sentinel out of the margin. The observer still handles ordinary scrolling.
- *[2026-08-06]* **Auto-queue follows Songs-view sort; native lock-screen pause refresh; QueueView icons**:
  - **Non-shuffle auto-queue respects the shared sort ordering** (`queueManager.ts`): the non-shuffle fill branch of `replenishAutoQueue`/`rebuildAutoQueue` no longer sorts strictly by library index. New `_buildOrderRank(meta)` ranks the FULL library using the shared `libraryFilters` sort (same comparator as SongsView: rating/loved/year/length Ã `sortAsc`, via `metadataCache`; ties broken by library index for a total order). The existing `_rotateAfterAnchor` is reused unchanged with that rank map, so the auto-queue continues from just after the last user-queue entry **in the sorted view order**. `sortBy == null` keeps library-order (old) behavior. Gotcha: while shuffle is off (`playbackManager._subscribeShared`), a **sort change itself rebuilds** the auto queue (sorted `sortBy`/`sortAsc` diff; no-op fires and filter-open toggles are skipped), so the queue always mirrors the current Songs-view order once shuffled off. Note: this ordering also applies to album/artist-scoped pools (`albumScope`/`artistScope`), so a scoped album queue follows the global Songs sort (e.g. by rating/year) rather than track-number order.
  - **iOS lock screen pause state** (`BackgroundAudioPlugin.swift`): `play`/`pause`/`toggle` plugin handlers now call `refreshNowPlaying()` unconditionally after the engine command. Previously now-playing info was only pushed though `engine.onPlaybackStateChanged`, which fires inside `AudioEngine.setPlaying(_:)` **only when `isPlaying` actually flips** (and `engine.pause()` early-returns on `guard isPlaying else { return }`) â so any no-op/edge command left the lock screen / control center showing the previous "playing" (pause) button forever. `NowPlayingController.update` already encodes `MPNowPlayingInfoPropertyPlaybackRate = playing ? speed : 0`, so the fix is purely forcing the refresh.
- **QueueView icons**: "View details" swapped from the stroke circled-`i` (which read as "!"/info dot) to a filled document-with-lines glyph (same in user + auto rows); "Move to end" swapped from the three-line list icon (which duplicated the drag-handle glyph) to a down-arrow-into-a-bottom-line ("send to end") glyph.
- *[2026-08-06]* **Icon unification**: ALL "view details" affordances now use the eye glyph from `TrackOptionsView.svelte` (stroke eye: `M12 4.5C7 4.5...`) â QueueView user/auto rows, the `TrackOptionsDropdown` "Details" menu item (h-3.5, was a circled-`i`), and TrackOptionsView itself (already eye). Supersedes the earlier doc-with-lines/circled-`i` split. "Move to end" (user rows only) dropped the down-arrow-into-line (read as a download button) for lucide-style double-down chevrons (`M7 13l5 5 5-5` + `M7 6l5 5 5-5`, stroke) â "jump to the bottom of the queue".
- *[2026-08-06]* **Native tail sync + rating/loved rebuild + queue wrap hint**: (1) `_refreshNativeQueue()` is now also called after any `rebuildAutoQueue()` from `_subscribeShared` (shuffle toggle, non-shuffle sort change) and after the rating/loved library-refresh rebuild, so the native armed-crossfade/standby is re-armed against the new tail instead of targeting a removed track. The method gained a `Capacitor.isNativePlatform()` guard at the top â `_subscribeShared` runs on web too, and without the guard every rebuild would spam the unregistered plugin. (2) `library.subscribe` now calls `rebuildAutoQueue()` (not `replenishAutoQueue()`) when shuffle is off AND `sortBy` is `rating`/`loved`, so a metadata refresh (rescan reads new ratings/loves) reorders the queue to match the view; other sorts keep append-replenish. (3) New `queueWrapNotice` store (`appState.ts`): `_rotateAfterAnchor` sets it `true` only when the pool has candidates but NONE rank after the anchor (nothing follows it in the sorted order â regenerates from the top); cleared otherwise, including in both shuffle branches. QueueView renders a small "Continuing from the top of the sort order" line under the boundary row while set â makes the wrap around the end of the sort order read as intentional.
- *[2026-08-06]* **Scrobbling, ratings-source routing, genre filters, sleep timer, brand icon**:
  - **Scrobbling** (`src/lib/scrobbleManager.ts`): subscribes to `currentTrack`/`currentTime` so one module covers web (timeupdate) and native (position poll). On track start it fires `nowPlaying.view` (Navidrome forwards to Last.fm/ListenBrainz server-side); on track leave it submits `scrobble.view` (submission=true) once the track reached â¥50% (or â¥240s for long tracks). Only `navidrome-`-prefixed tracks are scrobbled. New `scrobbling` setting (Playback tab) gates it.
  - **Ratings source** (`src/lib/feedbackService.ts`): `commitFeedback(track, rating, loved)` is now the single UI commit path (TrackOptionsView + DetailView). `ratingSource: 'webdav'` (default) caches locally as `pending_sync` (Push Changes still writes tags); `'navidrome'` writes star/unstar + `setRating` (1â5) directly to the server and never marks `pending_sync`. `syncToNavidrome` (meaningful only in webdav mode; forced ON when `ratingSource` flips to navidrome) mirrors every change to the server; turning it on runs a `reconcileToNavidrome()` diff push of all cached ratings. `seedNavidromeFeedback(tracks)` merges server `starred`/`userRating` into the metadata cache on Connect (skips `pending_sync` rows). `userRating` is Subsonic 1â5 â local 0â100 via Ã20.
  - **Genre filters**: `genre` added to `LibraryFilterState` (shared, persisted) + `AutoQueueFilters`, with `trackMatchesGenre` (token-aware: case-insensitive split on `/`, `;`, `,`) and `distinctGenres(library)`. Applied in SongsView `processed`, `applyFilterSort` (any-track match for album/artist groups), and `_matchesAutoQueueFilters`. Genre `<select>` added to FilterSortBar and the QueueView filter panel. `Track.genre` now mapped from Navidrome songs.
  - **Sleep timer** (`src/lib/sleepTimer.ts` + native engine): JS store `sleepTimer` (`active`, `mode: 'minutes'|'endOfTrack'`, `endsAt`, `remainingSeconds`) mirrored by `sleepTimerManager` â web runs a local countdown/end-of-track watch and pauses via `playbackManager.pause()`; native sends `BackgroundAudio.setSleepTimer({active,mode,minutes})` where the engine owns the timer (run on the MainLoop so it survives backgrounding), cancels it in `stopPlayback()`, tears down armed crossfades on end-of-track, pauses at the natural track end, and emits `sleepTimerFired` (JS `sleepTimerManager.init()` listens and clears local state). UI: clock button + popover (raw minutes input, End of Track, Cancel) in the now-playing utility row.
  - **Brand icon**: downscaled `C:\Users\micha\Downloads\icon.png` (2048Â²) by System.Drawing HighQualityBicubic (pure downscale) into `public/icon-{192,512,1024}.png` via a PowerShell script, plus the single 1024Â² `AppIcon.appiconset` asset. `manifest.webmanifest`: `purpose` `"any maskable"` â `"any"` (avoids Android circular mask cropping of the square logo), added the 1024 entry. `public/sw.js` precaches `icon-1024.png` too. `LazyThumb` shows the brand icon as a fallback (on `onerror`/missing coverConfig) instead of an empty tile. `index.html` favicon already pointed at `/icon-512.png` (now the brand icon).
- *[2026-08-06]* **Rating-clear push fix (`feedbackService.ts`)**: `pushToNavidrome` originally guarded rating pushes with `if (rating > 0)`, so clearing a rating (0) never reached the server: in `ratingSource: 'navidrome'` the cleared change was marked `synced` and the next `seedNavidromeFeedback` re-imported the stale server value (cleared rating resurrected); in webdav-mirror mode it couldn't be mirrored either. Fix: `pushToNavidrome(track, rating, loved, prevRating)` now pushes `setRating(songId, 0)` when `rating == 0 && prevRating > 0` (an actual clear) but skips it when rating is 0 because the track simply got a â¥ with no prior rating (prevRating 0) â mirroring unstar symmetry without wiping unrelated server ratings. Callers pass `existing?.rating ?? 0` from the metadata cache. `npm run check` clean.
- *[2026-08-06]* **Native track-advance stall + end-of-track sleep resume (AudioEngine.swift + web parity)**:
  - **Stall on next track â in-flight prefetch deadlock**: `TrackFileLoader.prefetch` returned WITHOUT invoking `completion` when `activeTasks[trackId]` was non-nil (the download had been started by `prefetchNeighbors` at the previous track's start). `loadAndStart` schedules its track ONLY inside its prefetch completion, so the new track was never scheduled â engine silent with `isPlaying` stuck true and NO `trackChanged`/`ended`/`error` emitted (JS never advances). `play()` (`hasLiveSchedule == false`) and `seek()` ("Track not ready" â JS retry replays the old track) re-enter the same deadlock until the file finally lands â "sometimes works". Worst with slow/lossless downloads, short tracks, rapid next-skips, and long crossfades where the standby isn't ready and the transition falls back to a plain end. **Fix**: `pendingCompletions: [String: [(URL?, Error?) -> Void]]` â an in-flight prefetch chains the new completion instead of dropping it, and the download callback fires ALL chained completions (success: cached URL; failure: error) so the next track schedules the instant its file lands. Added `loader.evict(trackId)`; `scheduleCurrentTrack`'s `AVAudioFile(forReading:)` guard now evicts corrupt/partial downloads instead of replaying a poisoned file forever (JS retry re-fetches). NOTE: `preloadTracks` is a WEB-ONLY setting (`setupPreloader` only in `_initWeb`) â on native it's a no-op; native "preload" is `prefetchNeighbors`.
  - **Sleep "end of track" dead resume**: the sleep-at-end branch in `handleSegmentCompletion` paused with `activeIndex` on the finished track and `hasLiveSchedule` true over a consumed `.dataConsumed` schedule â subsequent `play()` resumed a dead node â no audio, no `trackChanged`, JS `currentTrack`/queue position stuck. **Fix**: `waitingAtTrackEnd` flag set in that branch; `play()` consumes it â `advanceFromSleepPause()` (loop-one restarts current; else `nextIndex` â `playTrack` â `onTrackChanged`; queue end â `handleTrackEnd()`), cleared in `cancelScheduled()` (seek/param-restart/stopPlayback/setQueue). Resume now advances to the next song AND fires `trackChanged` so JS state advances.
  - **Web parity**: (1) `playbackManager.pause()` pauses `audioManager.playbackElement` (the `_bgEl` in iOS-Safari background mode) instead of `activeElement` â otherwise the sleep pause can't stop background audio on the PWA. (2) `sleepTimerManager` gained `pendingStop`/`consumePendingStop()`/`clearPendingStop()`: the end-of-track watch sets it, `_loadAndPlay`/`_loadAndPlayInBg` check it before their autoplay (so an in-flight transition stays parked instead of the pending `play()` undoing the pause), and explicit controls (`play`/`next`/`prev`/`seek`/`playTrackAt`/`playTrackById`) clear it.
- *[2026-08-06]* **Cross-platform audit fixes (native queue sync, metadata scan semantics, replay-gain crossfade, settings race, prev/volume parity)**:
  - **Native tail sync on ANY queue mutation**: `playbackManager._subscribeShared` now also `queue.subscribe`s and calls the new `_scheduleNativeQueueSync()` â a microtask-coalesced `_refreshNativeQueue()`. Previously only `_subscribeShared` triggers (library/shuffle/sort) synced the native tail, so user edits (`addToUserQueue`, `playNext`, `removeFromUserQueue`, `removeFromAutoQueue`, `clearQueue`, and QueueView's own `queue.update` reorder/clear) drifted from the native engine (armed crossfade could target a removed track). Coalescing matters: a single transition mutates the queue store several times (`setActiveQueueIndex` â promote â replenish), and each native `refreshQueue` tears down/re-arms the armed crossfade â the microtask collapses same-task bursts into one call. `_scheduleNativeQueueSync` is a no-op on web (`!isNative()`), pre-engage (`!_hasNativeEngaged`), and pre-init (`!_initialized`); `_refreshNativeQueue` itself gained the same `_hasNativeEngaged` guard (it already had `isNative()` â without `_hasNativeEngaged`, the queue.subscribe's very first fire before engagement would still send a snapshot).
  - **Web crossfade replay-gain leak**: `_executeCrossfade` only wrote the standby element's rg gain when the upcoming track HAD replay gain, leaving the OLD track's gain applied to a no-RG track. Now `standbyRgGain.gain.value = _nextTrackReplayGainLinear ?? 1`.
  - **"Check Modified Ratings" now honest** (`SettingsView`): `startMetadataScan` called `scanAllNow(true)` which forces `rebuildIndex()` (full WebDAV PROPFIND walk), contradicting both the button label and the incremental intent (the index was ALSO just ensured by the preceding `ensureIndex()`). Now `scanAllNow(false)` â incremental changed-track scan only. The forced full index+rescan remains on "Rescan All Metadata" (`scanAllForceRescan`).
  - **Settings credential race**: settings fields debounce store writes by 300ms, but action buttons (`Test`/`Rebuild Index`/`Check Modified Ratings`/`Rescan`/`Connect & Load`/`Push Changes`) read `$settings`/Dexie immediately â clicking within the debounce window used stale creds. Added `SettingsView.commitCredentials()` (awaits `setSetting` for all six credential fields + `updateSetting`) and every handler awaits it first. Also trimmed `saveViewStateSession('settings', â¦)` payload to only `{ tab, scrollTops }` â credentials were previously mirrored into sessionStorage (a redundant plaintext copy `restoreViewStateSession` ignored).
  - **prev() parity (web matches native)**: native `previous()` restarts the track when offset > 3s and wraps on loop-all; web `prev()` was a pure no-op at index 0. Web `prev()` now: restart at 0 when `currentTime > 3`; else go back one; else (loop-all) jump to the last track; else restart at 0 â mirroring `AudioEngine.previous()` exactly.
  - **Master volume range parity**: native `setMasterVolume` clamped to `0...1` (capped at 100%) while the web engine allows 0..200% (the preamp is a `GainNode` on web but an `AVAudioMixerNode` whose `outputVolume` accepts >1). Changed the native clamp to `max(0, min(2, volume))` so the "200%" end of the VolumeView slider works on iOS just like the PWA.
- *[2026-08-07]* **Half-star rating fidelity fixes (3 parts, `npm run check` clean)**:
  1. **`seedNavidromeFeedback` no longer clobbers WebDAV ratings in `webdav` mode** (`appState.ts`): on every connect the loop overwrote `existing.rating` with `Math.round(userRating * 20)` whenever `syncStatus !== 'pending_sync'` â i.e. Navidrome's coarse integer star (1â5) silently replaced the fine-grained (0â100) value read from file tags, and the incremental WebDAV scan never restored it (unchanged file mtime â skipped). Fix: in `webdav` mode, Navidrome's `userRating`/`starred` only seed rating/loved when there is no existing entry OR no `webdavPath` and rating/loved are still zero; in `navidrome` mode server values always win. Navidrome's integer `userRating` roundtrips through `*20` are inherently whole-star â keep it from overwriting tag-level 10-step (half-star) ratings.
   - **`TrackDetailsModal.svelte` now routes through `commitFeedback`**: it previously wrote `pending_sync` + `updateMetadata` directly, bypassing `ratingSource`/`syncToNavidrome`. Now consistent with TrackOptionsView/DetailView (single authoritative commit path).
   - **`popmToLocalRating` thresholds** (`tagWriter.ts`): old read map turned its OWN writes lossy (write 90âPOPM 204, reread 204â80 = 4â not 4.5â; 70â154 reread 154â60; 10â13 reread 13â10 OK but boundaries drifted). New map: `<=19â10, <=39â20, <=63â30, <=90â40, <=116â50, <=140â60, <=166â70, <=195â80, <=248â90, else 100` â every `ratingToMp3Popm` output round-trips exactly. **Major FORCE-RESCAN footgun**: `metadataScanner.processItem` previously overwrote EVERY scanned row with the file tag + `syncStatus: 'synced'` â so a "Rescan All Metadata" (force) could silently erase a not-yet-pushed `pending_sync` rating/loved edit (the file still had the OLD tag since Push hadn't written it). Guarded: `processItem` now skips rows whose existing `syncStatus === 'pending_sync'` (counted as scanned, not failed). What remains safe for pending edits: app re-connect (seedNavidromeFeedback skips pending_sync), incremental "Check Modified Ratings" (unchanged mtime â not queued), and Push (writes intent). **Do NOT regress the skip** â a force rescan before Push wipes local edits.

- *[2026-08-07]* **Library-replacement hardening + shared Navidrome pipeline (`npm run check` clean)**:
  - **Shared pipeline** (`src/lib/syncEngine.ts::loadLibraryFromNavidrome`): connect -> `setLibrary` -> metadata seeding -> server lastScan -> **automatic incremental WebDAV metadata scan** when WebDAV is configured. App startup AND SettingsView "Connect & Load Songs" now both call exactly this, so the Settings path no longer skips the auto-scan (metadata was stale until a manual scan).
  - **Cache fingerprint**: `connectNavidrome` song-library cache key now `baseUrl|username|lastScan` â switching Navidrome servers no longer serves the previous server's cached tracks.
  - **`setLibrary(tracks, complete=true)`** now reconciles everything on a full load: queue ids missing from the new library are dropped (user/auto/history), `activeIndex` re-anchored via the old active track, and `pruneStaleMetadata` batch-deletes metadata rows of vanished tracks â **pending_sync rows are kept by both** (surfaced by Push Changes, never silently lost). `removeMetadata` (appState) deleted â dead.
  - **Auto-queue**: library.subscribe and scan-complete (rating/loved sorts only) now `replenishAutoQueue()` instead of the old rating/loved `rebuildAutoQueue` â replenish keeps the queued head (including any ARMED crossfade target) in place; rebuilds only happen on user sort/shuffle changes. A rebuild mid-play could drop an armed track and the crossfade rescue would then mutate userQueue.
  - **Push Changes** (`runManualWebDAVSync`): rows without a matched `webdavPath` are now **skipped, not written to fabricated `/navidrome-...` paths**; rows carry a new `webdavBase` (server base url stamped by the scanner) and rows whose base doesn't match the CURRENT server are skipped too (prevents pushing onto the wrong server). `skipped` count shown in the result line. Push also updates the in-memory `metadataCache` row (was Dexie-only â row stayed pending in the UI). Rows flagged `ignored` (File Matching "Not on this server") are skipped even when a path is stamped.
- *[2026-08-08]* **File Matching (manual WebDAV binding UI)**:
  - **`matchTrackToWebdavCandidates` + `TrackMatchCandidates`** (`metadataReader.ts`): the match scoring (`scoreTrackMatches` — 100/80/60 title-vs-filename ranges, +10 size bonus, 40 size-only) is now shared between the unchanged `matchTrackToWebdav` (unique-best or null/ambiguous — push safety unchanged) and a candidates view: `{ status: 'matched'|'ambiguous'|'none', promptCandidates, allCandidates }` (the tied top-score group when ambiguous, top 5 near-misses when none, all extension-matching files for the search picker).
  - **Scanner services** (`metadataScanner.ts`): `listUnresolvedMatches()` — every library track the scanner can't safely target: `no-match`, `ambiguous`, `vanished` (stamped path gone from a fresh index), `stale-base` (path present but `webdavBase` != current `url|user` — server switched), `ignored`. Pending-push rows sort first (they block Push Changes — that's the point). Uses the in-memory index when built this session (no extra PROPFIND), else `refreshIndex()` once (probe failure throws). `bindTrackToFile(trackId, path, force)` — validates the path against the in-memory index, **double-bind guard** (conflict => bound already, `force=true` skips), stamps `webdavPath`/`webdavLastModified`/`webdavBase` + `matchSource:'manual'`, preserves `syncStatus` (a pending edit becomes pushable), creates a row from the library track when none exists. `unbindTrack`, `ignoreTrack`, `unignoreTrack`, `searchWebdavFiles(query, fileType)` (in-memory, cap 50).
  - **Issue-1 guard (processItem)**: rows with `matchSource === 'manual'` whose stamped path still exists in the index keep their bound path — only `webdavBase`/`webdavLastModified`/`fileType` refresh against the BOUND entry. The auto-matcher must never overwrite a user's pick (WebDAV layout can differ entirely from Navidrome's). The vanished-clear branch resets `matchSource` (row becomes re-pickable). Ignored rows are skipped by scans entirely.
  - **SettingsView Library tab — File Matching section** (below Metadata Scan): auto-loads on tab open; header count + Refresh (re-probes via `refreshIndex`); per-kind badges (Ambiguous yellow / No match red / File vanished red / Server changed orange / Ignored gray); rows show a `pendingPush` warning ("blocked until a file is bound"); actions per kind: stale-base => Re-stamp to current server; regular => Find/Choose file… (picker: prompt candidates + full search with Enter) + Not on this server; ignored => Un-ignore + Hide/Show ignored (N) footer. Double-binding shows a modal with the other track's title + Bind anyway (`force`). Creds-less state surfaces "WebDAV credentials not configured" instead of a misleading "All tracks matched".
  - **Review-fix round (`npm run check` clean, commits fbaa6be..394ef43)**:
    - **Force-bind is now truthful**: `bindTrackToFile(..., force)` no longer skips the conflict guard — it UNBINDS every other row targeting that path (clearing `webdavPath`/`webdavBase`/`matchSource`, preserving rating/loved/status) so "the other track becomes unmatched" really happens, and REFUSES (`conflict-pending`) when any other row carries `pending_sync` (unbinding would strand the edit — no path, Push/scan skip it). `runManualWebDAVSync` also dedupes per run: a `pushedPaths` set skips later rows targeting an already-pushed path (first writer wins; the row stays pending and surfaces on the next Push) — heals pre-existing duplicate binds.
    - **`matched` audit bucket**: `listUnresolvedMatches` now also lists correctly-bound rows (kind `'matched'`, `matchSource` 'auto'|'manual') so a wrong auto-match can be audited and **Clear match** (`unbindTrack`) — previously impossible: matched rows never appeared and `doUnbind` had zero call sites. UI: "Show matched (N)" footer toggle; badge shows "Manually bound" for manual rows.
    - **Cheap exact counts + cap**: `listUnresolvedMatches` returns `{ rows, counts, pendingBlocked }` — per-kind counts cover the WHOLE library without scoring (classification by row state only), rows are capped at `DISPLAY_CAP = 100` (pending-push first), "+ M more" footer appears when capped, count line "Unresolved — X no match, Y ambiguous…; Z blocked by pending edits" under the section intro.
    - **stale-base rows get the picker**: Re-stamp AND "Find file…" (their prompt candidates + the full search picker); `bindError` now renders at card level (was only inside the open picker — Re-stamp failures were silent).
    - **Ignored pending truth**: ignored rows' `pendingPush` is now `syncStatus === 'pending_sync'` (was hardcoded false) — the warning says push is skipped while ignored instead of hiding the stuck edit.
    - **Manual-bind guard BEFORE the matcher** (processItem top): valid manual bindings are counted as scanned and never re-matched — a later scan could previously bump `ambiguousCount` forever ("N ambiguous" never dropped) even though the row was resolved. The old success-path manualBind/boundEntry logic was deleted (now unreachable). `bindTrackToFile` distinguishes missing creds (`no-creds`) from a missing row.
    - **UI wording**: search picker shows "No matches for …", conflict-pending refusal explains the pending edit, pending-push warnings are kind-aware (ignored / stale-base / unbound).
    - **Review-fix round 2 (4eb674c..9b59c57)**:
      - **Rank-sort before the cap** (`listUnresolvedMatches`): the old loop dropped rows beyond `DISPLAY_CAP` in library order and sorted *after* — a big library whose first 100 tracks were matched/ignored rendered zero unresolved rows while the count line claimed "X no match, Y ambiguous". Now every row is classified, then sorted by rank (0 = unresolved+pending edit, 1 = unresolved, 2 = matched/ignored, then title), THEN capped — the blocked/unresolved rows always make the cut. Prompt candidates are kept only for the displayed (capped) row objects; unbound rows are still scored once each for the exact no-match/ambiguous count split (unavoidable — the split IS the count).
      - **Manual-bound rows are re-read, not skipped** (`processItem`): round-1's blanket top-of-function skip froze tag propagation — a MusicBee edit on a manually bound file never reached the app in `webdav` mode (row counted scanned, returned, never read again). Now a valid manual binding takes the BOUND-file read path: `readFileMetadata(webdavUrl, existing.webdavPath, …)` (the bound path, NOT the auto-matcher's pick — round 1 read the auto path from the success branch), keeps `webdavPath` + `matchSource:'manual'`, refreshes `webdavLastModified`/`webdavBase` from the bound index entry, propagates rating/loved/comments in `webdav` mode while the `navidrome` branch preserves cached values, re-checks `pending_sync` between fetch and write, and counts read failures as `failedCount`. Phantom-ambiguous stays fixed: valid manual rows never reach the matcher.
      - **pendingBlocked now includes vanished+pending** — those rows are equally un-pushable (404 → skipped), so the count line and row warnings agree.
      - **bindError is row-scoped** (`bindError: { trackId, message } | null`) — a failed Re-stamp on row A no longer paints the same red text on all 100 cards. Vanished pending warning says the bound file is gone ("re-bind to push").
- *[2026-08-08]* **File Matching polish — discard action + plain-English copy** (commit d3105a1):
  - Added `discardLocalEdit(trackId)` (`metadataScanner.ts`) — abandons a pending local rating/loved edit without pushing, resetting the row to `synced` while keeping `webdavPath`/`webdavBase`/`matchSource` so the track stays linked. Exposed in the File Matching UI as a **"Discard local change"** button on pending rows.
  - Reworded File Matching UI copy to plain English: badges (file not found / multiple matches / removed from server / server URL updated / ignored / matched), count-line bits, stale-base button "Update file link", picker button "Select correct file…", intro copy "link a file / mark as not on this server".
- *[2026-08-08]* **Push confirmation dialog** (commit 5111d50):
  - `Push Changes` now counts safely-bindable pending rows (`webdavPath` present + `webdavBase === baseUrl|user`) and shows a modal ("Write these N ratings to WebDAV files? … wrong file link would overwrite the wrong tags — check File Matching") with Cancel/Write to files, instead of writing immediately. Rows that can only be skipped (no path / wrong server) still run headlessly and render the result line (preserves the original "Pushed 0, N skipped" reporting path).
   - Honest comment in `listUnresolvedMatches`: exact no-match/ambiguous counts genuinely require scoring every unbound track (no cheap tie-classification); the cap only trims displayed rows' candidate arrays.
   - **Show/hide ignored/matched toggles fixed** (commit 5d1d15d): the filter was a shared ternary that fell back to the unfiltered list when *either* toggle was on — so "Show matched" also revealed ignored rows. Switched to an independent AND of two kind checks so each toggle controls only its own kind.
   - **Accurate "more" count** (commit f21b4a2): the `+ N more (list capped)` footer now compares the filtered shown count against `countTotal()` — "Showing X of M (list capped …)" when DISPLAY_CAP rows are rendered, or "+ N hidden by filters" otherwise; `DISPLAY_CAP` is exported and referenced instead of a magic `100`.

- *[2026-08-07]* **Scan-freshness overhaul + push/matching hardening (findings-driven, `npm run check` clean)**:
  - **"Check Modified Ratings" now always probes the server**: `ensureIndex` deleted; `refreshIndex()` (metadataScanner) does a fresh PROPFIND; `scanAllNow(false)`/`scanAllForceRescan` refresh FIRST and abort into `status:'error'` ("Index refresh failed — is the WebDAV server reachable?") on probe failure — never scan a stale snapshot (a stale index could never detect remote MusicBee tag edits: both compared mtimestates were old). The Dexie index remains only as a write-through snapshot (debug tooling reads it), never a scan basis. CRITICAL: loading now does exactly one PROPFIND per connect — pre-call `ensureIndex()` before `scanAllNow` would double it (removed from `SettingsView` + `loadLibraryFromNavidrome`); `ensureIndex` exported symbol was DELETED (debug tool uses `refreshIndex()`).
  - **`matchTrackToWebdav` returns `{ entry, ambiguous }`** (`metadataReader.ts`): a tie for the top score — e.g. duplicate "01 - Intro.flac" same title+size, or 2+ files matching by size only — returns `entry: null, ambiguous: true` instead of guessing the first index entry (guessing could rewrite the WRONG file's tags on Push). `debugTrackData`/`readFileMetadataWithIndex` updated.
  - **Vanished-file cleanup** (`processItem`): when a row's `webdavPath` is absent from a FRESH index, the path/base are cleared (rating/loved/status preserved, counted missing) so the row can re-match after renames and Push skips the gone file (404 → NotFoundError → skipped, not failed) instead of failing forever. `pending_sync` rows are still skipped before all this.
  - **`MetadataScanState.progress` gains `missing` + `duplicateMatches`**; SettingsView's two status sites now render the error state red (previously error was never rendered — the empty-library error sat on a 'complete' state and showed green "Scan complete") and the complete line appends ", N files missing / M ambiguous".
  - **Library replacement hardening**: `connectNavidrome` falls back to a valid same-baseKey song cache when the live load errors with zero songs (`cached: true`, error kept on `loadResult`); `loadLibraryFromNavidrome` refuses to `setLibrary` when `loadResult.error && songs.length === 0` (a failed load must not wipe the queue via reconcile; empty-success still replaces). `unManualWebDAVSync` result gains `wrongServer` count (distinct from skipped), surfaced in the Settings result line.

- *[2026-08-07]* **Audit fixes: push re-pend, provenance guard, fingerprint scan gate, wrap-notice condition, test-and-scan (`npm run check` clean)**:
  - **Push re-edits race (`runManualWebDAVSync` tail)**: the push loop wrote `{...track, syncStatus:'synced'}` from its stale snapshot — an edit landing during the GET→PUT got clobbered in Dexie + the in-memory cache, and since `webdavLastModified` was never updated the mtime diff skipped it next scan (edit invisible until force rescan). Now after each successful PUT the live `metadataCache` row is checked: if it's `pending_sync` with differing rating/loved, the newer values are re-upserted as `pending_sync` (file already carries the pushed values; next Push surfaces the intent) instead of flattening to `synced`. Covers the conflict-retry path too (same tail).
  - **webdavBase — never push unverified rows**: rows with NO `webdavBase` (scanned before base stamping) were previously pushed against the CURRENT creds — a coincidentally identical path on a different server meant rewriting the wrong file. Now: no `webdavPath` → `skipped`; no `webdavBase` → `skipped` (legacy/unverified); `webdavBase !== currentBaseKey` → `wrongServer` (distinct count kept for the UI wording).
  - **Unmatched-retry gate — `computeIndexFingerprint`** (metadataReader.ts): FNV-1a over sorted `path|size` pairs. `refreshIndex()`/`rebuildIndex()` persist it on the Dexie `WebdavFileIndex` snapshot; `scanAllNow(false)` reads the PRIOR snapshot's fingerprint BEFORE the probe (refreshIndex overwrites the stored one) and skips re-queuing `unmatched` rows when the set is unchanged — unmatchable rows can't become matchable without an added/renamed/resized file. mtime-diff `changed` rows still re-read; a missing stored fingerprint (first run after upgrade) treats the set as changed. CPU-only (never-matched rows never paid a GET — only `findChangedTracks`+`matchTrackToWebdav` CPU per click).
  - **queueWrapNotice false positive (`_rotateAfterAnchor`)**: `splitAt === 0` (first pool candidate already follows the anchor — natural order, NOT a wrap) still fired `queueWrapNotice.set(pool.length > 0)`. Patch: `queueWrapNotice.set(splitAt < 0 && pool.length > 0)` — the hint only appears when nothing ranks after the anchor.
  - **Test-WebDAV → auto incremental scan** (`SettingsView testWebdav`): on a successful test with a loaded library and no scan already running, commit creds + `setWebdavCredentials` + `scanAllNow(false)` — first-time setup populates ratings immediately instead of waiting for the next connect/restart.

- *[2026-08-07]* **Scanner collapse `scanAllNow`/`scanAllForceRescan` → `scanAll(shape)` (`npm run check` clean)**:
  - `src/lib/metadataScanner.ts` now exposes a single `scanAll(shape: 'modified' | 'force' = 'modified')` replacing both `scanAllNow(forceRescan)` and `scanAllForceRescan()`. `'modified'` is the old `scanAllNow(false)` body end-to-end (prior-fingerprint read, `refreshIndex` probe → `'error'` abort on failure, empty-index short-circuit, mtime diff + fingerprint-gated unmatched retry); `'force'` is the old `scanAllForceRescan()` (fresh `rebuildIndex` then re-read EVERY library track). The two had identical drain/progress/error plumbing — the only behavioral deltas were the probe path and the queue fill. The dead `scanAllNow(true)` signature is gone (no caller existed).
  - `MetadataScanProgress` gained optional `annotation?: string` — "Scanning all files..." / "Scanning changed files..." set at scan start and threaded through `updateScanProgress`, so the shared `metadataScanState` store carries the active scan kind for UI display. SettingsView's two status sites render it in the scanning branch ("{annotation} — X/Y (Z failed)"); button spinners unchanged.
  - Call sites: `SettingsView` (`startMetadataScan` → `'modified'`, `rescanAllMetadata` → `'force'`), `syncEngine.loadLibraryFromNavidrome` auto-incremental → `'modified'`, `buildIndex` still uses exported `rebuildIndex()` directly (plain index rebuild, no row scan). `refreshIndex()` export unchanged (debug tooling).
  - Replaced-in-file constant `shape` (module-level) is the single source for the annotation in both `scanAll` and `updateScanProgress` — no param threading.
  - NOTE: no unit-test framework exists in this repo; `npm run check` is the only test/verification gate.

- *[2026-08-07]* **Scan UX hardening + navidrome-mode preserve + starred normalization + dead code (5 steps, `npm run check` clean after each)**:
  - **Creds-bail now speaks** (`scanAll`/`rebuildIndex`): previously both returned silently when webdav credentials were absent — `SettingsView.startMetadataScan` calls `scanAll` unconditionally after `commitCredentials`, so an empty-field click left the store at `idle` with no feedback. Now: `scanAll` head reordered (creds check AFTER `scanGen !== myGen` guard) and sets `{'error', 'WebDAV credentials not configured'}` — gen-safe by ordering (no await between guard and set). `rebuildIndex` throws `Error('WebDAV credentials not configured')`; `SettingsView.buildIndex` catch shows that message (exact-match) else the generic "Index refresh failed". `testWebdav` auto-scan gate widened `status === 'idle'` → `idle || error` so a creds-less attempt recovers once fields are filled and Test is re-run; 'complete' stays inert (explicit Check Modified Ratings button is the re-scan control).
  - **Annotation threaded once**: module-level `activeAnnotation` set at `scanAll` head via `annotationFor(shape)`; all four progress writes + `updateScanProgress` read it (kills the per-call recompute that previously duplicated the ternary).
  - **A1 — scan no longer clobbers Navidrome-sourced ratings** (`processItem` success write): Navidrome is authoritative for rating/loved; file tags can be stale or server-edited. When `existing` row present AND `get(settings).ratingSource === 'navidrome'`, the scan keeps `existing.rating/loved/comments` and only refreshes `fileType/webdavPath/webdavLastModified/webdavBase/lastModifiedLocally`. Vanished-clear branch already preserved values; pending_sync skip untouched. Otherwise the previous clobber could flip the displayed rating to a stale tag value and a subsequent `commitFeedback` would push that stale value to the server.
  - **Dual-write toggle** (`writeTagsInNavidromeMode`, default off): new SettingsMap key (loadSettings keys list updated). When `ratingSource === 'navidrome'` AND the toggle is ON, `commitFeedback` marks the row `pending_sync` (was always `synced`), still passing through `webdavPath`/`webdavBase` so Push Changes writes the tag to the WebDAV file (MusicBee sees phone edits); path-less rows count as skipped by Push. Safety via existing mechanisms: `seedNavidromeFeedback` skips `pending_sync` (reconnect survives), A1 scans preserve locked rows, Push flattens after PUT + re-pend tail. UI: checkbox + label in the Ratings Source section (Sources tab), shown only in the `ratingSource === 'navidrome'` branch (replaces the old "file tags are left untouched" hint line — that claim is now conditional). Do NOT confuse with `syncToNavidrome`, the opposite-direction mirror (webDDAVu → server, force-on when flipping to navidrome, checkbox visible only in webdav mode).
  - **B3 — `isStarred`** (`navidromeApi.ts`): Subsonic REST sends `starred` as an ISO-8601 timestamp string when starred (absent otherwise), but some servers/clients serialize `boolean` — `Track.starred` was `boolean | string` with consumers having to guess via `Boolean()`. New exported `isStarred(x)`: `true`/`'true'`/`'1'`/non-empty non-`'false'`/`'0'` strings → true. `navidromeSongToTrack` maps `starred: isStarred(song.starred)`; `Track.starred` narrowed to `boolean`; `seedNavidromeFeedback` consumer simplified to `loved = starred === true`. `NavidromeSong.starred` stays raw `boolean | string` (API layer).
  - **D1 — dead code**: removed `db.ts::getMetadata` and `db.ts::deleteMetadata` (zero callers — only `bulkDeleteMetadata` used), removed the never-populated `Track.modifiedAt` (declared, rendered at TrackDetailPanel "Modified" row, written by NOBODY — `createdAt` row already shows the info; dropped the row to avoid `formatDate(undefined)` forever).

- *[2026-08-08]* **fileType decoupling — WAV et al. no longer coerced to 'mp3' (`npm run check` clean)**:
  - **Root cause of "File not found" for every WAV in File Matching**: `navidromeSongToTrack` (`navidromeApi.ts`) ran `(song.suffix || song.contentType || 'mp3').toLowerCase().replace('.', '')` through a `validTypes` whitelist `['mp3','flac','m4a','ogg','opus']` and coerced anything else to `'mp3'` — so a `.wav` file on the server could NEVER match a track stamped `mp3`, and `searchWebdavFiles` (extension-gated `endsWith('.${fileType}')`) could never surface it either. `scoreTrackMatches`/`matchTrackToWebdav*` all key off `track.fileType`, so the mismatch cascaded into zero candidates, an empty search, and a misleading "File not found" everywhere.
  - **Fix**: `deriveFileType(song)` in `navidrome.ts` — suffix is the actual file extension and is kept as-is; only when the suffix is missing does it parse the content-type last segment (`audio/x-wav` → `wav`) through `CT_SUBTYPE_ALIASES` (`x-wav`→`wav`, `wave`→`wav`, `mpeg`/`mp3`→`mp3`, `mp4`/`x-m4a`→`m4a`, `x-aiff`→`aiff`, `x-ms-wma`→`wma`); unknown subtypes (`octet-stream`, `mpegurl`) fall back to `mp3`. `Track['fileType']` (appState.ts) + `LocalMetadataStore.fileType` (db.ts) unions widened: `+ "wav" | "aac" | "aiff" | "wma"`. `getMetadataChunkSize` gets the new types in the 256 KB bucket (was 64 KB default — metadata read failures would double anyway).
  - **Stale-cache hardening — a cached row's fileType must never outlive the library Track**: `processItem` vanished-clear wrote `existing.fileType || track.fileType` (stale 'mp3' persisted forever after the fix); `seedNavidromeFeedback` and `commitFeedback` kept `existing?.fileType ?? track.fileType` — all now write `track.fileType` (library-stamped, i.e. the fixed mapper). `runManualWebDAVSync` (Push) resolves the write-branch type from the library via a `libTracks` map (`fileTypeOf`), ignoring a stale row type for `modifyMetadataBuffer` — a pre-fix cache row could otherwise send a `.wav` down the mp3 POPM path.
  - **WAV tag support verified**: taglib-wasm supports WAV (INFO chunk metadata; `FileTypeToTagFormat` maps `WAV` → `WAV` tag format), so the generic non-mp3 `props["RATING"]`/`"LOVE RATING"` write path in `tagWriter.ts` applies. NOTE: a re-scan (or force rescan) is needed to heal rows previously stamped `mp3`; re-running File Matching refresh (or Connect) re-derives the library from the fixed mapper.
- *[2026-08-09]* **Scan claim-guard race + ambiguous classification parity (review fixes, `npm run check` clean)**:
  - **Claim guard was racy under `CONCURRENCY = 6`**: each `processItem` built its `excludePaths` from a cache *snapshot* before its async `readFileMetadata` — two duplicate tracks processed concurrently both saw the empty snapshot and double-bound the same file (the guard only ever protected sequential passes; the manual-bind branch was equally exposed during its fetch). Fix: `claimedInScan: Set<path>` — per-run, cleared at `scanAllInternal` start — claimed **synchronously at match time** (check+add with no `await` in between, so it's atomic across microtasks); a sibling finding an existing claim counts the row scanned and leaves it unbound (it re-queues on a later run once the claim is gone and the winner's bind is in the cache). Held until the item actually binds; released when the item ends without binding (pending/ignored recheck, read failure); all releases are `scanGen === startedGen`-guarded so a superseded run can never delete a claim the new run made. Manual bindings claim their path up front, before their fetch.
  - **Candidates/scanner classification parity**: `matchTrackToWebdavCandidates` now mirrors `matchTrackToWebdav`'s tag-led-uncertain rule (`tagScore > nameScore && !tagCertain → ambiguous`), so the File Matching count line's ambiguous split agrees with the scanner's `ambiguousCount`. Since a tag-led ambiguous row has a single top candidate (not a tie), SettingsView's prompt copy distinguishes: >1 candidates → "Multiple files match equally", 1 candidate → "matches by its tags but not certainly — confirm:".

- *[2026-08-08]* **Tag cache + background probe for File Matching** (metadataScanner.ts + db.ts):
  - **`FileTagCacheEntry` (db.ts)**: per-`baseKey|path` probe result keyed `fileTagId(baseKey, path)` = `baseKey\0path` (null separator — `table.bulkGet` of `id`s deduped). Fields: `size` at probe time, `tags` (`FileTags`: title/artist/album/trackNumber), `status`: `'ok'` (identity tags present) | `'empty'` (reachable but no identity tags) | `'unreadable'` (probe poisoned — don't retry), `probedAt` (Info-cached clean-up; not relied on).
  - **`ensureTagProbe()` (metadataScanner)**: content-probes UNCLAIMED audio files so matching uses real in-file identity instead of only filenames. Selection is hint-gated (**never sweeps the server**): rank 2 = byte size equals an unclaimed track's size (strongest, runs first), rank 1 = filename/title hint (base minus track numbers matches/contains a normalized unclaimed title); files with no hint at all are skipped entirely (orphan forests stay untouched). Claimed paths (any row with `webdavPath`) and cache-fresh entries (same size) excluded. Concurrency 4, waits (`TAG_PROBE_RETRY_MS` = 800 ms) while a scan queue drains so the two never compete. Failures cached `'unreadable'` (never retried until size changes). `tagProbeState` store drives the Settings status line.
  - **`scanAll` wrapper**: every scan ends with `void ensureTagProbe()` — as long as tracks remain unclaimed, identity keeps being harvested in the background. Probe is gen-guarded + gated on `tagProbeActive`; a credential swap (`setWebdavCredentials`) bumps `tagProbeGen` to cancel an in-flight pass (results must never be fetched under one server and cached under another) — the finally resets the busy flag UNCONDITIONALLY or every future probe would no-op forever.
  - **Matching (`metadataReader.ts`)**: `entry.tags` (probed, size-verified via `applyCachedTags` — a size change invalidates) joins the scoring: `scoreAgainstTags` = title strong signal (exact 100 / substring 78−Δlen), artist corroborates (+25 exact / +12 partial), album (+20 exact / +10 partial), track number (+5), equal size (+5); `certain` = exact title AND exact artist — **the only verdict the scanner auto-binds on**. Tag verdicts beating weak name guesses are trusted (`score = tag.score`), but a tag-lead that is NOT certain surfaces as `ambiguous` (user confirms via the picker, which shows the tag context). Size-only fallback suppressed when probed tags contradict (`tagsContradictSize`).
  - **SettingsView File Matching**: probe status line ("Reading tags from N files (M remaining)…") under the section intro; candidate buttons (prompt + search) show "¶ Title — Artist — Album" tag context under the path. `refreshUnresolved` kicks `ensureTagProbe()` (fire-and-forget) so opening the tab starts harvesting.

- *[2026-08-08]* **File Matching scroll reset fix** (commit d099208, `npm run check` clean): the File Matching list in SettingsView reset to the top after every action because `refreshUnresolved()` set `unresolvedLoading` and the template swapped the whole `{#each}` list for a one-line "Loading…" — collapsing the tab's scroll container and clamping `scrollTop` to 0 with no restore. Fix is two-part: (1) template restructured so the list stays MOUNTED during a refresh (branch order: error → rows → loading → empty) with `class:opacity-50={unresolvedLoading}` dimming instead of unmount; "Loading…" now only shows when there are no rows yet. (2) `refreshUnresolved()` captures `prevTop = scrollContainer?.scrollTop ?? 0` before the async work and restores it in `finally` after `await tick()` (tick needed so the new rows are in the DOM before the scrollTop assignment clamps). Guard `prevTop > 0` skips restore when the user was at the top. Same pattern applies anywhere a loading flag unmounts a scrollable list.

- *[2026-08-09]* **Tag/WebDAV/Navidrome hardening round — POPM layout, rating scaling, taglib leaks, star batching, scrobble seeks, Depth fallback (all `npm run check` clean; POPM findings verified empirically with a scratch Node script + taglib-wasm WASI backend)**:
  - **POPM byte layout is FORK-specific — do NOT "spec-fix" it** (`tagWriter.ts`). Empirically verified against taglib-wasm 1.5.3 (synthetic ID3v2.3 + MPEG frame buffers, `getId3v2Frames`/`getRating` read-back): the fork's POPM model is `[owner \0][rating byte][counter(4)]` — the rating lands IMMEDIATELY after the null terminator, counter trails. Writing the ID3v2.3 §4.15 order (`[owner \0][counter][rating]`) makes the fork read the rating back as **0**. The old mmdrome body had a leading `0x03` byte that the fork parses as PART OF THE OWNER (`email "\x03MusicBee"`) — rating read was correct (0.8 = 204/255 exactly), but the owner string was tainted. Fix: bare `"MusicBee"\0 [rating] 0000` — identical read-back, clean owner (matches the fork's own `setRating(0.8,"MusicBee")` canonical render). Round-trip math preserved: write 90 → POPM 204 → `getRating()` 0.8 → `popmToLocalRating(204)` = 90.
  - **Non-MP3 rating scaling on read** (`metadataReader.ts::extractMetadataFromBuffer`): `getRating()` is normalized 0..1 for EVERY format (ID3v2 POPM/255, Vorbis RATING/100, MP4 freeform atom/100). The old code ran all formats through `popmToLocalRating(Math.round(r*255))`, inflating e.g. FLAC RATING=80 → 0.8*255=204 → grid 90. Now only `fileType === 'mp3'` uses the POPM grid; other formats map `Math.min(100, Math.max(0, Math.round(r*100)))`. The previously-dead `_fileType` param is now consumed (renamed `fileType`).
  - **taglib-wasm handle leaks + silent rating-zeroing** (`metadataReader.ts`): `extractMetadataFromBuffer`/`extractRawTagProperties` only disposed on the success path — an exception mid-parse leaked the WASM handle AND the catch swallowed post-open errors, returning `rating: 0` that the scanner then wrote into the row (clobbering a real rating it failed to read; also defeated `readFileMetadata`'s chunk-doubling retry, which only engages on thrown errors). Both now `try { reads } finally { dispose }` (dispose wrapped in its own try/catch so it can't mask the read error) and post-open errors RETHROW (scan counts the row failed; row keeps prior values). `modifyMetadataBuffer` was already safe (`taglib.edit()` disposes in a finally itself — taglib-class.js:139-152).
  - **Star/unstar URL length** (`navidromeApi.ts::setNavidromeStarred`): repeated `id` query params ride in a GET URL; 100 ids × 37 chars exceeded ~4 KB proxy limits (HTTP 414). Batch reduced to 30 (~1.1 KB + auth, safe under 2 KB limits).
  - **Scrobble seek handling** (`scrobbleManager.ts`): `onTick` counted pure position deltas, so a forward seek (0:10 → 3:30) manufactured ~200 s of "listening". Now: forward jumps ≥ `MAX_SEEK_DELTA` (5 s) are not credited (playhead re-anchors only); backward jumps ≥ 5 s reset `played` to 0 (the audio is re-listened); accrual is clamped to the track duration (poll glitches can't inflate past full length). Normal ticks (~0.25–1 s) never hit the threshold.
  - **PROPFIND `Depth: infinity` fallback** (`metadataReader.ts::buildWebdavFileIndex`): servers that refuse infinite depth (403/409/412, DoS protection) previously killed the whole index — `refreshIndex` returned false, every scan aborted. Per-directory probing is factored into `propfindDir(baseUrl,user,token,path,depth)` returning `{entries, collections}` (collection hrefs normalized by trailing slash); `buildWebdavFileIndex` tries `Depth: infinity` first and falls back to `crawlIndex()` — a BFS `Depth: 1` walk (concurrency 6, per-request 90 s timeout, `seen` dedupe) that emits IDENTICAL relative-path entries, so matching/scoring are untouched. A failing subdirectory is skipped with a `console.warn` (partial index degrades to "more no-match rows", never stale Push targets); only the root probe failing propagates. `Depth: 1` responses reuse the same `stripBasePath` for hrefs (absolute or relative both handled).
  - **Verification note**: taglib-wasm's Node/WASI backend requires `node --experimental-wasm-exnref` (else emscripten fallback crashes `dispose`), and `getId3v2Frames` throws `WasmMemoryError -6` on synthetic buffers — use `getRating()`/`getRatings()` for empirical POPM checks. The scratch script was deleted after use.
- *[2026-08-09]* **File Matching design v2 — no-signal auto-bind removed + Re-verify flows** ("Server URL updated" rows were wrongly bound: OTIKA/PSYQUI tracks all pointed at other albums' files). Root causes: size-only auto-bind (`nameScore === 40`, no name/tag signal), stale-base rows re-stamped by scans (claim guard serialized them so exactly one vanished per scan), and "Update file link" hitting `bindTrackToFile`'s double-bind conflict. Fixes:
  - **Auto-bind now requires evidence** (`matchTrackToWebdav`): gate `scored[0].score >= 40 && (scored[0].nameScore > 40 || scored[0].tagScore > 0)` — `nameScore === 40` (exactly the byte-size heuristic) never auto-binds; the `track.size` fallback (a duplicate of that path) deleted. Size-only matches stay visible as picker suggestions.
  - **Scans skip unverified bindings** (`processItem`): rows with `(!webdavBase || webdavBase !== currentIndexKey()) && webdavPath != null` whose path still EXISTS in the index are counted-and-skipped (counted scanned, never read/re-stamped/re-matched) after the pending/ignored guard, before the manual-bind branch. Vanished+stale rows keep the vanish-clear cleanup (path-gone cleanup preserves values — legit, no write).
  - **`verifyEntryAgainstTrack(track, entry)`** (metadataReader): the single verdict source — exact normalized title = 'verified', different = 'conflict', missing/unreadable = 'unknown'. Reconciliation flows (bulk + per-row) build the temp entry via `readFileMetadata` (not probe artifacts), so they audit what the file ACTUALLY says now.
  - **`reverifyStaleLinks()`** (metadataScanner, user-button only — never auto-runs on connect): batch (`CONCURRENCY = 6`) audit of stale-base rows; verified rows re-stamp `webdavBase`/`webdavLastModified` (rating/loved/syncStatus/matchSource preserved — pending edits become pushable) via a live-row re-check (user may have moved the row mid-run); conflict/unknown stay flagged, counted for the result line, never cleared.
  - **`reverifyTrack(trackId)`** (per-row "Update file link"): 'conflict' REFUSES the re-stamp (would canonize a wrong binding — the file's tags say X not Y, plain-English error); 'unknown' (untagged/unreadable) is allowed — the user's deliberate click IS the verdict. Reasons: `no-creds | index-failure | not-in-index | no-row | not-stale | conflict`. Re-resolves the live row after the verdict fetch (no stale-snapshot writes).
  - **Verified rows ALSO import the file's tags** (`writeReverified`): in `ratingSource === 'webdav'` mode with a present, non-`pending_sync` row, the re-verify write pulls `rating/loved/comments` from the just-fetched file — same semantics as the scanner's re-read path. Without this, rows bound (and polluted) under an old server keep the WRONG file's rating forever: the re-stamp makes the mtime diff read "unchanged", so incremental scans never re-read the now-verified file. (The old auto-restamp path DID read the file — that read was the poisoning mechanism for wrong bindings; the import is now gated behind the user-verified verdict.)
  - **Stale-row candidates in the picker** (`listUnresolvedMatches`): hoisted `allBoundPaths` (all bound paths across the library — reused by unbound rows too, replacing the per-row rebuild) and stale-base rows now get `matchTrackToWebdavCandidates(...).promptCandidates.slice(0,3)` with ALL bound paths excluded — including the row's own stamp — so a sibling-shared wrong file is never suggested.
  - **UI** (SettingsView): stale-base rows gain **Clear file link** (`doUnbind`); **Re-verify file links** button + result line (e.g. "Re-verified 5 — 2 don't match the linked file, 1 couldn't be read — check the rows below.") rendered only when `unresolvedCounts['stale-base'] > 0`, disabled while running/scanned. `doRestamp` → `reverifyTrack`. Rule of thumb: automatic paths never write unverified bindings; user-initiated paths may.
  - **Review round (`npm run check` clean)**: (1) **`normalizeForMatch` was CJK-blind** — `[^\w\s]` without the `u` flag strips Japanese (a large share of this library): "バビロン" → "", so EVERY CJK track scored as a filename near-match (`.includes("")` is always true; wrong auto-binds), `tagsContradictSize` never contradicted, and `verifyEntryAgainstTrack` could never reach 'verified' (both sides empty → 'unknown' → per-row re-stamp would canonize wrong bindings with no warning). Fixed to `[^\p{L}\p{N}\s]/gu` — letters/numbers of any script survive. (2) **`matchTrackToWebdavCandidates` got the same evidence gate** — a size-only TIE previously counted 'ambiguous' in File Matching while the scanner counted it 'no-match' (count divergence); the leftover `track.size` allCandidates fallback that re-suggested probe-*contradicted* files is deleted (size-only leads still surface as near-miss suggestions via `scored`). (3) `reverifyStaleLinks`/`reverifyTrack` share `writeReverified` + a full `FileMetadata` from the verdict fetch (no double GET). NOTE: title-scoring changes (like this CJK fix) only affect FUTURE scoring — already-unmatched rows are NOT re-queued by incremental scans (unmatched-retry is fingerprint/mtime-gated), so healing them needs a force "Rescan All Metadata" or the File Matching picker (suggestions re-score live on Refresh).
- *[2026-08-09]* **Rating-slider drag vs. concurrent scan race** (DetailView/TrackOptionsView/TrackDetailsModal): the range sliders used `bind:value` + `onchange={commit}` — commit only fired on release, so DURING a drag the metadataCache row stayed `synced` with the stale file rating, and a force rescan's `processItem` (whose post-fetch `pending_sync` re-check at metadataScanner.ts:624 protects *committed* edits) wrote the old value mid-drag; the `$effect` re-syncing `rating` from the store then yanked the thumb back to the original. Fix (all three views): leading-edge `commit()` on the FIRST `input` tick (row flips `pending_sync` instantly → scanner guard skips it) + `dragging` guard in the re-sync effect (`if (meta && !dragging)`) + final commit on `change` and `blur` (modal-close mid-drag no longer discards the edit). The data path was already safe (release-commit always lands after any scan write; no await between the scanner's re-check and its write) — this closed the visual window that could corrupt the committed value. No scanner change needed.
- *[2026-08-09]* **Layer rule + dead-code audit** (`npm run check` clean): **navidromeApi.ts is a stateless Subsonic HTTP layer + config cache** (`setCachedConfig`/`getCachedConfig`); **syncEngine.ts owns ALL policy** — credential resolution (`getNavidromeConfig`), baseKey-scoped + lastScan-validated song-library cache, cache fallback on failed load, and orchestration (`connectNavidrome`/`loadLibraryFromNavidrome`/`testWebdavConn`/`testNavidromeConn`). Rule: ONLY orchestration functions pull config; api functions always take an explicit `NavidromeConfig`. Deleted wrapper fossils: `navidromeApi.connectNavidrome` (pre-cache era — same name as syncEngine's with silently different semantics; a future import from the api layer would type-check but lose caching/fallback), `navidromeApi.getNavidromeSongStreamUrl` (one-line passthrough), `syncEngine.testNavidromeConnection` + `loadNavidromeSongs` (config-pulling wrappers absorbed by `connectNavidrome`). `NavidromeConnectResult` stays in navidromeApi (shared DTO). Added **Navidrome Test Connection** button (SettingsView Sources tab, WebDAV parity) — `syncEngine.testNavidromeConn()` pings `ping.view` only (no library load), writes the shared `navidromeConnection` store so the existing Connected/serverVersion/error block below Connect & Load renders the result. App.svelte no longer imports `setLibrary`/`initMetadataForTracks`/`seedNavidromeFeedback`/`metadataScanState`/`playbackSpeed` (unused there). NOTE: `connectNavidrome`'s `if (!forceRefresh && lastScan)` cache guard is INTENTIONAL correctness-first (no scan timestamp = can't trust cache) — don't "fix" it to cache when `getScanStatus` fails.

- *[2026-08-10]* **Recency-window refactor — `historyQueue` replaced by a queueManager-owned LRU `recentTrackIds` window (Phase 2 items 2.3/2.6 closed, `npm run check` clean)**:
  - **Model**: `QueueState.recentTrackIds` (persisted in the playQueue row, `RECENT_LIMIT = 100`) is a bounded rolling anti-repeat memory that COMPLEMENTS `userQueue` promo-based exclusion — it survives queue trims (`clearQueue` keeps only the current track), covers removed/skipped tracks, and is the rotation source when the pool runs short. `historyQueue`'s load bearing was misjudged in the audit as "near-redundant": the Clear-Queue case proves the played inscriptions matter (without them, auto-fill immediately replays the last 100 heard tracks after a Clear).
  - **Single writer**: `queueManager.markRecent(trackId)`/`advanceTo(index, markRecentId?)` via pure helpers in `src/lib/recentWindow.ts` (`inscribeRecent` = LRU dedupe + move-to-end + cap-at-write dropping the OLDEST; `sanitizeRecent` = load-time dedupe-keep-newest + cap). `advanceQueue`'s lazy `slice(0,100)` trim and the append-vs-prepend ordering inconsistency (`pushHistory` appended, `removeFromAutoQueue` prepended, trim kept the OLDEST 100) are gone. The fallback's `indexOf` recency sort was dead weight — the mode sort/shuffle fully overrode it; deleted. Old bugs fixed: the cap now keeps the NEWEST 100, and dedupe-on-inscribe means re-playing a cooled-down track refreshes its recency instead of wasting cap.
  - **One write per transition**: marks fold into the same `queue.update` as the index change in `advanceQueue`/`next()`/`prev()`/`playTrackAt` (a forward/backward jump marks the leaving track — previously unmarked)/native `_onNativeTrackChanged` (both branches; the re-adopt branch folds via `inscribeRecent` inside its own update)/`_onNativeEnd`. This also cuts `_scheduleNativeQueueSync` churn on native (each queue write tears down/re-arms the armed crossfade); standalone `markRecent` remains for the queue-end advance path where nothing else changes.
  - **Pool building** (`_buildPool`, shared by `replenishAutoQueue` + `rebuildAutoQueue` — the rebuild asymmetry where `rebuildAutoQueue` ignored the window is fixed): tier 1 fresh (not queued, not cooling, matching filters) -> tier 2 top-up (cooling-down tracks matching filters, when short) -> tier 3 rotation (every filter-matching track minus the active track and minus present-auto rows, when still short) — the queue REcycles instead of silently dying during long sessions (mitigates Phase 2 item 2.5's functional freeze; the memory-policy item stays open). Tier 3 in non-shuffle reuses `_rotateAfterAnchor` so the existing wrapNotice line ("Continuing from the top") surfaces recycling; never schedules the actively playing track; filters ALWAYS gate every tier (exhaustion never overrides user intent, only recency).
  - **Moved into queueManager**: `removeFromAutoQueue` (marks the removed track = "not now"), `removeFromUserQueue` (marks the removed id so a user-queue row removal can't instantly re-enter via auto-fill; active-row decrement semantics unchanged), `resetRecentWindow()` (Play All flows — both views previously zeroed `historyQueue: []` inline; the reset must NOT be dropped — explicit bulk replay supersedes recency). `pushHistory`/`removeFrom*` deleted from appState (dead code). Load tolerates legacy rows: `sanitizeRecent(q.recentTrackIds ?? q.historyQueue)` in `initStores`.
  - **Semantics preserved/verified**: Play All clears the window; Clear Queue KEEPS it (asymmetric by intent); prev/native/next/web-end/auto-removal all inscribe; loop-one restarts and same-index resumes do NOT. Window ops verified with a scratch Node script (`node scripts/scratch-verify-recent.mts` importing the pure module via Node 24 type stripping — asserts dedupe/move-to-end/cap-oldest-drop, sanitize-newest-keep, 10k-transition bounded/duplicate-free/recency-ordered simulation, legacy+inscribe integration; script deleted after passing, repo precedent). `queueWrapNotice` is cleared on both `replenishAutoQueue` early-returns (TODO 2.6); `_rotateAfterAnchor`'s set-clear contract unchanged.
  - Remaining Phase 2 queue work: `playNext`/`clearQueue` still in appState, QueueView inline reorder dupes, removeFromUserQueue active-row semantic decision (2.4), userQueue memory policy (2.5). KNOWN DELTA: a jump (playTrackAt) and prev now mark the leaving track — the "leaves the active slot => enters the window" invariant is complete, previously the jump path could immediately re-suggest the track you just jumped away from.
  - **Tier-3 recycling admits user-queued tracks — intentional (product decision, 2026-08-10)**: under pool shortage, tier 3 does NOT exclude `inUser` (parity with the pre-refactor fallback): a user-queued track can be admitted to the auto tail → a duplicate row in QueueView. Chosen deliberately: excluding `inUser` risks starving the tail when the user queue covers every filter-matching track (playback stops at queue end instead of cycling). Track removed from the user queue is still cooled by the window (tier 1/2 exclude it; only tier 3 can re-admit). UPDATE (2026-08-11): playing the auto copy now COLLAPSES (dedupe-append + slice) instead of "skipping" — the duplicate disappears from the tail, so it no longer cycles in place every N tracks; the user copy remains queued. Do NOT "fix" tier 3 without revisiting the starvation tradeoff.
  - **wrapNotice empty-pool fix**: `_buildPool` clears `queueWrapNotice` when `pool.length === 0` — the rebuild path (shuffle/sort change with everything filtered out) previously left a stale `true` from an earlier top-of-sort rotation, showing "Continuing from the top of the sort order" over an empty queue. Replenish's early-returns already cleared it; the empty-pool branch in `_buildPool` now covers rebuild too.

- *[2026-08-11]* **Queue anchor invariant — ALL queue mutations consolidated into `queueManager._mutateQueue` (audit issue 2.2, Commit A of the 8-issue playback/queue plan; `npm run check` clean)**:
  - **The helper** (`src/lib/queueManager.ts`): `private _mutateQueue(mutate: (q: QueueState) => QueueMutation | null)` is the single-write choke point for every length-changing mutation. Flow per mutation: capture `activeId` from `combined[activeIndex]` PRE-mutation → apply `mutate(q)` sections (`QueueMutation = Partial<Pick<QueueState, 'userQueue'|'autoQueue'|'recentTrackIds'>>` — `activeIndex` is deliberately NOT part of the mutation surface) → re-anchor `updated.activeIndex = [...newUser, ...newAuto].indexOf(activeId)` (fallback: pre-mutation index — for id-removed mutations, position semantics win; an out-of-range pre-mutation index implies no active id and the fallback is then −1 verbatim) → ONE `saveQueue`. `null` return = explicit no-op, NO store write. **Invariant: `combined[activeIndex]` points at the playing track whenever it remains queued.** Scope boundary: the helper is for ANCHOR-PRESERVING edits only — transitions that change the active id must NOT route here (`advanceTo`, native re-adopt: re-anchor would target the retired track; `removeFromUserQueue`: anchors to position, not id).
  - **Moved in**: `addToUserQueue` (append; NO-OP when the track already has a user slot — uniqueness rule), `playNext` (dedupe-MOVE of an existing user copy to just after the active row — plain insert only when absent; previously could append a duplicate row), `promoteToUser` (dedupe: keeps the existing user copy, drops the auto copy), `promoteToUserNext` (pre-check target === `combined[activeIndex]` → `promoteActiveTrack()`, else dedicated dedupe-move: the user copy is moved to the next slot and the AUTO copy is always dropped — a cross-section duplicate never survives), `moveToNext`/`moveToEnd` (user-internal reorder; single-reducer with `null` no-op checks — the fresh store value is the only read, no snapshot pre-checks), `removeFromAutoQueue` (pre-check target === active → plain promote, else filter + `inscribeRecent` folded into the SAME write), `promoteActiveTrack` (rewritten through the helper — **dedupe-append**: the id gains a user slot ONLY when it has none, and the auto queue is sliced from the id's first occurrence; this is the **tier-3 cross-section duplicate COLLAPSE** — see the 2026-08-11 follow-up bullet below; null when the active row isn't in auto), `clearQueue` (pure move, keeps the active track at user[0], preserves the recency window — asymmetric by intent vs. Play All), `reorderAll(userQueue, autoQueue)` (full section replace — QueueView drag `applyDrop` routes through it, the last hand-rolled view-side queue write is gone; auto-anchor: no active id → −1), `playAll(trackIds)` (full replacement for the album/artist Play All flows — userQueue = tracks, autoQueue cleared, activeIndex 0, recency-window reset folded in; deliberately NOT routed through the id re-anchor — the previous active track is replaced wholesale). Deleted from `appState.ts`: `addToUserQueue`/`playNext`/`clearQueue`. Deleted from `QueueView.svelte`: the four inline `promoteToUser`/`promoteToUserNext`/`moveToNext`/`moveToEnd` duplicates — call sites route through `queueManager.*` (`saveQueue`/`QueueState` imports dropped from QueueView; `saveQueue`/`queue` dropped from AlbumsView/ArtistsView). `TrackRow`/`TrackOptionsDropdown` route through `queueManager`; `SongsView` dropped its unused `addToUserQueue`/`playNext` imports. `removeFromUserQueue` untouched — it is the documented exception (see below). Phase 2 items 2.2 + the QueueView-dupes half of 2.3 CLOSED.
  - **Transition-window fact (why the helper exists)**: the active row can sit in auto at `auto[0]` (natural advance) or `auto[j>0]` (user jumped into an auto row via `playTrackAt`) until `promoteActiveTrack` runs after successful `el.play()` — row buttons are reachable in that window. Old inline code recomputed `activeIndex` arithmetically and went stale (e.g. `removeFromAutoQueue` removing a PRECEDING auto row left `activeIndex` one-past the active id; `promoteToUser`/`promoteToUserNext` shared the family). Every mutation now re-anchors by id — arithmetics like `adjustedIndex = activeIndex >= insertAt ? activeIndex + 1 : activeIndex` (dead code: `insertAt` was always `activeIndex+1`) are gone.
  - **removeFromUserQueue = DELIBERATE DEVIATION**: active-row removal anchors to the position playback continues from (decrement arithmetic), not the id — `indexOf` would fail by construction (id gone). Verified correct for any auto depth. Do NOT convert. (Item 2.4 remains open — the semantic decision itself, not the mechanics.)
  - **Tier-3 cross-section duplicate COLLAPSE**: a track occupying both user and auto rows now collapses instead of persisting — `promoteActiveTrack` appends a user slot ONLY when none exists (dedupe-append) and slices the auto queue at the copy, so the just-finished track can never be immediately up next (it re-enters only via a later tier-3 admission, which is blocked while it is the active id). The promote helpers never manufacture a second copy. Multi-copy rows can still exist as an explicit DRAG artifact — the preview's conversion rule can fold a cross-section duplicate into the user section (auto-internal duplicates are unreachable: the preview only ever moves auto rows INTO user) — and those persist until the user removes them (they queued them deliberately); the builders never resolve another op's fold. Re-anchor resolves to the first (only) user occurrence.
  - **Pure mutation core + fuzz verification (`src/lib/queueMutation.ts`)**: the mutation bodies live in a store/Dexie-free module (repo precedent: recentWindow.ts) — `applyQueueMutation` owns the re-anchor and dev-asserts (`import.meta.env?.DEV`) that no mutation drops the active id. `_mutateQueue` is a thin shell. Verified with a scratch fuzz (repo precedent: scratch-verify-recent.mts — `node` type-stripped import of the REAL builders, 10k iterations × 6 seeds, script deleted after passing): anchor invariant; manufacture-free builders (an id may enter a section only when the section is empty of it — 0→1 moves into user are legal, 1→2 duplication never happens); drag emulation faithful to `previewUserItems`/`previewAutoItems` (row count preserved); active-id containment (exactly one user copy, never an auto copy, in the fuzz's reachable set — production transition windows DO sit on auto rows until promote, which the fuzz does not model); promote-repeats covered by the targeted scenarios (dedupe-append collapse on the user-copy / auto-copy / cross-section paths).
  - **NOT IMPLEMENTED THIS SESSION (docs only)**: the remaining audit commits B–E (1.9/1.11/1.12 bg-exit loop-one + lock-screen seek + `playbackElement` play; 1.13 element-gated `onWaiting`/`onPlaying` + 10s stall watchdog; 1.2/1.3 `_advanceInBg` unguarded extraction + `_bgRetryInProgress` + 250ms end-of-track sleep tick with id-change fallback; 1.10 standby `play().catch`, `cancelNextTrack` on stop branches, crossfade replay-gain sync) are approved specs only — do not assume they exist when auditing `playbackManager.ts`/`audioManager.ts`/`mediaSession.ts`/`sleepTimer.ts`.
  - **General**: no unit-test framework; `npm run check` is the only gate; commit small + verified.

- *[2026-08-11]* **Settings inputs as single source of truth + offline/cache hardening + normalization (`npm run check` + `npm run build` clean)**:
  - **Debounce race eliminated (`SettingsView.svelte`)**: all Settings inputs (credentials + playback/library fields) wrote into local `$state` mirrors via a 300ms debounce and an `$effect` fed them back from the store — action buttons reading `$settings`/Dexie in the debounce window used stale values. The mirrors + sync `$effect` + debounce are deleted; every input calls `updateSetting(field, value)` directly on `input`, so the store is always the single source of truth and the previous `commitCredentials()`-awaits-everything workaround remains for persistence/trimming right before a network call. All setters (`setPreload`/`setCrossfade`/`setTapeMode`/`setReplayGainMode`/`setRatingSource`/`setSyncToNavidrome`/`setWriteTagsInNavidromeMode`/`setScrobbling`/`setSnapTolerance`) now read current state from `$settings` instead of the removed mirrors; `onInput(field)` is generated once per field and bound with `oninput={...}` (a single generated-per-field handler, no per-keystroke closure creation). The `reconcileRatings` helper is kept (still called from `setSyncToNavidrome`).
  - **Normalized credentials (`commitCredentials`)**: `webdavUrl`/`webdavUser`/`navidromeUrl`/`navidromeUser` are `.trim()`ed (stray whitespace breaks request URLs and cache baseKeys); tokens/passwords are preserved byte-for-byte. Applied on every button action (Test, Connect, Push, Re-verify, etc.) that calls `commitCredentials`.
  - **lastScan-independent song-library cache (`navidromeApi.ts`)**: the cache is now validated purely on `baseKey` (server identity), dropping the "did the previous load WITH a valid scanStamp write a cache" dependency. This fixes the common "offline / server unavailable at startup → empty library" deadlock: previously only a load that was both successful AND had a usable lastScan wrote the cache, so an offline start (no scan stamp) could never be cached and always re-failed. The persistent baseKey + lastScan are still written on success; the `getScanStatus` call now happens only before the cache does the light check. Cache correctness note: a stale cache can go undetected if the server's scan timestamps are never updated — acceptable, the "Connect & Load" button forces a refresh.
  - **Offline cached-library fallback (`connectNavidrome`/`loadLibraryFromNavidrome`)**: `connectNavidrome` returns the cached snapshot when the live load fails (empty set + error) AND the cache belongs to this baseKey, even when the flow errored before reaching Navidrome — the result keeps `loadResult.error` so the UI shows the underlying error. `loadLibraryFromNavidrome` now only bails when `!connected && songs.length === 0`.
  - **WebDAV auto-scan gated on `navigator.onLine` (`loadLibraryFromNavidrome`)**: an offline startup that fell back to the song cache would fire a doomed PROPFIND and paint a scan error the user didn't ask for. The automatic `scanAll('modified')` is now skipped when online status is false (no-op on iOS native where `navigator.onLine` is reliable).
  - **WebDAV error attribution (`metadataScanner.ts`)**: error strings that were previously generic ("Index refresh failed — is the WebDAV server reachable?") or ambiguous now lead with the source: "WebDAV index refresh failed — is the WebDAV server reachable?" and "WebDAV credentials not configured". `SettingsView.buildIndex`'s catch matches `err.message === 'WebDAV credentials not configured'` and otherwise uses the new string. Users with no WebDAV server no longer see a confusing generic error after connecting Navidrome.
  - **Chunked IndexedDB bulk ops (`db.ts`)**: `bulkUpsertMetadata` and `bulkDeleteMetadata` now split into `BULK_CHUNK = 2000`-row transactions instead of one giant `bulkPut`/`bulkDelete` — a first connect of a large library could spike memory and time out/abort in iOS WebKit.
  - **Gotcha (Settings inputs)**: never reintroduce local mirror + `$effect` + debounce for these fields; the store write-through IS the fix. `commitCredentials` is what guarantees persistence before a network call (the store is in-memory-first).
  - **Sources tab reorder (`SettingsView.svelte`)**: Navidrome moved above WebDAV (was the other way around — WebDAV's "Check Modified Ratings" button and live scan-status line made WebDAV look like the primary source). Navidrome keeps its Test Connection + Connect & Load buttons and its Connected/load-result lines; the WebDAV section below now leads with credentials + Test Connection and the primary action became **Rescan All Metadata** (`rescanAllMetadata` → `scanAll('force')`, sharing the Library tab's full-rewrite handler — the button shows a green checkmark + "Done — rescan again" once a complete-error-free scan lands). The incremental `startMetadataScan` (`scanAll('modified')`) remains the Library tab's "Check Modified Ratings" (line 862). The CORS hint under the WebDAV title is now platform-gated: native explains it needs no CORS setup; web retains "Requires CORS to be allowed on the server for this site."
